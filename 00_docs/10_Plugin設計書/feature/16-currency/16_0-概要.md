# 16_0-概要

## 1. 目的

`currency` feature は、アカウントの `CURRENCY` インベントリを通貨 GUI へ表示し、組み込みゴールド額面の合計・交換を提供する。残高モデル、API repository、独立した永続化処理は持たず、実行中の正本と保存境界は inventory feature に委譲する。

## 2. 通貨の保持方針

- 通貨は `PlayerInventoryState` 内の `InventoryType.CURRENCY` に item ID ごとの entry として保持する。
- `CURRENCY` は同一 item ID を集約し、通常のスタック上限を適用しない。
- `99a00001` entry がない場合も通貨一覧には 0 個の表示 item を補完する。
- 基本額面 `99a00001` は旧 ID `gold` / `ast_gold` を残高取得・消費・表示で合算する。
- 報酬 item のカテゴリが `currency` の場合、inventory feature が通常 BAG ではなく `CURRENCY` を付与先に選ぶ。

## 3. 組み込みゴールド

`GoldDenomination` は次の 7 額面を低額面順に定義する。

| item ID | 表示名 | ゴールド換算値 |
|:--|:--|--:|
| `99a00001` | ルーンの金片 | 1 |
| `99a00002` | ヴァルハラの金貨 | 10 |
| `99a00003` | ミズガルズの黄金インゴット | 100 |
| `99a00004` | アースガルズの黄金ブロック | 1,000 |
| `99a00005` | ビフレストのダイヤ | 10,000 |
| `99a00006` | 神域のダイヤ結晶 | 100,000 |
| `99a00007` | ユグドラシルの星核 | 1,000,000 |

旧 ID `gold`、`ast_gold`、`gold_coin`、`gold_ingot`、`gold_block`、`gold_diamond`、`gold_diamond_block`、`yggdrasil_star_core` は既存インベントリの移行互換として対応する。

支払いは低額面を優先し、不足する場合だけ所持中の上位額面を 1 個崩して釣り銭を生成する。合計値や中間演算が `long` 上限を超える場合は飽和または失敗として扱う。

## 4. GUI

- 通貨一覧は menu feature の 54 slot ページング GUI で表示する。
- 通貨一覧の item は残高表示専用とし、`CURRENCY` から通常 BAG へ取り出せない。旧データ等で通常 BAG／ホットバーに存在する通貨 item は、下部のプレイヤーインベントリから `CURRENCY` へ戻せる。
- 通貨一覧の slot 51 は、`99a00007` を 1 個以上所持する場合だけ両替アイコンを表示し、同アイコンから両替 GUI を開ける。条件未達時は他の空き slot と同じダミー pane を表示する。
- NPC interaction `CURRENCY_EXCHANGE` からは所持条件なしで両替 GUI を開く。
- 両替 GUI は隣接額面間の等価交換だけを行う。
- 両替 GUI は 27 slot の `HotbarShortcutGuiHolder` で開き、slot 10 から 16 へ低額面順に `GOLD_NUGGET`、`RAW_GOLD`、`GOLD_INGOT`、`GOLD_BLOCK`、`DIAMOND`、`DIAMOND_BLOCK`、`NETHER_STAR` を配置する。slot からは同じ位置の額面を逆引きする。
- 通貨一覧の slot 51 は両替導線、slot 50 は背景 pane とする。星核所持時は金色の「ゴールド両替所」とクリック案内 lore を表示し、未所持時はダミー pane を表示する。

## 5. 構成要素

| 物理名 | 責務 |
|:--|:--|
| `CurrencyService` | 通貨表示、残高参照、額面交換、通貨 item 判定 |
| `GoldDenomination` | 組み込み額面、表示情報、隣接額面 |
| `GoldCurrencyCalculator` | 合計、分解、低額面優先支払い |
| `CurrencyGuiView` | 通貨一覧のページング描画と両替アイコン |
| `CurrencyExchangeGuiView` | 両替 GUI 描画 |
| `CurrencyExchangeGuiEventHandler` | 両替 GUI の open / click / drag |

## 6. 依存・連携

| feature | 依存内容 |
|:--|:--|
| inventory | `CURRENCY` の entry、スナップショット、増減、保存 |
| item | 通貨 item 定義、表示用 `ItemStack`、カテゴリ判定 |
| menu | 通貨一覧 GUI、通常 BAG との移動、両替所への導線 |
| mob / mail / shop / trade / world | 報酬付与、支払い、プレイヤー間移転、帰還コストの呼び出し元 |

## 7. 変更影響

- 組み込みゴールド額面、換算値、互換 ID を変更した場合は [[16_0-概要]]、[[16_1-モデル定義]]、[[16_3-メソッド仕様]]を更新する。
- 通貨 GUI の移動量、ページング、両替導線を変更した場合は [[16_2-ユースケース]]、[[16_4-統合フロー]]を更新する。
- `CURRENCY` の保存・増減・補償処理を変更した場合は inventory feature と [[16_3-メソッド仕様]]、[[16_5-例外・ログ・運用]]を更新する。
- 呼び出し元の支払い・報酬契約を変更した場合は、該当 feature の設計書も更新する。
