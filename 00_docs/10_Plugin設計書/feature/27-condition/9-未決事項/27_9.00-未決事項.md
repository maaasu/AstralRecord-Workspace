# 27_9.00-未決事項

## 1. filebase 化の範囲

初期実装では `ConditionType` の既定値をコード定義で持つ。
将来、状態異常定義を `40_filebase` へ移すかは未決。

検討対象:

- duration 既定値
- tick interval
- power coefficient
- stack 上限
- 表示 particle / material
- boss resistance profile

## 2. 属性耐性ステータスの追加

初期実装では `DamageElement` を導入するが、属性耐性 `StatusType` は追加しない。
全属性倍率は 1.0 とする。

将来候補:

- `FIRE_DAMAGE`
- `ICE_DAMAGE`
- `POISON_DAMAGE`
- `LIGHTNING_DAMAGE`
- `HOLY_DAMAGE`
- `DARK_DAMAGE`
- `FIRE_RESISTANCE`
- `ICE_RESISTANCE`
- `POISON_RESISTANCE`
- `LIGHTNING_RESISTANCE`
- `HOLY_RESISTANCE`
- `DARK_RESISTANCE`

追加する場合はプラグイン側の `StatusType.kt` と equipment / class / mob スキーマの参照説明を更新する。

## 3. 状態異常の永続化

プレイヤー logout / login を跨いで状態異常を維持するかは未決。
初期実装では logout 時に解除し、再ログイン時に復元しない。

## 4. PvP 適用

プレイヤー同士の状態異常付与を許可するかは未決。
初期実装では existing combat の対象判定に従い、Mob 戦闘を主対象にする。

## 5. UI 表示

メニュー GUI や HUD に状態異常一覧を表示するかは未決。
初期実装では頭上 TextDisplay、本人向け短時間表示、周囲 particle を優先する。

2026-07-07 実装メモ:

- 初回コード実装では、戦闘影響、DoT、操作制限、stack、周期 task、particle、actionbar、fire ticks を先行実装する。
- TextDisplay / BlockDisplay の実体生成と追従管理は、表示専用 entity の lifecycle 設計と合わせて後続実装に分離する。
