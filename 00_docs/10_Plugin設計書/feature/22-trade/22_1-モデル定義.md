# 22_1-モデル定義

## TradeRequest

| 要素 | 型 | 意味 |
|:--|:--|:--|
| `requestId` | `UUID` | request ID |
| `senderUuid` / `targetUuid` | `UUID` | Bukkit player IDs |
| `senderName` / `targetName` | `String` | 作成時表示名 |
| `createdAt` / `expiresAt` | `Instant` | 作成・期限。期限は作成から 60 秒 |
| `status` | `TradeRequestStatus` | mutable status |

status は `PENDING`、`ACCEPTED`、`CANCELLED`、`EXPIRED`。

## TradeSession

| 要素 | 型 | 意味 |
|:--|:--|:--|
| `sessionId` | `UUID` | session ID |
| player A/B UUID | `UUID` | online player identity |
| player A/B account ID | `UUID` | inventory ownership identity |
| player A/B name | `String` | 表示名 |
| player A/B items | `List<ItemStack>` | GUI 表示用 clone。API commit 前に inventory 正本を escrow へ移さない |
| player A/B source inventory entry IDs | `List<UUID>` | 各提示 clone に対応する起点 `inventory_entry_id`。数量 reservation と対で API commit 時に再検証する。同じ source entry が複数 clone に分割された場合、API 明細では source entry 単位に数量を合算する |
| player A/B gold amount | `long` | 提示 gold。0 以上 |
| player A/B ready | `boolean` | 確定意思 |
| `status` | `TradeSessionStatus` | `OPEN`、`COMMITTING`、`COMPLETED`、`CANCELLED` |
| `openedAt` / `updatedAt` | `Instant` | 開始・最終更新 |

items または gold が変わっても双方 ready は維持する。提示中は起点 entry と数量の reservation だけを保持し、NORMAL inventory の正本 state は API commit まで変更しない。

## GUI holder

- `TradeGui.TradeHolder(sessionId, viewerUuid)`
- `TradeCancelConfirmGui.CancelHolder(sessionId, viewerUuid)`
- `GoldAmountSettingGui.GoldAmountHolder(sourceKey, sessionId, viewerUuid, amount, maxAmount)`。holder 内部の `step` は 1 から 10^18 の10進調整単位

概念的な `TradeGuiState` ではなく、上記 physical holder を実装契約とする。

## Layout

- size: 54
- 自分の offer slots: `10,11,12,19,20,21,28,29,30,37,38,39`
- 相手の表示 slots: `14,15,16,23,24,25,32,33,34,41,42,43`
- gold: 22、ready: 49、divider: `4,13,31,40`
- cancel confirm: size 27、cancel 11、back 15
