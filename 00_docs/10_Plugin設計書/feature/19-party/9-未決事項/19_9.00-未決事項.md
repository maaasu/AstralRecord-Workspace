# 19_9.00-未決事項

## 招待の有効期限

`PartyInvite.createdAt` は保持しているが、現在の `PartyService` は期限切れ判定・定期削除・期限表示を実装していない。

決定が必要な項目:

- 有効時間と時間源
- 承認時の期限再検証
- target／leader への期限切れ通知
- logout、解散、leader 変更時の invite cleanup 規則
- GUI の残り時間表示要否

方針確定までは「一定時間で失効する」と実装済み仕様へ記載しない。
