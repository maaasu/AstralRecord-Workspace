# 03_3.03-コマンド

## 1. ログイン報酬表示コマンド（廃止）

`LoginBonusCommand` と `/loginbonus` は廃止しました。

ログインボーナスは、NPC の `interactions.rightClick` に次の GUI アクションを定義して表示します。

```yaml
- id: gui
  params:
    type: LOGIN_BONUS
```

このアクションは [[03_3.02-サービス]].ログインボーナス表示 を実行します。
