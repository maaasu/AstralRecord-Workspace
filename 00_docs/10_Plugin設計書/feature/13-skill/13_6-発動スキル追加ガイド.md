# 13_6-発動スキル追加ガイド

## 1. 目的

本章は、新しいプレイヤー発動スキルを追加する際の実装境界と確認項目を定める。
個別スキルはゲームデザインを読み取れる小さなクラスとし、Bukkit scheduler、ダメージ経路、Mob 検索、particle 表示を個別実装で作り直さない。

## 2. 責務の分離

| 層 | 責務 | 配置 |
|---|---|---|
| マスタデータ | 表示、共通コスト、詠唱、要求レベル | `40_filebase/30.features.skill/` |
| 個別 executor | 形状、射程、倍率、命中後の効果 | `feature/skill/executor/active/<profession>/` |
| 共通実行基盤 | 当たり判定、custom combat、演出、移動、projectile、遅延処理、一時効果 | `feature/skill/active/` |
| 登録 | executor 一覧の組み立て | `ActiveSkillExecutorCatalog` と職業別 catalog |
| 共通発動制御 | 個体所持、使用許可、コスト、クールダウン、詠唱 | `SkillService` |

## 3. 追加手順

1. `v1.<skill-id>.yml` を作成する。
2. コード定義の発動スキルでは `id` と `implementationId` を同じ値にする。
3. `resourceType` / `resourceCost` / `cooldownTicks` / `castTimeTicks` / `requiredLevel` は top-level に記載する。
4. 個別 executor を 1 スキル 1 クラスで作成し、対応する職業別 catalog へ追加する。
5. 試作中のスキルは `administrator.usableSkills` へ使用許可を追加し、対応する自動生成ジェムを取得して別途習得して確認する。
6. Plugin テスト、API テスト、filebase 検証を実行する。

### 最小 YAML 例

```yaml
schemaVersion: 1
id: swordsman_example
type: SKILL
implementationId: swordsman_example
name: "&f例示技"
description: "&7前方を攻撃する。"
icon: IRON_SWORD
lore:
  - "&7前方3mへ近接攻撃力100%のダメージ。"
cooldownTicks: 40
resourceType: ENERGY
resourceCost: 10
castTimeTicks: 0
requiredLevel: 1
maxLevel: 1
sigilSlotsByLevel: []
allowedSigilIds: []
gem:
  rarity: COMMON
  tradeable: false
  sellable: false
tags:
  - active
  - swordsman
```

`params` は省略を既定とする。射程や倍率だけを変えた多数の定義を一つの executor で共有するなど、振る舞いをデータ駆動にする明確な理由がある場合だけ使用する。

## 4. 個別 executor の規約

- `PlayerActiveSkillExecutor` を継承し、コンストラクタで skill ID と `ActiveSkillServices` を渡す。
- `castPlayer` には「対象形状を決める → 効果を適用する → 演出を出す」というスキル固有の流れだけを記述する。
- 消費リソースとクールダウン値を executor から返さない。成功時は `context.success()` を返し、`SkillService` がマスタ定義を一元適用する。
- ダメージは `SkillCombatService` から custom combat へ流す。Bukkit の `LivingEntity#damage` を直接呼び出さない。
- Mob 検索は `SkillTargetingService` を使い、NPC、別 world、死亡済み Mob を個別クラスで再判定しない。
- particle は `SharedParticleDefinitions` と `SkillEffectService` を使う。プレイヤーの particle 密度設定を無視する `World#spawnParticle` の直接使用を行わない。
- 数 tick 後の攻撃や連続攻撃は `SkillTaskService` で発動者 UUID と紐付ける。退出、死亡、world 変更後に効果を残さず、攻撃ステータスは `SkillCastContext.statusSnapshot` の発動時点値を使う。
- 短距離移動は `SkillMovementService` を使い、移動不能状態、全身幅を含む経路遮蔽、足場、頭上空間を確認する。

## 5. 共通サービス選択

| したいこと | 使用するサービス |
|---|---|
| 扇形・直線・円柱の当たり判定 | `SkillTargetingService` |
| 属性ダメージ・状態異常・ヘイト・ノックバック | `SkillCombatService` |
| 線・円・弧の particle、効果音 | `SkillEffectService` |
| 貫通と swept capsule 命中を持つ飛翔体 | `SkillProjectileService` |
| ダッシュ・バックステップ・ブリンク | `SkillMovementService` |
| 遅延攻撃・複数 wave・罠 | `SkillTaskService` |
| 一定時間の被ダメージ・与ダメージ・ノックバック補正 | `TemporarySkillEffectService` |

## 6. レビュー・テストチェック

- skill ID、`implementationId`、executor の登録キーが完全に一致している。
- コストと cooldown は YAML の top-level 定義だけが決定している。
- ブロック遮蔽、複数命中順、最大命中数、同一対象への重複命中を確認する。
- 衝突に応じて発動する projectile は、最初のMob衝突・地形Block衝突・最大射程への無衝突到達を区別する。Mob の swept capsule 交差距離と Block 面の距離を比較し、Mob が厳密に手前なら ENTITY、同距離または Block が手前なら BLOCK とする。Block 着弾では表示用の正確な衝突地点と遮蔽判定・範囲効果用の手前側中心を分け、着弾 Block 自身で手前側の Mob を遮蔽しないことを確認する。最大射程へ無衝突で到達したときの効果発生は個別skill仕様に従い、着弾時だけ起爆するskillはそこで効果を発生させない。
- 命中しない場合にダメージ、状態異常、ヘイトが発生しない。
- 退出、死亡、world 変更、Plugin 停止時にタスクと一時効果が破棄される。
- `params` を使わない executor は、マスタデータに `params` が追加された場合を読込時エラーとして検出する。
- API から取得した定義と仮想ジェムが登録され、ジェムで習得した個体を Administrator の使用許可でバインド・発動できる。

`ActiveSkillExecutorCatalog` が返す built-in player executor は次の 13 ID とし、各 ID を重複なく一度だけ登録する。全要素は `PlayerActiveSkillExecutor` として共通 player 発動契約へ接続する。

| 職業 | built-in skill ID |
|:--|:--|
| adventurer | `adventurer_astral_edge`, `adventurer_blast_arrow`, `adventurer_mana_burst`, `adventurer_smash`, `adventurer_quick_shot`, `adventurer_lightning_bolt` |
| swordsman | `swordsman_shield_drain`, `swordsman_flame_rush`, `swordsman_challenging_roar`, `swordsman_bastion_strike` |
| hunter | `hunter_fade_shot`, `hunter_arrow_rain`, `hunter_crash_arrow` |
| mage | 未定 |

## 7. 冒険者クイックショットの実装契約

`adventurer_quick_shot` は、通常クラスでは冒険者だけが `usableSkills` で使用許可を得る、旅人の射撃を置き換える基礎的な単体・間接攻撃である。マスタは `v1.adventurer_quick_shot.yml`、executor は `AdventurerQuickShotExecutor`、登録先は `AdventurerSkillExecutorCatalog` とする。

- 発動時に照準方向へ、Mob だけを対象とする仮想飛翔体を1本発射する。`SkillProjectileService` の swept capsule 判定を使用し、PvP対象は選択しない。
- 飛翔体は射程12m、速度2.2m/tick、当たり半径0.45m、非貫通・最大1体とする。ブロック遮蔽で停止し、水は遮蔽として扱わない。
- 最初に命中したMob 1体だけへ `RANGED` / `NONE` の間接攻撃力150%を適用する。Lv.2～5の `SKILL_DAMAGE_INCREASE +5%` により、Lv.5の実効倍率は180%となる。
- 消費はEN 6、cooldownは40 tick（2.0秒）、詠唱は0 tickとする。通常攻撃との追加GCD・キャンセル処理は設けない。
- 演出は `SKILL_HUNTER_ARROW` と `SKILL_HUNTER_IMPACT` を流用し、`ENTITY_ARROW_SHOOT` を高めのpitchで鳴らす。新しい共有particle定義は追加しない。
- アクションリングと左クリックバインドは既存の有効な主手 `WEAPON` 判定をそのまま使用し、弓限定の追加条件は設けない。

## 8. 冒険者ライトニングボルトの実装契約

`adventurer_lightning_bolt` は、冒険者が扱う雷属性の基礎魔法である。マスタは `v1.adventurer_lightning_bolt.yml`、executor は `AdventurerLightningBoltExecutor`、登録先は `AdventurerSkillExecutorCatalog` とする。`id`、`implementationId`、executor の登録キーは同一値にそろえる。

- MP 10、cooldown 60 tick（3.0秒）、詠唱4 tick（0.2秒）、射程14m、飛翔体速度2.8m/tick、当たり半径0.45mとする。発動時に青白い収束粒子を出した後、視線方向へ非貫通の仮想飛翔体を1本だけ発射する。
- 最初に命中したMob 1体へ `MAGIC` / `LIGHTNING` の基礎倍率145%を1回適用する。Lv.2～5の `SKILL_DAMAGE_INCREASE +5%` は主攻撃と連鎖攻撃の双方へ共通 combat 経路で適用する。主対象が `SHOCKED` である必要はない。
- 主対象から水平半径5m・上下5m以内で、`SkillTargetingService.inRadius` が返す視線遮蔽のないMobを距離順・UUID順に確認する。そのうち `SkillCombatService.hasCondition(target, ConditionType.SHOCKED)` が真の別対象だけを最大2体まで選び、各対象へ基礎倍率40%の `MAGIC` / `LIGHTNING` を1回ずつ適用する。候補を先に最大数へ切らず、感電対象を抽出してから最大2体へ切るため、近い非感電Mobがいても後続の感電Mobを選べる。
- 連鎖は主対象の命中処理で1段だけ発生し、連鎖先を新しい中心として探索しない。主対象は連鎖対象から除外し、候補リストは共通サービスの一意なMobを使うため、同一発動内の再命中・元対象への戻り・循環は発生しない。本スキルは `SHOCKED` を付与せず、感電していない敵へ連鎖しない。
- 対象選択は既存 active skill と同じMob専用規約に従うため、PvEの管理対象Mobのみを対象とし、PvP、NPC、別world、死亡Mobは対象外とする。Blockまたは最大射程で終端した場合はダメージも連鎖も発生しない。
- 現行リポジトリには `mage_chain_lightning` の定義・executor は存在しないが、比較上の無条件複数対象連鎖とは異なり、ライトニングボルトは通常時に単体で完結し、`SHOCKED` の配置がある場合だけ追加火力を得る。
- 通常命中は `SKILL_MAGE_LIGHTNING` の青白い雷光と `ENTITY_LIGHTNING_BOLT_IMPACT` で示す。連鎖時は主対象から各感電対象へ `CONDITION_SHOCKED_SPARK` の線分を描き、主命中と連鎖を視覚的に判別できるようにする。
- Lv.5 の基礎倍率は主対象174%、連鎖48%。Lv.30の `R=151.4`、命中100%、属性耐性・会心・防御・操作時間を除いた主対象DPSは `151.4 × 1.74 ÷ 3.0 = 87.81`。感電対象を2体満たす理想条件では `151.4 × (1.74 + 0.48 × 2) ÷ 3.0 = 136.26` となり、通常時の単体性能をクイックショットより低く、条件成立時の合計を同程度へ抑える。
- 冒険者の `usableSkills` と Administrator の検証用 `usableSkills` を許可し、ジェムは `skill_gem_exchange` の1ページ目で無印原石1個から1個を交換する。初級以上の原石から個別ジェムへは直接交換しない。専用skilltree nodeは追加しない。

## 9. 冒険者マナバーストの実装契約

`adventurer_mana_burst` は、冒険者が接近した複数の敵を正面へまとめて攻撃する、無属性・魔法属性なしの基礎範囲魔法である。マスタは `v1.adventurer_mana_burst.yml`、executor は `AdventurerManaBurstExecutor`、登録先は `AdventurerSkillExecutorCatalog` とする。冒険者と Administrator が `usableSkills` で使用でき、`skill_gem_exchange` の1ページ目で無印原石1個から個別ジェム1個を交換して習得する。

### 9.1 数値と対象形状

- 発動者の目線方向を中心に、射程7m・全角60度の前方扇形へ判定する。全角は180度以下の値だけを許可し、360度の周囲攻撃へ変化できないよう executor でも検証する。
- `SkillTargetingService.inCone(player, range, angle, maxTargets, true)` を使用し、最大6体、ブロック遮蔽ありで選択する。既存の Mob 選択規則により、NPC・別world・死亡済みMob・PvP対象を除外し、Bossを含める。
- 対象は `inCone` の既存ソートで発動者の現在位置から近い順に選び、同一対象へ遅延・反復・副対象処理を行わない。したがって1回の発動で同一対象へ1回だけ命中する。
- 選択した各対象へ `MAGIC` / `NONE` の `damageRatio` 1.10（Lv.1）を1回適用する。Lv.2～5は `SKILL_DAMAGE_INCREASE +5%` を1回ずつ加え、Lv.5は132%となる。状態異常、属性効果、ノックバック、ヘイト追加処理は持たない。
- 消費MPは13、cooldownは60 tick（3.0秒）、詠唱は2 tick（0.1秒）とする。射程12m・MP10・3.5秒の単体魔法比較基準より短射程・高コストとし、単体では明確に高効率にならないようにする。

### 9.2 演出

発動者の目線から前方0.35m・下方0.25mの手元／武器先端近傍へ `MAGIC_PROJECTILE_CORE_DUST` の紫色の魔力波を複数の円弧として表示し、射程と全角を視認できるようにする。最終円弧へ `MAGIC_IMPACT_ENCHANT` を重ね、星光を含む半透明の波として表現する。選択対象ごとに `MAGIC_IMPACT_ENCHANT` と `MAGIC_IMPACT_DUST` を対象位置へ表示し、魔力粒子が弾ける命中表現とする。既存の `SkillEffectService` / `ParticleDisplayService` を経由し、直接 `World#spawnParticle` は呼び出さない。

### 9.3 テスト契約

`ActiveSkillExecutorDesignTest` は13個の built-in ID の一意性とマスタ params の必須値を検証する。`AdventurerManaBurstExecutorTest` は、射程7m・全角60度・最大6体・遮蔽判定ありの扇形選択、`MAGIC` / `NONE` の1回命中、複数対象への命中粒子、360度攻撃や状態異常処理がないことを検証する。

## 10. シールドドレインの実装契約

`swordsman_shield_drain` は、敵のシールド破壊を主目的とする単体近接スキルである。マスタは `v1.swordsman_shield_drain.yml`、executor は `SwordsmanShieldDrainExecutor`、登録先は `SwordsmanSkillExecutorCatalog` とする。使用許可はソードマンの `usableSkills` から初期配布し、Administrator の `usableSkills` には検証用許可を残す。skilltreeには配置しない。

### 10.1 数値・対象・演出

- 発動者の前方6m・全角40度から、遮蔽のない最寄りMob 1体だけを選ぶ。既存の `SkillTargetingService.inCone` によりNPC・別world・死亡Mob・PvP対象を除外する。EN 10、cooldown 60 tick、詠唱0 tickとする。
- `MELEE` / `NONE` の基礎倍率は65%。Lv.2～5は `SKILL_DAMAGE_INCREASE +5%` を各レベルで加え、Lv.5は78%。自身の現在Shield量による追加倍率は適用しない。
- シールドダメージは通常式 `(最低1の換算値 + SHIELD_BREAK) × 3.0` とし、この一撃だけ3倍にする。HPダメージ、命中、会心、防御、属性、無敵、脅威、リチャージは既存経路を変更しない。
- `DamageResult.shieldDamage` が返す、当該hitで実際に減少した対象Shieldの50%を `StatusService.recoverShield` で自身へ加算する。対象Shieldがない、回避・無効・0ダメージ、または回復阻害中なら吸収0とし、自身の `MAX_SHIELD` を超えない。
- 青白い斬撃と対象Shield周囲の破片を表示する。実際に自身のShieldが回復した場合だけ、対象から発動者へ流れる粒子と到達4 tick後の円形Shieldを表示する。すべて `SkillEffectService` / `ParticleDisplayService` と `SharedParticleDefinitions` を経由する。

### 10.2 入手・テスト契約

API が自動生成する `00_skill_gem_swordsman_shield_drain` を、`skill_gem_exchange` の1ページ目にある個別ジェム商品として無印原石1個で販売する。`skill_gem_raw_exchange` は上位原石1個から直下位原石3個への一方向交換だけを表示する。

恒久テストは、現在Shield量にかかわらず基礎65%を維持すること、実Shield減少量50%回復、シールドが削れない場合の吸収0、実回復量0での吸収演出不使用、3倍シールドブレイク指定、catalog ID一意性を検証する。

## 11. フレイムラッシュの実装契約

`swordsman_flame_rush` は、前方の敵を炎で横薙ぎし、短い間隔の後に同じ対象へ縦斬りを重ねるソードマンの範囲近接スキルである。マスタは `v1.swordsman_flame_rush.yml`、executor は `SwordsmanFlameRushExecutor`、登録先は `SwordsmanSkillExecutorCatalog` とする。使用許可はソードマンのskilltree node `1204` とAdministratorの検証用 `usableSkills` から与える。ソードマンの `usableSkills` には追加しない。

### 11.1 数値・対象・演出

- EN 14、cooldown 80 tick、詠唱0 tick。前方6m・全角60度の遮蔽なし範囲から、距離順のMobを最大5体選ぶ。既存の `SkillTargetingService.inCone` により、PvP対象、NPC、別world、死亡Mobは選ばない。
- 初撃は火属性 `MELEE` 65%の横薙ぎ、4 tick後の二撃目は火属性 `MELEE` 75%の縦斬りとする。`SKILL_DAMAGE_INCREASE` は両方へ共通 combat 経路で適用し、Lv.10の実効倍率はそれぞれ94.25%・108.75%、合計203%となる。
- 初撃ではノックバックを適用しない。二撃目も対象を再選択せず、初撃で選択した各Mobへ1回だけ処理する。
- 二撃目の炎上付与率は、Lv.1〜7が0%、Lv.8が35%、Lv.9が40%、Lv.10が45%である。炎上DoTは既存状態異常規則に従い、20 tickごとに付与元の解決攻撃力20%を与える。
- 横薙ぎは使用者に近い半径1.6m・2.7m・3.8mのオレンジ `DUST` と `FLAME` を重ね、右から左へ6 tickで流す。縦斬りは赤 `DUST` と `FLAME` を増量して表示し、命中点へ既存の炎粒子も重ねる。表示は `SkillEffectService` / `ParticleDisplayService` 経由に限定する。

### 11.2 DPS・入手・テスト契約

- 単体のCD基準DPSは、命中100%、会心・防御・属性補正・操作時間なし、詠唱0秒で確認する。Lv.10では `R × (0.9425 + 1.0875) ÷ 4.0 = R × 0.5075`。炎上は付与20 tick後から100 tickの期限前までに4回DoTが発生するため、Lv.10の45%付与では `R × 0.20 × 4 × 0.45 ÷ 4.0 = R × 0.09` を追加する。したがって理想条件の合計は `R × 0.5975` であり、炎上を外した通常単体ではアストラルエッジLv.5の `R × 0.72` を超えない。
- APIが自動生成する `00_skill_gem_swordsman_flame_rush` は存在するが、依頼範囲では交換ショップへ追加しない。
- 恒久テストは、catalog ID一意性、前方5体上限、初撃のノックバック不使用、右から左へ6 frameの横薙ぎ演出、4 tick後の二撃目、Lv.1〜7では炎上なし・Lv.8/9/10では二撃目へ35%/40%/45%の炎上判定が付くことを検証する。

## 12. チャレンジングロアの実装契約

`swordsman_challenging_roar` は、ソードマンの標準タンク行動として周囲のMobを一時挑発する非攻撃スキルである。マスタは `v1.swordsman_challenging_roar.yml`、executor は `SwordsmanChallengingRoarExecutor`、登録先は `SwordsmanSkillExecutorCatalog` とする。ソードマンとAdministratorの `usableSkills` から使用許可を与え、専用skilltree nodeは追加しない。自動生成ジェムは `skill_gem_exchange` で無印原石1個から交換する。

### 12.1 数値・対象・一時挑発

- EN 25、cooldown 400 tick、詠唱0 tick。持続時間はLv.1で80 tickとし、Lv.2～5の `durationTicks +20` によりLv.5で160 tickとする。
- 発動直後から20 tickごとに、発動者中心の水平半径8m・上下8m、ブロック遮蔽のないMobを距離順・UUID順で最大24体選ぶ。既存の円柱選択によりNPC・死亡済みMob・別world・PvP対象を除外し、ENEMYとBOSSを対象にする。
- 一時挑発はMobの脅威値を変更せず、挑発者UUIDと有効期限をMob単位で保持する。有効中は `MobAiService` の既存ターゲット維持と通常のtargeting strategyより挑発者を優先する。同じMobへ別プレイヤーの挑発が届いた場合は後発を優先する。
- 各pulseの挑発保持は21 tickとし、次の20 tick pulseまで途切れない。最終pulseの期限切れ、発動者の退出・死亡・world変更、Mob破棄、Plugin停止では固定対象を解除し、残存する通常脅威値からAIが再選定できる状態へ戻す。
- ダメージ、状態異常、脅威値加算、PvP効果は発生させない。

### 12.2 演出・テスト契約

発動時に発動者の現在位置から `ENTITY_RAVAGER_ROAR` をvolume 1.35、pitch 0.85で1回鳴らす。発動中は5 tickごとに発動者の高さ1mを中心として `WARPED_SPORE` を32個表示する。表示は `CHALLENGING_ROAR_WARPED_SPORE` と `SkillEffectService` を経由し、プレイヤーのparticle設定を反映する。

恒久テストは、Lv.1で5 tick周期16回のタスク、発動音、4 frameごとの半径8m・最大24体・遮蔽あり選択、対象ごとの21 tick挑発、非pulse frameの粒子のみ表示、後発挑発の上書き、期限切れとlifecycleでの固定対象解除、catalog ID一意性を検証する。

## 13. バスティオンストライクの実装契約

`swordsman_bastion_strike` は、現在MPを使い切る代わりに発動時の自己Shield全回復を得る、ソードマンの単体近接スキルである。マスタは `v1.swordsman_bastion_strike.yml`、executor は `SwordsmanBastionStrikeExecutor`、登録先は `SwordsmanSkillExecutorCatalog` とする。使用許可はソードマンの skilltree node `1203` と Administrator の `usableSkills` から与える。ソードマンの `usableSkills` には追加しない。

### 13.1 数値・対象・演出

- 発動者の前方6m・全角40度から、遮蔽のない最寄りMob 1体だけを選ぶ。既存の `SkillTargetingService.inCone` によりNPC・別world・死亡Mob・PvP対象を除外する。cooldownはLv.1の3000 tick（150秒）からLv.2〜5で各-200 tickとなり、Lv.5では2200 tick（110秒）とする。詠唱は0 tick、シジル枠は全レベル0とする。
- `MELEE` / `NONE` の基礎倍率は125%。Lv.2～5は `SKILL_DAMAGE_INCREASE +5%` を各レベルで加え、Lv.5は150%とする。
- `resourceType: MANA` と `resourceCost: 0` は共通固定コストを重ねないための定義である。executorは `params.consumeAllCurrentMana: true` と `params.levelFiveRequiredManaRatio: 0.80` を必須とし、Lv.1〜4では現在MPが `MAX_MANA` と一致するときだけ、Lv.5では現在MPが `MAX_MANA × 0.80` 以上のときだけ発動する。不足時はMP消費・Shield回復・攻撃・cooldown開始を行わない。発動成功時に時点の現在MPを全量消費する。
- MP全量消費の直後、対象の有無・命中・回避・無効化にかかわらず、発動時スナップショットの `MAX_SHIELD - currentShield` を `StatusService.recoverShield` へ渡す。既存の上限・回復阻害を適用するため、Shieldが最大、最大Shieldが0、または回復阻害中は実回復量0となる。
- 共通発動音は `block.beacon.power_select` とする。使用者の位置へ青いソウルファイアの縦柱とルーンを表示し、2 tickごとの4段拡張ルーン、最終段の前方着弾フラッシュと雷鳴を重ねる。シールドドレインの青白い直線斬撃・前方弧・吸収線とは共有しない。すべて `SkillEffectService` / `ParticleDisplayService` と `SharedParticleDefinitions` を経由する。

### 13.2 入手・テスト契約

API が自動生成する `00_skill_gem_swordsman_bastion_strike` を、`skill_gem_exchange` の1ページ目にある個別ジェム商品として無印原石1個で販売する。

恒久テストは、前方単体への125%近接hit、発動時の不足Shield量だけの回復要求、回避または0ダメージ時にも回復すること、Lv.1〜4の最大MP限定、Lv.5の80%MP発動、現在MP全量消費、4段演出予約、catalog ID一意性を検証する。
## 14. ラストシールドの実装契約

`swordsman_last_shield` は、シールド破壊が確定する直接攻撃を1回だけ無効化するソードマン用バインド必須パッシブである。マスタは `v1.swordsman_last_shield.yml`（表示アイコン `BEACON`、クールダウン120秒）、executor は `SwordsmanLastShieldSkillExecutor`、runtime は `LastShieldSkillRuntimeService` とする。使用許可はソードマンのskilltree node `1211` とAdministratorの `usableSkills` から与え、ソードマンの `usableSkills` には追加しない。

### 14.1 発動条件・クールダウン・演出

- `DamageService` は、`NORMAL_ATTACK` または `SKILL` による有効な直接ダメージで、現在Shieldを0以下へ減らす場合だけruntimeへ判定を委譲する。回避済み、0ダメージ、Shieldなし、状態異常DoT、環境ダメージは発動条件に含めない。
- runtimeが有効かつ120秒（2400 tick）のクールダウン終了後なら、当該攻撃は `DamageResult(0)` として終了する。Shield消費、HPダメージ、ノックバック、耐久消費、Shield再充填待機は発生させない。クールダウン中の破壊は通常のShield破壊経路へ委ねる。
- 無効化時は `PlayerMessageService` 経由で専用の発動通知を送信し、発動地点へ `ITEM_SHIELD_BLOCK`、`BLOCK_BEACON_ACTIVATE`、`ITEM_TRIDENT_THUNDER` の連続音と、トーテム・ソウルファイア・電気火花・閃光、上下3段のリング（下段シアンDust・中段End Rod・上段電気火花）を表示する。パーティクルは `ParticleDisplayService` / `SkillEffectService` 経由で表示し、通常のShield被弾演出と区別できる専用の `SharedParticleDefinition` を使用する。

### 14.2 入手・テスト契約

APIが自動生成する `00_skill_gem_swordsman_last_shield` を、`skill_gem_exchange` の1ページ目にある個別ジェム商品として無印原石1個で販売する。

恒久テストは、Shieldを破壊する直接攻撃だけを無効化し、Shield・HPを残すこと、クールダウン中は通常どおりShieldを破壊すること、状態異常DoTを無効化しないこと、executorが正のクールダウンを要求することを検証する。

## 15. シールドリチャージの実装契約

`administrator_shield_recharge` は、シールドを保持した状態の被弾後に段階再充填を行う防御パッシブである。マスタは `v1.administrator_shield_recharge.yml`、executor は `AdministratorShieldRechargeSkillExecutor`、登録先は通常の skill registry とする。使用許可はソードマンの skilltree node `1202` と Administrator の `usableSkills` から与え、ソードマンの `usableSkills` には追加しない。既存のID・executor・ジェム交換経路は維持する。

- 最大Shieldを30増加し、シールド残存時の被弾後8秒から最大Shieldの毎秒2%を再充填する。再充填中に被弾すると待機をやり直し、シールド破壊時は通常の30秒後一括回復へ委ねる。
- `passive.bindRequired: true` とし、通常のパッシブライフサイクルで `StatusService` の再充填設定を有効化・解除する。再充填中のパーティクルは `ParticleDisplayService` と共有定義を経由する。
- 恒久テストは、最大Shield補正、待機時間、段階回復率、再被弾による待機更新、シールド破壊時の通常回復経路、パッシブ解除時の設定破棄を検証する。

## 16. ハンターフェイドショットの実装契約

`hunter_fade_shot` は、短射程の散弾を放つと同時に間合いを切るハンターの機動射撃である。マスタは `v1.hunter_fade_shot.yml`、executor は `HunterFadeShotExecutor`、登録先は `HunterSkillExecutorCatalog` とする。ハンターの `usableSkills` から直接使用許可を配布し、Administrator の `usableSkills` に検証用許可を残す。専用skilltree nodeは追加しない。

### 16.1 散弾・移動・演出

- EN 14、cooldown 80 tick（4.0秒）、詠唱0 tickとする。発動時の視線方向を中央として、視線に直交する横軸上の全角30度へ5本の仮想飛翔体を等間隔で同時発射する。
- 各矢は射程9m、速度1.8m/tick、当たり半径0.30m、非貫通・最大1体とする。各矢は独立して衝突判定するため、同一発動の複数矢が同じMobへ命中できる。地形Blockで停止し、最大射程への無衝突到達では効果を発生させない。
- 各命中へ `RANGED` / `NONE` の基礎倍率32%を1回適用する。Lv.2〜5の `SKILL_DAMAGE_INCREASE +5%` により、Lv.5は1本38.4%、5本すべて命中した場合は合計192%となる。状態異常、貫通、対象間の連鎖は発生させない。
- 対象は既存の仮想飛翔体と同じ管理対象Mobに限定し、PvP対象、NPC、別world、死亡Mobは対象外とする。Bossは通常Mobと同じく対象に含める。
- 5本の発射要求後、`SkillMovementService.backstep` で視線と反対方向へ最大3.5m後退する。移動不能状態、全身幅を含む経路遮蔽、足場、頭上空間を既存サービスで確認し、安全な移動先がない場合は後退だけを省略して射撃・コスト・cooldownを成立させる。
- 矢の軌跡と命中には既存 `SKILL_HUNTER_ARROW` / `SKILL_HUNTER_IMPACT` を使い、後退に成功した場合だけ移動経路へ `HUNTER_FADE_SHOT_STEP` の薄い雲を表示する。発射音は `ENTITY_ARROW_SHOOT` を1回だけ鳴らす。

### 16.2 DPS・入手・テスト契約

- 単体DPSはSkill Lv.5、5本全弾命中、命中100%、会心・防御・属性補正・操作時間なしで `R × 1.92 ÷ 4.0 = R × 0.48` とする。クイックショットLv.5の `R × 0.90` より継続火力を抑え、複数対象への分散命中と安全な後退へ性能を配分する。
- APIが自動生成する `00_skill_gem_hunter_fade_shot` を、`skill_gem_exchange` の1ページ目で無印原石1個から1個へ交換できるようにする。
- 恒久テストは、catalog ID一意性、5本・全角30度の左右対称な散弾方向、各矢の非貫通・単体衝突、同一Mobへの複数命中、各命中32%、発射後の3.5mバックステップ、移動成功時の後退演出を検証する。

## 17. ハンター アローレインの実装契約

`hunter_arrow_rain` は、重力のある初弾で着弾地点を指定し、発動者の頭上から円内へ矢群を降らせるハンター初期範囲スキルである。マスタは `v1.hunter_arrow_rain.yml`、executorは `HunterArrowRainExecutor`、登録先は `HunterSkillExecutorCatalog` とする。ハンターとAdministratorの `usableSkills` に使用許可を与え、APIが自動生成する `00_skill_gem_hunter_arrow_rain` は `skill_gem_exchange` で無印原石1個から交換する。専用skilltree nodeは追加しない。

### 17.1 数値・対象・消費

- Lv.1は詠唱40 tick（2秒）、cooldown 240 tick（12秒）、主ENG 16、副MP 8とする。共通サービスが両残量を事前検証し、成功時だけ両方を消費する。
- 初弾は視線方向へ速度1.60m/tick、重力0.05m/tick²、最大射程18m、当たり半径0.45mの非貫通仮想飛翔体を1本放つ。Mob命中時は `RANGED` / `NONE` 70%を1回与える。Mobまたはblockに当たった場合だけ雨矢を開始し、射程終端では開始しない。
- 雨矢はLv.1で着弾地点中心の半径3mへ15本、Lv.2〜5はレベルごとに半径+0.5m・矢数+3本とし、Lv.5は半径5m・27本とする。1 tickに3本ずつ開始し、各矢は最初に触れたMobへ `RANGED` / `NONE` 30%を1回だけ与える。
- Lv.2〜5の `SKILL_DAMAGE_INCREASE +5%` は初弾と雨矢の双方へ適用し、Lv.5は84% / 36%とする。PvP、NPC、別world、死亡Mobは既存の仮想飛翔体対象規則により除外する。

### 17.2 弾道

雨矢の始点は雨開始時点のプレイヤー目線位置から5m上空を基準に、水平方向1.25m・高さ0.45mの乱数を加える。終点は半径内へ面積一様となる `radius × sqrt(random)` で選び、着弾地点付近から上下にsolid floorと通過可能な足元を探索する。飛翔時間は10〜14 tickから選び、毎tick移動後に0.14m/tick²の重力を加えて指定終点へ届く離散初速を計算する。各矢の最大移動距離はこの離散弾道の全step長に0.25mの許容値を加えて決め、深い地表でも固定上限で途中終了させない。全雨矢を1つの反復taskで管理し、同tickの軌跡粒子はviewer探索をまとめて表示する。

### 17.3 バランス・テスト契約

終点面積だけでなく、プレイヤー頭上から着弾円へ収束する途中の全線分を実衝突と同じ拡張AABBで評価する。通常幅0.6mの中心静止Mob、遮蔽なし、距離3〜18mの決定的シミュレーションでは、単体への雨矢期待命中はLv.1で約4.0〜5.1本、Lv.5で約2.5〜4.6本となる。このため雨矢基礎倍率を30%へ下げ、Lv.5のCD＋詠唱周期DPSは `R × (0.84 + 0.36 × 2.5〜4.6) ÷ 14 = R × 0.124〜0.178` とする。全27本が別対象へ有効命中する理想分散上限は `R × 0.754` であり、対象密度と地形の影響を受ける範囲攻撃として扱う。

恒久テストは、catalogの13 ID一意性、必須params、初弾の重力・射程・70%間接hit、射程終端では雨を開始しないこと、entity/block着弾後の15本・3本/tick・30%間接hit、正確なENTITY交点、Block面と手前側効果中心の分離、複数tickの重力・終了優先、深い地表へ届く弾道距離、実弾道の距離別期待命中、MP/ENGの原子的な事前検証と成功時消費を検証する。プレイテストでは通常幅静止Mob、大型Boss、半径内6体集団を各30回以上測定し、単体命中本数・総命中本数・block衝突を記録する。

## 18. ハンタークラッシュアローの実装契約

`hunter_crash_arrow` は、HPへのダメージを抑えてシールドブレイクを強化するハンターの単体遠距離スキルである。マスタは `v1.hunter_crash_arrow.yml`、executor は `HunterCrashArrowExecutor`、登録先は `HunterSkillExecutorCatalog` と `ActiveSkillExecutorCatalog` とする。ハンターの class `usableSkills` には追加せず、skilltree node `1212` の使用許可と、Administratorの検証用 `usableSkills` を使用する。自動生成ジェムは `skill_gem_exchange` の1ページ目で無印原石1個から交換する。

### 18.1 数値・対象・ダメージ

- 発動時の目線方向へ、射程14m・毎tick1.35m・命中半径0.45mの非貫通仮想飛翔体を1本だけ発射する。最初のMobへ1回だけ命中し、Block衝突または最大射程ではダメージを発生させない。`SkillProjectileService` の通常線形経路を使うため、重力による落下・ホーミング・同一対象への再命中はない。
- `RANGED` / `NONE` の基礎倍率は30%（`damageRatio: 0.30`）とする。Lv.2〜5は `SKILL_DAMAGE_INCREASE +5%` を各レベルで加え、Lv.5のHP基礎倍率は36%となる。低いHP倍率と引き換えに、シールドを持つ対象への一撃性能を主目的とする。
- `SkillCombatService.hit(attacker, target, RANGED, NONE, damageRatio, shieldBreakMultiplier)` を使い、シールドブレイク倍率だけをこの一撃へ渡す。現行 `DamageService` のシールド式では、基礎シールド換算値と攻撃者の `SHIELD_BREAK` を合算した値へこの倍率を乗算し、対象の現在Shieldを上限に実減少量を決める。Lv.1は3.0倍、Lv.2〜5は各+0.5で、Lv.5は5.0倍とする。
- EN14、cooldown120tick（6秒）、詠唱20tick（1秒）とする。詠唱込みの単体HP DPSは、命中100%、会心・防御・属性補正・操作時間なし、同じ解決攻撃力 `R` を使う条件で `R × 0.36 ÷ (6.0 + 1.0) = R × 0.0514`。この値にシールド減少量は含めない。
- 既存の `SkillTargetingService` / `SkillProjectileService` のMob専用規則を使用し、NPC、PvP対象、別world、死亡済みMobは対象外とする。状態異常、ノックバック、範囲追加攻撃は発生させない。

### 18.2 演出

飛翔体の毎tick軌跡に `SKILL_HUNTER_CRASH_ARROW_TRAIL`、Mob命中時に `SKILL_HUNTER_CRASH_ARROW_IMPACT` を使い、水色の `DUST` を表示する。発射時は `ENTITY_ARROW_SHOOT` を低めのpitchで1回鳴らす。粒子表示は `SkillProjectileService`、`SkillEffectService`、`SharedParticleDefinitions` を経由し、個別executorから `World#spawnParticle` を直接呼び出さない。

### 18.3 テスト契約

`HunterCrashArrowExecutorTest` は、射程・速度・半径・非貫通・1体命中、重力なしの線形飛翔体、水色の軌跡・着弾定義、`RANGED` / `NONE` の30%と3.0倍シールドブレイク倍率を検証する。`ActiveSkillExecutorDesignTest` はcatalogの13 ID一意性と、クラッシュアローの必須params検証を行う。Filebase検証では `hunter_crash_arrow` と `node 1212` の参照、`skill_gem_exchange` の仮想ジェム参照、シールド倍率のレベル差分を確認する。
