# 13_3.03-コマンド

## 1. skill コマンド

クラス名: `SkillCommand`
物理名: `executePlayerCommand`

`/skill [gui]` を処理する。引数なしまたは `gui` で `SkillBindGuiEventHandler.open` を呼び、その他は usage を表示する。コマンド登録は `CommandRegister` が行う。
