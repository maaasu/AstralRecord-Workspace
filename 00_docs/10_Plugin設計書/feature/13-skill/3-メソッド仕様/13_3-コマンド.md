# 13_3-コマンド

## 1. skill コマンド

クラス名: `SkillCommand`
物理名: `executePlayerCommand`

`/skill [gui]` を処理する。引数なしまたは `gui` で `SkillBindGuiEventHandler.open` を呼び、画面を開けた場合だけ `GuiSound.OPEN` を一度再生する。その他は usage を表示する。コマンド登録は `CommandRegister` が行う。
