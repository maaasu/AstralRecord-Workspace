# 13_9.00-未決事項

## 1. player の required level 正本

`PlayerSkillCaster.level()` は現在 `Integer.MAX_VALUE` を返し、required level 検証を通過させる。account level、class level、skill 熟練度のどれを採用するかを決めた後に caster 実装と GUI 表示を同期する。

## 2. bind-required passive の編集 UI

API model は passive 8 slot を持ち、`PassiveSkillService` も bind-required 判定を実装済みだが、現行 GUI は active 8 slot しか描画しない。passive slot の表示位置、active / passive page 分離、既存 preset migration を決める必要がある。

## 3. params schema の運用

`params` は executor 固有 map で、共通 reader と executor validation はあるが、全 implementation ID の schema を機械検証する共通仕様はない。filebase authoring / CI での schema 管理方式を決める。

## 4. definition reload 中の継続状態

reload 時に既存 cooldown、詠唱 session、passive effect を新 definition へどう引き継ぐかの運用 policy は未決。現状は頻繁な reload を想定しない。

## 5. executor catalog の登録方式

現行は built-in catalog の明示登録とし、重複を禁止する。class path 自動探索や plugin 拡張 API を導入する場合の順序・衝突規則は未決。
