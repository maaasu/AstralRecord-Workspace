# 13_8-実装予定

## 1. スキル定義の runtime 再同期

状態: 採用済み・未実装

サーバーを停止せず、`account.mode = ADMIN` の管理者が API からスキル定義を再取得できる導線を追加する。

受入条件:

- 管理者以外は再同期を開始できない。
- `params` 不備や未登録 `implementationId` はスキル単位で隔離し、一件の不備で全定義の利用を停止しない。
- cooldown、詠唱 session、passive effect の引継ぎと管理者向け操作面は、[[13_9-未決事項]] の判断後に確定する。
