# 14_9.00-未決事項
## 1. 固定値ダメージに対する追加 modifier
`applyEffectDamage(...)` / `applyDamage(..., FIXED)` は、攻撃者が managed entity の場合に critical / super critical を適用する。
final damage / buff 由来の特殊係数を DoT にも乗せるかどうかは、buff / status 側の設計と合わせて別途決める。

## 2. custom 通常攻撃の当たり判定詳細
`WeaponAttackSkillExecutor` は skill params の `hitRange` / `hitRadius` / `hitStepDistance` で前方走査する。
壁貫通、部位判定、味方判定、無敵時間、複数ヒット制御は未実装。

## 3. Mob AI からの本格接続
対応済み。`MobAiService` は `ai.combat.skills` が設定されている Mob について `SkillService.castSkill(..., SkillCastTrigger.MOB_AI, ...)` を呼び出し、`normal_attack` executor は Mob 起点時にプレイヤーへ `DamageService.attack(...)` でダメージを適用する。
