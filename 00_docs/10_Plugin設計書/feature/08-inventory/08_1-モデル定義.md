# 08_1-モデル定義

## 1. インベントリモデル

対象実装: `InventoryModel`

| 項目 | 説明 |
|---|---|
| `inventoryId` | インベントリ UUID |
| `accountId` | 所有アカウント UUID |
| `inventoryType` | [[08_1-モデル定義]].インベントリ種別 |
| `inventoryProfile` | [[08_1-モデル定義]].インベントリプロファイル のコード |
| `slotCapacity` | 非BAGの固定スロット数。無制限・未設定・BAGでは `null`。BAG容量の正本にはしない |
| `isEnabled` | 利用可否 |
| `metadataJson` | スナップショット系付加情報 JSON |
| `createdAt`, `updatedAt` | 作成・更新日時 |
| `createdBy`, `updatedBy` | 作成・更新者 UUID |
| `isDeleted` | 論理削除フラグ |

## 2. インベントリエントリモデル

対象実装: `InventoryEntryModel`

| 項目 | 説明 |
|---|---|
| `inventoryEntryId` | エントリ UUID |
| `inventoryId` | 所属インベントリ UUID |
| `slotIndex` | スロット番号。未指定時は `null` |
| `itemCategory` | アイテムカテゴリ |
| `itemId` | マスタアイテム ID |
| `instanceType` | [[08_1-モデル定義]].インベントリインスタンスタイプ のコード |
| `instanceId` | 装備・ルーンなどインスタンス UUID |
| `quantity` | 数量 |
| `metadataJson` | 付加情報 JSON |
| `createdAt`, `updatedAt` | 作成・更新日時 |
| `createdBy`, `updatedBy` | 作成・更新者 UUID |
| `isDeleted` | 論理削除フラグ |

## 3. インベントリ種別

対象実装: `InventoryType`

| 値 | コード | 用途 | スロット管理 | インスタンス対応 | コマンド表示 |
|---|---|---|---|---|---|
| `BAG` | `BAG` | 未装着の通常アイテム・装備・ルーンを統合した所持品 | あり | あり | 可 |
| `CURRENCY` | `CURRENCY` | 通貨 | あり | なし | 不可 |
| `STORAGE` | `STORAGE` | ストレージ | なし | なし | 不可 |
| `EQUIP_SLOT` | `EQUIP_SLOT` | 装備スロット保存 | あり | あり | 不可 |
| `HOTBAR` | `HOTBAR` | ホットバー保存 | あり | なし | 不可 |
| `ACCESSORY_SLOT` | `ACCESSORY_SLOT` | アクセサリスロット保存 | あり | あり | 不可 |

`/inventory` の表示対象は `BAG` のみ。アイテムカテゴリによる表示切替は行わない。

BAG の実効容量は `InventoryModel.slotCapacity` ではなく、`INVENTORY_SLOTS` ステータスの確定値だけを正本とする。基礎値は36、プレイヤーレベル5ごとに1増加し、装備・スキルツリー・バフ等の通常のステータス補正を合算したうえで小数点以下を切り捨て、最小0とする。ゲーム仕様上の最大値は設けない。BAG 作成時の `slotCapacity` は `null` とする。

1行を8スロットとして扱い、Bukkit PlayerInventory の3行には24スロットを表示する。右端列の slot 17 / 26 / 35 はそれぞれ上スクロール、インベントリ情報、下スクロールの制御領域とし、BAG entry は配置しない。表示可能な行数は、実効容量と保持中 entry の最大 `slotIndex` の大きい方から求める。

容量減少で `slotIndex` が実効容量を超えた既存 entry は削除・詰め直しせず、容量外として同じスロット番号のまま表示する。容量外 entry は通常スロット・ホットバー・装備・ストレージへの移動または破棄だけを許可し、容量外の空き枠は新規取得やスタック先に使わない。一度空いた容量外スロットは、実効容量が回復するまで再利用できない。

`STORAGE` は `/storage` と別途用意されるストレージオープンイベントから操作する。メインメニューにはストレージアイコンを配置しない。`slotIndex` は GUI の物理スロットではなく収納順を表す連番として使い、`metadataJson.acquiredAt` に収納元 entry の作成日時を保持して獲得順ソートに利用する。`instanceType` / `instanceId` を持たない通常アイテムは同一 itemId / itemCategory の既存 entry へ数量統合し、装備・ルーンなど instanceId と紐づくアイテムは統合せず別 entry として保持する。

## 4. 装備種別

対象実装: `EquipmentType`

| 値 | 用途 |
|---|---|
| `MAIN_HAND` | メインハンド |
| `HEAD` | 頭装備 |
| `CHEST` | 胴装備 |
| `LEGS` | 脚装備 |
| `FEET` | 足装備 |
| `OFF_HAND` | オフハンド |
| `UNSUPPORTED` | 装備種別解決不可。フォールバック用 |

## 5. アクセサリスロット種別

対象実装: `AccessorySlotType`

`ACCESSORY_SLOT` inventory の `slotIndex 1..10` を表す列挙。`slotIndex 1` はオフハンド、`slotIndex 2..10` は種類別アクセサリとして扱う。アクセサリ装備は item master の `equipment.slot = ACCESSORY` に加え、`equipment.tag` がスロット種別と一致する場合のみ配置できる。

| 値 | slotIndex | 用途 |
|---|---:|---|
| `OFF_HAND` | 1 | オフハンド |
| `AMULET` | 2 | アミュレット x1。`equipment.tag = AMULET` |
| `TALISMAN_1` | 3 | タリスマン x2 の1枠目。`equipment.tag = TALISMAN` |
| `TALISMAN_2` | 4 | タリスマン x2 の2枠目。`equipment.tag = TALISMAN` |
| `CHARM_1` | 5 | チャーム x3 の1枠目。`equipment.tag = CHARM` |
| `CHARM_2` | 6 | チャーム x3 の2枠目。`equipment.tag = CHARM` |
| `CHARM_3` | 7 | チャーム x3 の3枠目。`equipment.tag = CHARM` |
| `CORE` | 8 | コア x1。`equipment.tag = CORE` |
| `RELIC_1` | 9 | レリック x2 の1枠目。`equipment.tag = RELIC` |
| `RELIC_2` | 10 | レリック x2 の2枠目。`equipment.tag = RELIC` |

## 6. インベントリインスタンスタイプ

対象実装: `InventoryInstanceType`

| 値 | コード | 意味 |
|---|---|---|
| `EQUIPMENT` | `EQUIPMENT` | 装備インスタンス。`instanceId` に equipment instance UUID を持つ |
| `RUNE` | `RUNE` | ルーンインスタンス。`instanceId` に rune instance UUID を持つ |

通常スタック可能アイテムでは `instanceType` / `instanceId` は `null`。

## 7. インベントリプロファイル

対象実装: `InventoryProfile`

| 値 | コード | 用途 |
|---|---|---|
| `GAME` | `GAME` | 通常プレイ用 inventory / loadout |
| `ADMIN` | `ADMIN` | 管理者モード用 inventory / loadout |

## 8. クリックアクション

対象実装: `InventoryClickGuard.ClickAction`

| 値 | 用途 |
|---|---|
| `HOTBAR_SLOT` | ホットバースロット / オフハンド移動 |
| `HOTBAR_SHORTCUT` | 旧GUI固有ホットバー操作との競合防止 |
| `ARMOR_SLOT` | Bukkit 防具スロット操作 |
| `DISPLAYED_ITEM` | 表示中 inventory からの装備移動 |
| `EQUIPMENT_GUI_SLOT` | 装備 GUI スロット操作 |
| `INVENTORY_SWITCH` | BAG の1行スクロール |

既定クールタイムは 100ms。

## 9. インベントリエントリ下書き

対象実装: `InventoryEntryDraft`

`InventoryRepository.replaceEntries` に送る request body 用モデル。

| 項目 | 型 | 必須 | 説明 |
|---|---|---|---|
| `slotIndex` | `Integer` | 任意 | スロット番号 |
| `itemCategory` | `String` | 必須 | アイテムカテゴリ |
| `itemId` | `String` | 任意 | マスタアイテム ID |
| `instanceType` | `String` | 任意 | インスタンスタイプ |
| `instanceId` | `UUID` | 任意 | インスタンス UUID |
| `quantity` | `Long` | 必須 | 数量 |
| `metadataJson` | `String` | 任意 | 付加情報 JSON |
| `inventoryEntryId` | `UUID` | 任意 | 既存またはclient採番entry UUID |
| `expectedUpdatedAt` | `LocalDateTime` | 任意 | 既存entryでは API から取得または保存成功応答で受け取った `updatedAt`。楽観ロック版として送信し、ローカルの数量・slot変更では保存成功まで更新しない。保存待ち中に同一entryが再変更された場合も、削除・別inventoryへの移動でなければ再変更内容を維持したまま応答の `updatedAt` だけを反映する |

## 10. 装備ロードアウトモデル

対象実装: `EquipmentLoadoutModel`

| 項目 | 説明 |
|---|---|
| `equipmentLoadoutId` | ロードアウト UUID |
| `accountId` | 所有アカウント UUID |
| `loadoutProfile` | プロファイル |
| `loadoutName` | 表示名 |
| `sortOrder` | 並び順 |
| `isActive` | 現在有効か |
| `metadataJson` | 付加情報 JSON |
| `slots` | [[08_1-モデル定義]].装備ロードアウトスロットモデル 一覧 |
| `createdAt`, `updatedAt` | 作成・更新日時 |
| `createdBy`, `updatedBy` | 作成・更新者 UUID |
| `isDeleted` | 論理削除フラグ |

## 11. 装備ロードアウトスロットモデル

対象実装: `EquipmentLoadoutSlotModel`

| 項目 | 説明 |
|---|---|
| `equipmentLoadoutSlotId` | ロードアウトスロット UUID |
| `equipmentLoadoutId` | 所属ロードアウト UUID |
| `slotType` | `HEAD` / `CHEST` / `LEGS` / `FEET` / `ACCESSORY` |
| `slotIndex` | 防具は `0`。`ACCESSORY` は `0..9` とし、`0` がオフハンド、`1` がアミュレット、`2..3` がタリスマン、`4..6` がチャーム、`7` がコア、`8..9` がレリック。`ACCESSORY_SLOT.slotIndex` へ保存するときは `+1` する |
| `equipmentInstanceId` | 装備インスタンス UUID |

## 12. プレイヤー状態モデル

対象実装: `PlayerInventoryState`

1 アカウント分の inventory / entry / loadout / GUI 表示状態を保持する in-memory 集約モデル。join 時に API から構築し、プレイ中は本モデルのみを更新する。

| 項目 | 説明 |
|---|---|
| `accountId` | 対象アカウント UUID |
| `inventories` | 所有 `InventoryModel` 一覧 |
| `entriesByInventoryId` | inventoryId ごとの `InventoryEntryModel` 一覧 |
| `loadouts` | 所有 `EquipmentLoadoutModel` 一覧 |
| `displayedType` | GUI 表示中の `InventoryType`。統合後は `BAG` 固定 |
| `bagScrollRow` | BAG 表示の先頭行（0始まり、非永続） |
| `bagSlotCapacity` | `INVENTORY_SLOTS` から反映した BAG 実効容量（非永続、初期値36、最小0） |
| `selectedHotbarSlot` | 選択中ホットバー。未選択時は `null` |
| `hotbarShortcutMode` | カスタムGUI表示フラグ。true の間は GUI 側のプレイヤーインベントリ操作を許可する |
| `dirty` | 次回 save 対象フラグ |

主な操作:

- `putInventory`, `replaceEntries`, `putLoadout`, `upsertActiveLoadoutSlot`
- `replaceEntriesFromLoad`
- `updateInventoryMetadata`
- `takeAndClearDirty`, `restoreDirty`

## 13. state レジストリ

対象実装: `PlayerInventoryStateRegistry`

join 中プレイヤーの `PlayerInventoryState` を accountId 単位で保持するレジストリ。

| 操作 | 説明 |
|---|---|
| `put(state)` | join 時に state を登録する |
| `get(accountId)` | inventory feature / save task が state を参照する |
| `remove(accountId)` | quit / plugin disable 時に state を破棄する |
| `all()` | オートセーブが全 state を走査する |

## 14. 永続化トリガ

対象実装: `InventoryPersistence.SaveTrigger`

| 値 | 用途 |
|---|---|
| `AUTO` | 60 秒オートセーブ |
| `LOGOUT` | player quit 時 |
| `PLUGIN_DISABLE` | plugin disable 時 |
| `IMMEDIATE` | 即時整合性が必要な `saveNow` |

## 15. 関連 feature とのモデル関係

- `item`: `itemId`、`itemCategory`、装備・ルーンインスタンス ID を参照し、ItemStack 生成に利用する
- `account`: accountId とアカウントモードを表示反映判定と同期する
- `menu`: inventory 種別選択、装備 GUI、ショートカット表示の状態を参照する
