# 32_3-処理契約

## 1. BSP 配置生成

1. `roomCount.min..max` から seed で目標部屋数を一つ選ぶ。
2. `area.width x area.depth` の根区画を、各子区画が最小部屋と余白を収容できる位置で再帰分割する。縦横比が大きい場合は長辺を優先し、それ以外は seed で分割方向を選ぶ。
3. 葉区画ごとに余白を確保した非重複部屋を一室生成し、`roomShapes` の相対 weight で形状を選ぶ。
4. 分割木の左右部分木ごとに中心間距離が最小の部屋を接続する。経路探索では接続元・接続先以外の部屋と、既に確定した通路の部屋外区間を障害物として避け、物理的な交差によるゲート迂回を作らない。全室を結ぶ接続数 `部屋数 - 1` の木にならない生成結果は採用しない。
5. 開始部屋から幅優先探索し、開始部屋以外で距離が最大の葉を唯一の BOSS 部屋にする。同じマスタと seed は部屋、接続、役割を含め同じ DungeonLayout を返す。

## 2. ブロック生成

- RECTANGLE は矩形外周、CYLINDER は楕円近似の境界を壁とし、床から天井までを生成する。
- 床、壁、天井、通路はそれぞれの Material リストを相対 weight でブロック単位に抽選する。
- 経路探索済みの直交中心線を指定幅へ拡張し、床・側壁・天井を持つ折れ線通路として生成する。部屋内部は再充填せず、外周との交点だけを開口する。
- 各通路の親部屋側出口には `gateMaterial` を通路幅・高さ分配置する。
- `pillar.enabled` かつ部屋ごとの seed 抽選が `chance` 未満なら、部屋中央へ床上から天井下まで柱を置く。床側と天井側の東西南北には向きを設定した階段を置く。

## 3. 遭遇 Mob と部屋進行

- START 部屋の通常 Mob 抽選前に `level <= firstCombatRoomMaxMobLevel` の候補だけへ絞り込む。このため通常 Mob pool 内の最強 Mob が上限を超える場合、開始部屋には出現しない。
- NORMAL 部屋は通常 Mob pool 全体から `mobsPerRoom.min..max` 体を相対 weight で抽選する。
- BOSS 部屋は `bossMobId` の受付時 MobTemplate を一体生成する。
- DungeonService は生成した Mob instance UUID を session/room に関連付ける。DamageService が通常報酬処理を終えた後に死亡 UUID を通知し、部屋の生存 UUID が空になった時だけクリアする。
- 通常部屋クリア時はその部屋から出る全ゲートを除去し、子部屋を AVAILABLE にする。ボス部屋クリア時は踏破としてセッションを終了する。

## 4. 開始・生成・転送

- `/dungeon start <id>` はパーティー所属時にリーダーだけ受理する。参加者は受付時にオンラインのパーティーメンバー、未所属時は実行者一名とし、途中参加させない。
- 参加者数、通常プレイモード、他ダンジョン利用中を同期検証してから全員を予約状態にする。
- BSP とブロック計画は Bukkit API を呼ばない非同期処理とする。void World の作成、実行時登録、Block 変更はメインスレッドで行い、一 tick あたりの上限件数に分割する。
- 必要チャンクを先読みして Plugin chunk ticket を保持し、全ブロック生成完了後にだけ参加者を START 部屋へ転送する。
- reload は検証済み定義を参照交換する。準備中・進行中セッションは受付時スナップショットを使用する。

## 5. 終了と回収

- `/dungeon leave` は保留中の入場転送があれば完了を待ってから、受付前 Location または非 DUNGEON ワールドへ戻す。インスタンス外への転送成功を確認してから参加者をセッションから外し、失敗時は追跡を維持して再試行可能にする。ログアウト時は即時に参加者を外し、参加者がゼロになれば終了する。
- 踏破、参加者不在、準備・転送・ボス生成失敗では残存 Mob を破棄し、オンライン参加者を受付前 Location または非 DUNGEON ワールドへ退避してから一時 World をアンロードする。
- アンロード成功後だけ実行時 World 登録を解除し、インスタンスフォルダを非同期削除する。
- Plugin 停止時はスケジューラに依存せず同期回収する。次回起動時は `instanceRootPath` 直下で本機能の `dungeon-` 接頭辞を持つ残存フォルダだけを削除する。

## 6. ワールド保護

DUNGEON ワールドではプレイヤーのブロック破壊・設置を常にキャンセルする。World マスタは自然 Mob スポーンを禁止し、AstralRecord の MobService による CUSTOM spawn だけを許可する。
