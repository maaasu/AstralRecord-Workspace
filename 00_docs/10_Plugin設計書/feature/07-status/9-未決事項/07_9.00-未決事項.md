# 07_9.00-未決事項

## 1. ステータス計算式の拡張

現在の設計では、基礎値・アカウントモード補正・レベル補正・装備補正・バフ補正を扱う。権限値はステータス補正に使用しない。
以下は未確定事項として扱う。

- 職業補正
- 近接 / 遠隔 / 魔法攻撃力の派生計算式
- 最終ダメージ系ステータスの戦闘計算への適用順

## 2. 永続化方針

現在 HP / MP / EN はオンライン中のメモリ状態として扱う。  
ログアウト時に保存対象とするかは未確定。

## 3. 表示名の管理

ステータス種別の表示名は実装 enum に定義されている。  
多言語化や外部設定化を行うかは未確定。

## 4. レベルアップ演出

- レベルアップ時に即時で HUD 演出、サウンド、メッセージを出すかは未確定。
- 再計算契機は account feature から発火するが、演出責務を `account` / `status` / `hud` のどこへ置くかは今後確定する。
