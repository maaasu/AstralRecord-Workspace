# 06_9-未決事項

## 1. 一覧 API のカテゴリ別件数ログ

- `LootRepository.findAll` 自体は件数ログを出さず、`LootService.replaceSnapshot` が `LogId.I_5300` で合計件数を記録する。
- table / pool 数などの内訳を運用ログへ追加するかは未決定。

## 2. ルートテーブルのスキーマバージョニング

- `LootModel.schemaVersion` は読み取り・保持するが、version 差による分岐や拒否は実装していない。
- 後方互換性を破る schema 変更時の対応 version、変換、起動拒否方針は未定義である。

## 3. cache miss 時の同期 fallback

- `getLoadedOrFetch` は cache miss 時に同期 API を実行する。
- loot service 単体として非同期 API を追加するか、利用側で必ず非同期 prepare するかの責務分担は未決定である。
- 現在到達する item 側の具体的経路は [[04_9-未決事項]] で管理する。
