# 24_4-統合フロー

## 1. Web ログインコード発行

### シーケンス

```mermaid
sequenceDiagram
    actor Player as プレイヤー
    participant Command as WebAuthCommand
    participant Service as WebAuthService
    participant Repository as WebAuthRepository
    participant API as AstralRecord API
    Player->>Command: /web login
    Command-->>Player: P_6402（発行開始）
    Command->>Service: issueLoginChallenge(userUuid, mcid) [非同期]
    Service->>Repository: createChallenge(userUuid, mcid, serverId)
    Repository->>API: POST /api/web-auth/challenges
    API-->>Repository: challengeId / loginCode / expiresAt / loginUrl
    Repository-->>Service: WebLoginChallengeIssueResult
    Service-->>Command: WebLoginChallengeIssueResult
    Command-->>Player: P_6400（コード・期限・URL）[メインスレッド]
```

### 処理要点

1. コマンドは user UUID と MCID を確定してから非同期タスクへ渡す。
2. サービスは設定済みの API server ID を補い、リポジトリへ委譲する。
3. リポジトリだけが HTTP 要求と JSON 変換を担当する。
4. プレイヤー通知はメインスレッドへ戻し、オンライン状態を再確認してから行う。
5. コードの保存・消費と Web の Cookie セッションは API / Web 側の責務であり、本フローの対象外とする。

## 2. コンソール指定発行

```mermaid
sequenceDiagram
    participant Console as サーバーコンソール
    participant Command as WebAuthCommand
    participant API as AstralRecord API
    Console->>Command: /web login <MCID>
    Command->>API: GET /api/web-auth/users/by-mcid/{mcid}
    API-->>Command: UUID / MCID（1件だけ）
    Command->>API: POST /api/web-auth/challenges
    API-->>Console: コード・期限・ログインURL
```

未登録またはMCID重複は発行しない。ゲーム内プレイヤーがMCIDを指定した場合も発行しない。

### 例外・終了条件

- 引数が `/web login` と一致しない場合は使用方法を表示し、API を呼ばない。
- API 通信、status 検証、JSON 変換のいずれかが失敗した場合は `P_6401` を通知する。
- 非同期処理中にプレイヤーが退出した場合は結果を送信しない。
