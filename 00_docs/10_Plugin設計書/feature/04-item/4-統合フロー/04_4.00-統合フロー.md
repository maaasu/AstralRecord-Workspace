# 04_4.00-統合フロー

## 1. 起動時の一括ロード（service -> repository）

```mermaid
%%{init: {
  "theme": "base",
  "themeVariables": {
    "lineColor": "#1f2937",
    "signalColor": "#1f2937",
    "signalTextColor": "#111827",
    "actorBkg": "#f8fafc",
    "actorBorder": "#4b5563",
    "actorTextColor": "#334155",
    "labelBoxBkgColor": "#ffffff",
    "labelBoxBorderColor": "#374151",
    "labelTextColor": "#111827"
  }
}}%%
sequenceDiagram
    participant P as AstralRecord
    participant LS as LootService
    participant IS as ItemService
    participant IR as ItemRepository

    P->>LS: 全ルートテーブル一括ロード
    LS-->>P: ロード完了
    P->>IS: 全アイテム一括ロード
    IS->>IR: アイテム一覧取得
    IR-->>IS: サマリ一覧
    loop サマリごと
        IS->>IR: アイテム詳細取得
        IR-->>IS: アイテムモデル
        IS->>IS: ロード済みキャッシュ登録
    end
```

物理名対応:

- 全ルートテーブル一括ロード: [[06_3.02-サービス]].全ルートテーブル一括ロード（`LootService#loadAll`）
- 全アイテム一括ロード: `loadAll`（`ItemService`）
- アイテム一覧取得: `findAll`（`ItemRepository`）
- アイテム詳細取得: `findById`（`ItemRepository`）

## 2. `/item get` 付与処理（command -> service -> inventory -> menu）

1. 実行者が `/item get <itemId> [amount]` を実行する
2. コマンドが [[04_3.02-サービス]].個別アイテムロード（ID のみ）を呼ぶ
3. キャッシュ未保持の場合、サービスが詳細 API から取得して ID 単位キャッシュへ登録する
4. `InventoryService.addItemToNormalInventory` で実プレイヤーのインベントリへ付与を委譲する
5. 付与カテゴリを `resolveInventoryType` で解決する
6. `CURRENCY` ならカレンシーメニュー、それ以外なら通常インベントリ GUI を再描画する
7. 実行者へ付与結果を通知する

物理名対応:

- アイテム付与実行: `handleGet`（`ItemCommand`）
- 個別アイテムロード: `loadItem(itemId)`（`ItemService`）

## 3. パケット送信時の icon 書き換え（adapter）

```mermaid
%%{init: {
  "theme": "base",
  "themeVariables": {
    "lineColor": "#1f2937",
    "signalColor": "#1f2937",
    "signalTextColor": "#111827",
    "actorBkg": "#f8fafc",
    "actorBorder": "#4b5563",
    "actorTextColor": "#334155",
    "labelBoxBkgColor": "#ffffff",
    "labelBoxBorderColor": "#374151",
    "labelTextColor": "#111827"
  }
}}%%
sequenceDiagram
    participant SVR as Server
    participant ADP as ItemStackPacketAdapter
    participant CLT as Client

    SVR->>ADP: SET_SLOT / WINDOW_ITEMS 送信
    ADP->>ADP: PDC から icon / customModelData を読み取り
    alt AstralRecord アイテム
        ADP->>ADP: icon Material 解決 / customModelData 書き換え
        ADP-->>SVR: 書き換え後 ItemStack でパケット差し戻し
    else AstralRecord 以外
        ADP-->>SVR: 元のパケットを通過
    end
    SVR->>CLT: パケット送信
```

物理名対応:

- パケットアダプタ登録: `register`（`ItemStackPacketAdapter`）
- SET_SLOT書き換え: `handleSetSlot`
- WINDOW_ITEMS書き換え: `handleWindowItems`
- アイコン書き換え判定: `replaceIcon`

## 4. 装備インスタンス生成（service -> repository -> API）

1. 呼び出し側が [[04_3.02-サービス]].装備インスタンス生成 を呼ぶ
2. リポジトリが `POST /api/equipment/instances` を発行する
3. API がステータスロールを解決し、確定済みインスタンスを返す
4. サービスが結果を呼び出し側へ返却する

物理名対応:

- 装備インスタンス生成: `createEquipmentInstance`（`ItemService` / `ItemRepository`）

## 5. ルーンインスタンス生成（service -> repository -> API）

1. 呼び出し側が [[04_3.02-サービス]].ルーンインスタンス生成 を呼ぶ
2. リポジトリが `POST /api/rune/instances` を発行する
3. API がステータスロールを確定し、インスタンスを返す
4. サービスが結果を呼び出し側へ返却する

物理名対応:

- ルーンインスタンス生成: `createRuneInstance`（`ItemService` / `ItemRepository`）

## 6. `/item` 無引数の管理者GUI表示フロー

1. 実行者が `/item` を引数なしで実行する
2. `ItemCommand` が `ItemAdminGuiEventHandler.open(player)` を呼ぶ
3. `ItemAdminGuiEventHandler` が `ADMIN(99)` 権限を再確認し、`ItemService.getLoadedItems()` を基準に現在フィルタの表示対象を決定する
4. `ItemAdminGuiView` が 45 スロット内容領域へ現在ページ分のアイテムを描画する
5. プレイヤーがフィルタボタンをクリックした場合、カテゴリまたはレア度の選択状態を更新し、ページを 0 に戻して同じ GUI を再描画する
6. プレイヤーが前ページ / 次ページをクリックした場合、現在フィルタ結果に対するページ番号だけを更新して同じ GUI を再描画する
7. プレイヤーが内容領域のアイテムをクリックした場合、クリック種別から `1 / 半スタック / 1スタック` の付与量を決定し、`InventoryService.addItemToNormalInventory(..., "admin_gui")` で付与する
8. 表示アイテムは `ItemStackFactory.createShopDisplay` 相当の表現を使うため、equipment などのランダムステータスは確定値ではなく `min~max` 範囲表示で確認できる
9. 付与に成功した場合は GUI を維持したままプレイヤーインベントリだけを更新し、失敗した場合は `P_5241` を返して拒否音を再生する
