# 02_9-未決事項

## 1. 経験値付与元の配分

- [[02_3-サービス]].経験値加算 の計算自体は確定したが、経験値を付与する gameplay 契機の配分比率は未確定。
- 戦闘、クエスト、採集、クラフト、探索ボーナスごとの期待時給は今後のゲームバランス設計で確定する。

## 2. プレイヤー向け進捗表示

- 次レベル必要経験値の exact 値を非表示にする方針は確定。
- 進捗バーを `LOW` / `MID` / `HIGH` / `NEAR` の 4 段階で出すか、別の演出にするかは未確定。

## 3. 初回 welcome mail の起動責務

- 現在の `AccountService.createAccount` はアカウント登録だけを行い、mail feature を呼び出さない。
- 初回アカウント作成に連動して `welcome_mail` を作る要件を維持するか、維持する場合に user / account / player join のどこを起動責務とするかは未決定。
- 二重作成を避ける一意性・再試行仕様と合わせて決定するまで、account 設計の確定処理には含めない。
