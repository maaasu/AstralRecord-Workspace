# 01_2.00-ユースケース

## 1. ログインする

1. プレイヤーが Minecraft 内で `/web login` を実行する。
2. 表示されたコードを `/Login` へ入力する。
3. Web が空白とハイフンを除去し、大文字へ正規化する。
4. Web が API へコード消費を依頼する。
5. 成功時は Cookie を発行し、`/MyPage` へリダイレクトする。

## 2. ログアウトする

`/Logout` の POST で Cookie 認証セッションを破棄し、`/Index` へ戻します。
