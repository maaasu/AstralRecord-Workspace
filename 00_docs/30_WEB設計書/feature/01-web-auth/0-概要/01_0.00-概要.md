# 01_0.00-概要

## 1. 目的

Minecraftサーバー側で発行したログインコードを利用して、AstralRecord Webへ安全にログインする導線を提供します。`/Login` はコードと任意のID・パスワードの2方式を提供し、成功時に `/MyPage` へ遷移します。初回有効化・忘れた場合の復旧はMinecraftコードで本人確認します。

## 2. 責務

- `/Login` でログインコードを受け取り、API `POST /api/web-auth/challenges/consume` へ送る。
- 成功時に12時間有効のHTTPS専用認証Cookieを発行し、`/MyPage` へ遷移する。
- Web管理画面の要求ごとにAPIへWeb管理権限を照会し、ManagementDBの `web_admin=true` と直近5分以内のMinecraftコード認証を確認できた場合だけ許可する。
- ゲーム側permission、Cookieに残った過去の権限、API照会失敗ではWeb管理画面を許可しない。

## 3. 管理権限

Web管理者の初期指定は自動で行わない。ログイン成功後に作られた `player` に対し、運用担当者が明示的に `web_admin` を更新する。
