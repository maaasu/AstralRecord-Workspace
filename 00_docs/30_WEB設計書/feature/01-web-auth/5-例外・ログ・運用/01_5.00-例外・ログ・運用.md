# 01_5.00-例外・ログ・運用

| 状態 | 画面動作 |
|---|---|
| 入力検証失敗 | 同じページで項目エラーを表示する |
| API が 4xx を返す | 無効・期限切れ・消費済みとして案内する |
| API が 5xx、通信失敗、応答解析失敗 | API 接続不能として再試行を案内する |
| 成功 | Cookie を発行して `/MyPage` へ移動する |

通信例外は `WebAuthApiClient` が Warning ログへ記録します。ログインコード、Cookie、認証 Claim の値はログへ出力しません。
