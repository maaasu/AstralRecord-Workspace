# 23_1.00-モデル定義

## 1. マーケット出品レスポンス

物理名: `MarketListingResponse`

| フィールド | 型 | nullable | 説明 |
|---|---|---|---|
| `listingId` | GUID | ✗ | 出品 UUID |
| `sellerAccountId` | GUID | ✗ | 出品アカウント UUID |
| `sellerAccountName` | string | ✗ | 出品アカウントの表示名。公開中の出品画面で出品者を確認するために使用する |
| `buyerAccountId` | GUID | ✓ | 購入アカウント UUID。未購入時は null |
| `sourceInventoryEntryId` | GUID | ✓ | escrow 元の inventory entry。購入後は個体品を購入者 BAG へ移す際にも使用する |
| `itemCategory` | string | ✗ | `material` / `consumable` / `equipment` / `rune` 等 |
| `itemId` | string | ✗ | item マスタ ID |
| `instanceType` | string | ✓ | `EQUIPMENT` / `RUNE`。スタック品は null |
| `instanceId` | GUID | ✓ | 個体 UUID。スタック品は null |
| `quantity` | int | ✗ | 出品数量 |
| `currencyId` | string | ✗ | 支払い通貨 ID。既定は `gold` |
| `unitPrice` | long | ✗ | 1 個あたりの出品価格 |
| `totalPrice` | long | ✗ | `unitPrice * quantity` |
| `priceFloor` | long | ✗ | 店売り価格下限 |
| `referenceUnitPrice` | long | ✓ | 相場参照単価 |
| `priceDeviationRate` | decimal | ✓ | `unitPrice / referenceUnitPrice` |
| `priceConfidence` | string | ✗ | `HIGH` / `MEDIUM` / `LOW` |
| `valuationSignature` | string | ✓ | 相場評価シグネチャ |
| `valuationSnapshot` | object | ✓ | 出品時の評価詳細 |
| `status` | string | ✗ | `ACTIVE` / `SOLD` / `CANCELED` / `EXPIRED` / `SUSPENDED` |
| `statusReason` | string | ✓ | 停止・キャンセル理由 |
| `listedAt` | DateTime | ✗ | 出品日時 |
| `expiresAt` | DateTime | ✗ | 掲載期限 |
| `soldAt` | DateTime | ✓ | 購入成立日時 |
| `canceledAt` | DateTime | ✓ | キャンセル日時 |
| `version` | int | ✗ | 楽観ロック用バージョン |
| `createdAt` | DateTime | ✗ | 作成日時 |
| `updatedAt` | DateTime | ✗ | 更新日時 |

## 2. マーケット出品作成リクエスト

物理名: `MarketListingCreateRequest`

| フィールド | 型 | nullable | 説明 |
|---|---|---|---|
| `sellerAccountId` | GUID | ✗ | 出品アカウント UUID |
| `sourceInventoryEntryId` | GUID | ✗ | スタック品・個体品共通の出品元 inventory entry |
| `itemCategory` | string | ✗ | item カテゴリ |
| `itemId` | string | ✗ | item マスタ ID |
| `instanceType` | string | ✓ | 個体品の種別 |
| `instanceId` | GUID | ✓ | 個体 UUID |
| `quantity` | int | ✗ | 出品数量。個体品は 1 |
| `currencyId` | string | ✗ | 支払い通貨 ID |
| `unitPrice` | long | ✗ | 1 個あたりの出品価格 |
| `expiresAt` | DateTime | ✓ | 掲載期限。未指定時は API 既定値 |
| `createdBy` | GUID | ✗ | 作成者 UUID |

ルール:

- スタック品は `sourceInventoryEntryId` 必須、`instanceType` / `instanceId` は null。
- 個体品は `instanceType` / `instanceId` 必須、`sourceInventoryEntryId` は任意。
- `quantity` は 1 以上。個体品は必ず 1。
- `unitPrice` は 1 以上。

## 3. 相場見積リクエスト

物理名: `MarketPriceQuoteRequest`

| フィールド | 型 | nullable | 説明 |
|---|---|---|---|
| `accountId` | GUID | ✓ | 出品予定アカウント UUID。所有権確認が必要な場合に指定 |
| `itemCategory` | string | ✗ | item カテゴリ |
| `itemId` | string | ✗ | item マスタ ID |
| `instanceType` | string | ✓ | 個体品の種別 |
| `instanceId` | GUID | ✓ | 個体 UUID |
| `quantity` | int | ✗ | 数量 |
| `unitPrice` | long | ✓ | 判定したい出品単価。未指定時は相場のみ返す |

## 4. 相場見積レスポンス

物理名: `MarketPriceQuoteResponse`

| フィールド | 型 | nullable | 説明 |
|---|---|---|---|
| `itemCategory` | string | ✗ | item カテゴリ |
| `itemId` | string | ✗ | item マスタ ID |
| `instanceType` | string | ✓ | 個体品の種別 |
| `instanceId` | GUID | ✓ | 個体 UUID |
| `sellPrice` | long | ✗ | item マスタの店売り単価 |
| `suggestedUnitPrice` | long | ✗ | API 推奨単価 |
| `referenceUnitPrice` | long | ✓ | 約定履歴から算出した参照単価 |
| `sampleCount` | int | ✗ | 参照した約定件数 |
| `referenceScope` | string | ✗ | `EXACT_SIGNATURE` / `QUALITY_BUCKET` / `ITEM_ONLY` / `SUGGESTED_ONLY` |
| `confidence` | string | ✗ | `HIGH` / `MEDIUM` / `LOW` |
| `allowedMinUnitPrice` | long | ✗ | 許容下限単価 |
| `allowedMaxUnitPrice` | long | ✗ | 許容上限単価 |
| `judgement` | string | ✗ | `ALLOW` / `BLOCK_BELOW_SELL_VALUE` / `BLOCK_OUT_OF_MARKET_RANGE` / `LOW_CONFIDENCE_ALLOW` |
| `valuationSignature` | string | ✓ | 相場評価シグネチャ |
| `rollQualityScore` | decimal | ✓ | ランダムステータス品質スコア |
| `rollQualityBucket` | string | ✓ | `D` / `C` / `B` / `A` / `S` |
| `evaluatedAt` | DateTime | ✗ | 評価日時 |

## 5. 購入リクエスト

物理名: `MarketPurchaseRequest`

| フィールド | 型 | nullable | 説明 |
|---|---|---|---|
| `buyerAccountId` | GUID | ✗ | 購入アカウント UUID |
| `idempotencyKey` | string | ✗ | 二重購入防止キー |
| `updatedBy` | GUID | ✗ | 更新者 UUID |

## 6. キャンセルリクエスト

物理名: `MarketCancelRequest`

| フィールド | 型 | nullable | 説明 |
|---|---|---|---|
| `sellerAccountId` | GUID | ✗ | 出品アカウント UUID |
| `reason` | string | ✓ | キャンセル理由 |
| `updatedBy` | GUID | ✗ | 更新者 UUID |

## 7. マーケット取引レスポンス

物理名: `MarketTransactionResponse`

| フィールド | 型 | nullable | 説明 |
|---|---|---|---|
| `transactionId` | GUID | ✗ | 取引 UUID |
| `listingId` | GUID | ✗ | 出品 UUID |
| `sellerAccountId` | GUID | ✗ | 出品者アカウント UUID |
| `buyerAccountId` | GUID | ✗ | 購入者アカウント UUID |
| `itemCategory` | string | ✗ | item カテゴリ |
| `itemId` | string | ✗ | item マスタ ID |
| `instanceType` | string | ✓ | 個体品の種別 |
| `instanceId` | GUID | ✓ | 個体 UUID |
| `quantity` | int | ✗ | 購入数量 |
| `currencyId` | string | ✗ | 支払い通貨 ID |
| `unitPrice` | long | ✗ | 単価 |
| `totalPrice` | long | ✗ | 総額 |
| `feeAmount` | long | ✗ | 手数料 |
| `sellerProceeds` | long | ✗ | 売主受取額 |
| `affectedInventoryEntryIds` | GUID[] | ✗ | 購入者が Plugin inventory を API 正本へ再同期するための更新 entry ID |
| `completedAt` | DateTime | ✗ | 成立日時 |

## 8. アカウントマーケット概要レスポンス

物理名: `MarketAccountSummaryResponse`

| フィールド | 型 | nullable | 説明 |
|---|---|---|---|
| `accountId` | GUID | ✗ | アカウント UUID |
| `activeListingCount` | int | ✗ | 現在の出品数 |
| `maxActiveListingCount` | int | ✗ | 互換用。`maxListingSlotCount` と同じ値 |
| `usedListingSlotCount` | int | ✗ | `ACTIVE` / `SUSPENDED` / `CANCELED` を含む使用済み出品枠数 |
| `maxListingSlotCount` | int | ✗ | 現在の最大出品枠数 |
| `completedTradeCount` | int | ✗ | Tier 算出対象の累計成立取引数 |
| `tier` | string | ✗ | `T0` / `T1` / `T2` / `T3` / `T4` |
| `suspendedUntil` | DateTime | ✓ | マーケット利用停止期限 |
| `updatedAt` | DateTime | ✗ | 更新日時 |
