# 23_9.00-未決事項

## Q-MARKET-001 通貨残高の API 正本化

現状、通貨 feature は Plugin 設計書に存在する。
マーケット購入では Plugin/Web 双方から同じ残高更新を行う必要があるため、通貨残高を API 内部サービスまたは API feature として正本化する。

初期実装では、購入時に item 所有権移転、出品 `SOLD` 化、約定履歴、成立取引数更新までを行う。
通貨残高の減算・加算は本未決事項の解決後に追加する。

判断待ち:

- 通貨残高テーブルを API/DB 側に追加するか
- 既存 Plugin 通貨 feature を API 経由へ移行するか

## Q-MARKET-002 マーケット手数料

本設計では `feeAmount` / `sellerProceeds` を持つが、手数料率は未確定。
初期実装では `0%` とし、運用で必要になった時点で設定化する。

## Q-MARKET-003 ステータス重み定義

ランダムステータス品質は、ステータスごとの重み定義がある場合に重み付き平均を使う。
初期実装では等重み平均を既定とし、将来 filebase に `market.statWeights` を追加できる余地を残す。
