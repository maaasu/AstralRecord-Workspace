# 23_9.00-未決事項

## Q-MARKET-001 通貨残高の API 正本化 — 解決済み

マーケット購入は、既存 Plugin の Gold 額面を持つ `GAME/CURRENCY` inventory entry を API transaction 内で更新する。
Gold 専用の新テーブルは追加しない。API は額面を正規化して buyer から seller へ移し、Plugin は `affectedInventoryEntryIds` と inventory 三者マージで購入者のローカル状態を再同期する。

## Q-MARKET-002 マーケット手数料

本設計では `feeAmount` / `sellerProceeds` を持つが、手数料率は未確定。
初期実装では `0%` とし、運用で必要になった時点で設定化する。

## Q-MARKET-003 ステータス重み定義

ランダムステータス品質は、ステータスごとの重み定義がある場合に重み付き平均を使う。
初期実装では等重み平均を既定とし、将来 filebase に `market.statWeights` を追加できる余地を残す。
