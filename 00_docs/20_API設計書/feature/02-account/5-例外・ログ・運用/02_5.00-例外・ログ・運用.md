# 02_5.00-例外・ログ・運用

API `account` 機能の例外応答、ログ、運用上の注意点を集約する。  
本機能のログ・メッセージはコード上にハードコーディングする。LogId は使用しない。

## 1. 共通エラーレスポンス

| ステータス | 発生条件 | ボディ |
|---|---|---|
| 401 Unauthorized | `X-Api-Key` 欠落 / 不一致 | なし |
| 400 Bad Request | Body のバリデーション失敗 | `ProblemDetails` 互換 |
| 409 Conflict | 未削除アカウントの `accountName` と大文字小文字を区別しない重複 | `{ "message": "..." }` |
| 500 Internal Server Error | 上記以外の例外 | `ProblemDetails` 互換 |

## 2. ログ一覧

### 2.1 取得系（[[02_3.00-索引]] アカウント取得）

| 項目 | 内容 |
|---|---|
| レベル | Error |
| テンプレート | `Failed to get account {Uuid}` |
| 出力条件 | リポジトリ呼び出しで例外発生時 |
| 引数 | `{Uuid}` = アカウントUUID |
| 出力例外 | `_logger.LogError(ex, ...)` で添付 |

### 2.2 登録系（[[02_3.00-索引]] アカウント登録）

| 項目 | 内容 |
|---|---|
| レベル | Error |
| テンプレート | `Failed to create account for user {UserId}` |
| 出力条件 | リポジトリ呼び出しで例外発生時 |
| 引数 | `{UserId}` = リクエストの `userId` |
| 出力例外 | `_logger.LogError(ex, ...)` で添付 |

### 2.3 更新系（[[02_3.00-索引]] アカウント更新）

| 項目 | 内容 |
|---|---|
| レベル | Error |
| テンプレート | `Failed to update account {Uuid}` |
| 出力条件 | リポジトリ呼び出しで例外発生時 |
| 引数 | `{Uuid}` = アカウントUUID |
| 出力例外 | `_logger.LogError(ex, ...)` で添付 |

### 2.4 削除系（[[02_3.04-削除系]] アカウント削除）

| 項目 | 内容 |
|---|---|
| レベル | Error |
| テンプレート | `Failed to delete account {Uuid}` |
| 出力条件 | 削除トランザクションで例外発生時 |
| 引数 | `{Uuid}` = アカウントUUID |
| 出力例外 | `_logger.LogError(ex, ...)` で添付 |

## 3. 運用上の注意

- account UUID はサーバ側で生成する。クライアント側の値は無視する。
- 手動変更のアカウント名は ASCII 英字（`A-Z` / `a-z`）のみ、1〜50文字とする。自動作成時はこの入力規則を適用せず、重複時に `名前(1)`、`名前(2)` の形式で空き名を生成する。
- 未削除アカウントのアカウント名は大文字小文字を区別せず一意とする。既存データの重複は `20260901_account_mailbox_name.sql` で整理する。
- `userId` の存在検証は API 単体では行わない。プラグイン側で先に `01-user` API のユーザ登録を完了させる前提。
- 論理削除済みレコードへの操作は常に 404。
- アカウント削除は履歴参照を維持する論理削除であり、履歴または共有データの物理削除を行わない。
