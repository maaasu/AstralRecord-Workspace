# 15_1.00-モデル定義

ルーン専用の動的 DTO は持たない。ルーンの表示・効果・装着条件は item API の `ItemResponse` に含まれる `category = rune` と `rune` 定義を正本とする。

所持数は inventory entry の `itemId` と `quantity`、装着状態は equipment API の `runes[].itemId` と `slotIndex` で表す。ルーン個体 ID、乱数ロール、所有 account は存在しない。
