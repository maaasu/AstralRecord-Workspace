# リリースノート登録・Discord通知 概要

公開対象のリリースノートはWebのデプロイ成果物に含まれる Markdown から取得する。Web は起動時に公開済みノートをAPIへ登録し、APIは同一トランザクションで `release_note` と通知Outboxを更新する。

同じ `slug` と同じ通知チャネルのOutboxは一意に保ち、Web再起動時の再同期で重複通知を発生させない。Discord送信はAPIのバックグラウンドワーカーが担当する。
