# 01_9.00-未決事項

API `user` 機能で確定していない事項を集約する。確定したら本ファイルから削除し、対応章を更新する。

## 1. BAN 解除日時 (`banDate`) の運用

- `banIndefinite = true` のとき `banDate` をどう扱うかが未確定。
- 候補:
  - `banDate` を `null` に強制する
  - `banIndefinite` 優先で `banDate` を無視する
- 決定次第 [[01_3.03-更新系]].ユーザ更新 の処理内容に反映する。

## 2. 物理削除エンドポイントの要否

- 現状は論理削除のみ想定。完全削除（GDPR 相当）の必要性が確認できれば DELETE エンドポイント追加を検討する。

## 3. レート制限・スロットリング

- API キー単位のレート制限ポリシーが未定。共通基盤側で扱う想定だが本機能特有の制限要否は要確認。
