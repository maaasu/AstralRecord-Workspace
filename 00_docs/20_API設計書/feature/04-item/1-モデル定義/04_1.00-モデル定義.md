# 04_1.00-モデル定義

## 1. アイテム一覧レスポンス要素

物理名: `ItemListItem`（`ItemResponse` 縮約）

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | string | アイテムID |
| `category` | string | カテゴリ |

## 2. アイテム詳細レスポンス

物理名: `ItemResponse`

### 2.1 共通フィールド

| フィールド | 型 | 説明 |
|---|---|---|
| `schemaVersion` | int | YAML スキーマバージョン |
| `id` | string | アイテムID |
| `category` | string | `material` / `orb` / `consumable` / `equipment` / `currency` / `bundle` / `rune` / `skill_gem` / `sigil` |
| `name` | string | アイテム名 |
| `icon` | string | Minecraft マテリアル名 |
| `rarity` | string | `COMMON` / `UNCOMMON` / `RARE` / `EPIC` / `LEGENDARY` 等 |
| `saleValue` | int | 売却価格 |
| `customModelData` | int? | カスタムモデルデータ |
| `maxStack` | int | 最大スタック数 |
| `lore` | string[] | 説明文 |
| `unTradeable` | bool | 取引不可フラグ |
| `unSellable` | bool | 売却不可フラグ |

### 2.2 カテゴリ別拡張

カテゴリに応じて以下のいずれかのオブジェクトが含まれる（他は `null` または欠落）。

- `consumable`: `consumable.onUse.{usingSound, sound, effect, amount, useTimeTicks, cooldownTicks}`, `consumable.effects[]`
- `equipment`: `equipment` オブジェクト（[[04_3.00-索引]] equipment フィールド表参照）
- `currency`: `currency.{type, group, expiresAt}`
- `bundle`: `bundle.{lootTableId, items[], onUse}`
- `rune`: `rune.{targetSlots, requiredEnhanceLevel, stats[]}`。スキル付与は行わない
- `orb`: `orb.effect`。`type` ごとに強化・修理・状態変化・エンチャントの対象条件と動作を持つ
- `skillGem`: `skillGem.skillId`。skill masterからAPIが仮想生成する
- `sigil`: `sigil.{equipGroupId, modifiers[]}`

`bundle.onUse` は filebase の inline 演出定義を返す。`sound` は `{sound, volume, pitch}`、`particle` は `{particle, count, originOffsetX, originOffsetY, originOffsetZ, offsetX, offsetY, offsetZ, extra}` のオブジェクトである。

`consumable.onUse.usingSound` は使用開始時・待機中、`sound` は効果適用後に再生する sound key を表す。`useTimeTicks` は使用完了までの待機時間、`cooldownTicks` は使用成功後のクールダウンを表す（20 tick = 1 秒）。省略時はサウンドが `null`、待機時間とクールダウンが `40`。

仮想スキルジェムはID `00_skill_gem_<skillId>`、表示名 `<スキル名>ジェム`、`maxStack = 0`（非スタック）とする。iconは`skill.gem.icon`からskill icon、通常iconの順にfallbackし、取引・売却はskill masterの指定がない限り不可とする。

詳細フィールドは本 API 設計書の item モデル定義を正本として扱う。
将来的にここへ転記する場合は YAML スキーマ（`40_filebase/10.features.item/`）と同期させること。

equipment の装備条件と状態変化条件は次の構造で返す。

| フィールド | 型 | 説明 |
|---|---|---|
| `equipment.requiredLevel` | int | 必要プレイヤーレベル。`0` は制限なし |
| `equipment.requiredClasses` | object[] | 装備可能な現在クラスと必要クラスレベル。空配列はクラス制限なし |
| `equipment.requiredClasses[].classId` | string | 現在選択中クラスと照合するクラス ID |
| `equipment.requiredClasses[].level` | int | 必要クラスレベル |
| `equipment.transcendence[].name` | string? | 状態変化後の表示名。候補一覧ではrank数値の代わりに使用する |
| `equipment.transcendence[].rank` | int | 状態変化後の目標rank |
| `equipment.transcendence[].requiredEnhanceLevel` | int | 状態変化に必要な最小強化値 |
| `equipment.transcendence[].requiredMaterials` | object[] | 状態変化確認画面で検証・消費する素材IDと数量 |
| `equipment.transcendence[].requiredCurrency` | int | 状態変化確認画面で検証・消費するgold |

状態変化の必要素材・通貨は装備マスタから直接返し、`recipeId` は持たない。

装備強化とエンチャントは次の縮約契約を返す。

| フィールド | 型 | 説明 |
|---|---|---|
| `equipment.enhance.levels[].successRate` | float | 当該レベルへの強化成功率 |
| `equipment.enhance.levels[].failAction` | string | `NONE` / `SET_LEVEL` / `DECREASE_ONE` |
| `equipment.enhance.levels[].failTargetLevel` | int? | `SET_LEVEL` の失敗後レベル |
| `equipment.enchant.maxSlots` | int | エンチャント最大枠数。装備個別の抽選候補は持たない |

`equipment.enhance.levels[]` に強化用素材・通貨・recipeは返さない。強化はオーブ1個だけで試行する。

### 2.3 オーブ効果

| フィールド | 型 | 説明 |
|---|---|---|
| `orb.effect.type` | string | `ENHANCE` / `REPAIR` / `TRANSCENDENCE` / `ENCHANT` |
| `orb.effect.targetSlots` | string[] | 強化対象の装備slot |
| `orb.effect.rank` | int? | 強化の現在状態rank、または状態変化の目標rank条件 |
| `orb.effect.rankMode` | string | `EXACT` / `AT_MOST` |
| `orb.effect.repairAmount` | int? | 固定耐久回復量 |
| `orb.effect.repairFull` | bool | 最大耐久まで回復するか |
| `orb.effect.enchantMasterId` | string? | 共通エンチャントマスタの必須参照（`enchant:<id>`） |
| `orb.effect.enchantOperation` | string? | `OVERWRITE_RANDOM` / `FILL_ONE_EMPTY` / `FILL_ALL_EMPTY` |

### 2.4 共通エンチャントマスタ

物理名: `EnchantMasterResponse`

| フィールド | 型 | 説明 |
|---|---|---|
| `schemaVersion` | int | 共通エンチャントマスタのスキーマバージョン |
| `id` | string | 共通エンチャントマスタID |
| `targets[].equipmentType` | string | `WEAPON` / `ARMOR` / `ACCESSORY` |
| `targets[].entries[].effectId` | string | 装備内重複判定に使う安定した効果ID |
| `targets[].entries[].status` | string | 付与ステータス |
| `targets[].entries[].type` | string | `FLAT` / `SCALAR` |
| `targets[].entries[].value` | string | 固定値または範囲値 |
| `targets[].entries[].weight` | int | 重み付き抽選の重さ |

## 3. セット効果レスポンス

物理名: `SetEffectResponse`

対象: `GET /api/seteffect`, `GET /api/seteffect/{setId}`

| フィールド | 型 | 説明 |
|---|---|---|
| `id` | string | セット効果 ID |
| `name` | string | セット効果名 |
| `pieces` | セット効果ピース[] | 装着数ごとの発動効果 |

### 3.1 セット効果ピース

物理名: `SetEffectPieceResponse`

| フィールド | 型 | 説明 |
|---|---|---|
| `count` | int | 効果発動に必要な同セット装備数 |
| `stats` | セット効果ステータス[] | 付与ステータス |

セット効果はステータスだけを付与し、スキル付与フィールドを持たない。

### 3.2 セット効果ステータス

物理名: `SetEffectStatResponse`

| フィールド | 型 | 説明 |
|---|---|---|
| `status` | string | ステータス種別 |
| `type` | string | 加算・乗算などの適用方式 |
| `value` | string | 適用値 |
