# 13_0.00-概要

## 1. 目的

`dbo.inventory` / `dbo.inventory_entry` への CRUD 契約を提供する。
インベントリ本体（コンテナ）とエントリ（中身）を別レイヤーで扱う。

## 2. 用途分離

- `inventoryProfile` で用途を分離する（`GAME` / `BUILDER`）。
- `inventoryType` の妥当性は API 単体ではマスタ参照しないため、呼び出し元の定義と合わせて扱う。

## 3. ペイロードルール

- スタックアイテム: `itemId` を使用
- インスタンス系アイテム: `instanceType` + `instanceId` を使用
- `itemId` と `instanceType`/`instanceId` は同時使用しない
- `quantity` は 1 以上

## 4. 構成要素

- `Controllers/InventoryController`
- `Repositories/InventoryRepository`
- `Models/InventoryModels.cs` 内の各 DTO

## 5. 関連設計書

- モデル定義: [[13_1.00-モデル定義]]
- エンドポイント索引: [[13_3.00-索引]]
- 障害対応: [[13_5.00-例外・ログ・運用]]
- DB: `40_database/AstralRecord/dbo.inventory/` / `dbo.inventory_entry/`
