# 13_3.03-更新系

## 1. 更新系エンドポイント仕様

### インベントリ更新

HTTPメソッド: `PUT`
パス: `/api/inventory/{inventoryId}`
コントローラ: `InventoryController`
アクション物理名: `UpdateInventory`

リクエスト:

| 種別 | 名前 | 型 | 必須 | 説明 |
|---|---|---|---|---|
| Header | `X-Api-Key` | string | ✓ | API キー |
| Header | `Content-Type` | string | ✓ | `application/json` |
| Path | `inventoryId` | GUID | ✓ | 更新対象UUID |
| Body | (全体) | [[13_1.00-モデル定義]].インベントリ更新リクエスト | ✓ | 更新内容 |

レスポンス:

| ステータス | 内容 | ボディ |
|---|---|---|
| 200 OK | 更新成功 | [[13_1.00-モデル定義]].インベントリレスポンス |
| 400 Bad Request | バリデーション失敗 | エラーオブジェクト |
| 401 Unauthorized | API キー未指定/無効 | なし |
| 404 Not Found | 未登録/論理削除済み | なし |

処理内容:

1. 認証パイプラインで `X-Api-Key` を検証する
2. リクエスト Body のバリデーション（`updatedBy` 必須。`inventoryProfile` 指定時は `GAME` または `ADMIN`）
3. 対象インベントリを取得（`is_deleted = 0`）。なければ 404
4. 非 null 項目のみ反映し `updatedBy` / `updatedAt` を更新する
5. 200 を返す
6. 例外時は [[13_5.00-例外・ログ・運用]].更新系 のログを記録し 500 を返す

---

### エントリ更新

HTTPメソッド: `PUT`
パス: `/api/inventory/entries/{inventoryEntryId}`
コントローラ: `InventoryController`
アクション物理名: `UpdateEntry`

リクエスト:

| 種別 | 名前 | 型 | 必須 | 説明 |
|---|---|---|---|---|
| Header | `X-Api-Key` | string | ✓ | API キー |
| Header | `Content-Type` | string | ✓ | `application/json` |
| Path | `inventoryEntryId` | GUID | ✓ | エントリUUID |
| Body | (全体) | [[13_1.00-モデル定義]].インベントリエントリ更新リクエスト | ✓ | 更新内容 |

レスポンス:

| ステータス | 内容 | ボディ |
|---|---|---|
| 200 OK | 更新成功 | [[13_1.00-モデル定義]].インベントリエントリレスポンス |
| 400 Bad Request | バリデーション失敗 | エラーオブジェクト |
| 401 Unauthorized | API キー未指定/無効 | なし |
| 404 Not Found | 未登録/論理削除済み | なし |

処理内容:

1. 認証パイプラインで `X-Api-Key` を検証する
2. リクエスト Body のバリデーション（`updatedBy` 必須）
3. 対象エントリを取得（`is_deleted = 0`）。なければ 404
4. 非 null 項目のみ反映し `updatedBy` / `updatedAt` を更新する
5. 200 を返す
6. 例外時は [[13_5.00-例外・ログ・運用]].更新系 のログを記録し 500 を返す

---

### エントリ一括置換

HTTPメソッド: `PUT`
パス: `/api/inventory/{inventoryId}/entries`
コントローラ: `InventoryController`
アクション物理名: `ReplaceEntries`

リクエスト:

| 種別 | 名前 | 型 | 必須 | 説明 |
|---|---|---|---|---|
| Header | `X-Api-Key` | string | ✓ | API キー |
| Header | `Content-Type` | string | ✓ | `application/json` |
| Path | `inventoryId` | GUID | ✓ | 置換対象インベントリUUID |
| Body | (全体) | [[13_1.00-モデル定義]].インベントリエントリ一括置換リクエスト | ✓ | 置換後 entry 一覧 |

レスポンス:

| ステータス | 内容 | ボディ |
|---|---|---|
| 200 OK | 置換成功 | [[13_1.00-モデル定義]].インベントリエントリレスポンス配列 |
| 400 Bad Request | バリデーション失敗 | エラーオブジェクト |
| 401 Unauthorized | API キー未指定/無効 | なし |
| 404 Not Found | 対象インベントリ未登録/論理削除済み | なし |

処理内容:

1. 認証パイプラインで `X-Api-Key` を検証する
2. `entries` の各要素で `itemId` と `instanceType`/`instanceId` の排他、および `quantity >= 1` を検証する
3. 対象インベントリを取得（`is_deleted = 0`）。なければ 404
4. `inventoryEntryId` 付き要素は同じaccountの既存entryを更新し、必要なら同account内の対象inventoryへ移動する
5. UUID未指定要素だけを新規採番し、client採番UUIDが指定されていればその値を維持する
6. 既存UUIDには取得時の `updatedAt` を `expectedUpdatedAt` として必須指定し、DB正本と一致しない古い要求を拒否する
7. 論理削除済みUUIDの再利用、別account UUID、request内重複UUIDを拒否する
8. serializable transaction内で対象inventoryの現在行と移動対象行を一度論理削除して保存し、最終配置で再有効化する。これによりslot交換時のactive一意制約への途中衝突を防ぐ
9. requestに存在しない対象inventory内の有効entryは論理削除状態のまま保持する
10. 作成・更新後のentry配列を200で返す
11. 例外時は [[13_5.00-例外・ログ・運用]].更新系のログを記録し500を返す
