# 11_0.00-概要

## 1. 位置づけ

filebase `40_filebase/30.features.skill/` を正本として定義された `skill` マスタを、MasterDataDB 経由で取得する読み取り専用 API。

## 2. 対象実装

- `Controllers/SkillController`
- `Repositories/SkillRepository`（MasterDataDB 参照）
- `Models/SkillResponse`

## 3. 関連設計

- モデル定義: [[11_1.00-モデル定義]]
- エンドポイント一覧: [[11_3.00-索引]]
- MasterDataDB 設計: [[MasterDataDB設計]]
- 関連: [[10_README]]（クラスから参照）, 将来の `onUse` 実装では [[14_README]] など

## 4. 責務分担

- filebase:
  - スキルの表示項目、top-level `resourceType` / `resourceCost`、`implementationId`、実装固有 `params` を定義する正本。
- API:
  - 正本から投入されたデータをそのまま返却する。
  - `params` の詳細妥当性や `implementationId` に対応する Plugin 実装クラスの存在は検証しない。
- Plugin:
  - `implementationId` と実装クラスの紐付け、共通項目と `params` の詳細検証、発動ロジックを担当する。
  - 旧定義は `params.resourceType` / `params.resourceCost`、`manaCost` の順で互換解決する。
