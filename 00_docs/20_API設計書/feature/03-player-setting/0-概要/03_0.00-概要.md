# 03_0.00-概要

`player-setting` はユーザ単位のプレイヤー設定を扱う API です。
設定は `account` ではなく `user` に紐づきます。

## 目的

- ユーザ単位で設定キー/設定値(JSON)を保持する
- 設定更新時に `version` で楽観ロック競合を検知する

## 対応テーブル

- `AstralRecord.dbo.user_setting`
