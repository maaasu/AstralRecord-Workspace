# 27_9.00-未決事項

## 1. プレイヤークエスト状態の本番永続化

現在の実装は `quest-states/<accountUuid>.yml` に保存する。  
モノレポ方針では動的データはSQL Server/API管理が正本のため、本番運用では以下を追加検討する。

- `AccountQuest` 相当のDBテーブル
- 受領中、報告可能、完了済み、クールタイムのAPI
- 報酬付与と状態更新のトランザクション境界
- サーバー多重起動時の二重報酬防止

## 2. 将来の目標種別

初期実装は `KILL_MOB` と `GATHERING` のみ。将来候補は以下。

- アイテム納品
- NPC会話
- 特定地点到達
- クラフト
- ダンジョン/ボス討伐
- 複数条件の組み合わせ

## 3. クエスト破棄確認

現在の一覧GUIではクリックで即破棄する。誤操作が問題になる場合は確認GUIを追加する。
