# 09_3.07-GUI・View

## 共通閉じる表示規則

- `HotbarShortcutGuiHolder` を実装する GUI は、プレイヤーインベントリの BAG 制御列中央 `slot 26` に表示する共通閉じるアイコンを使用する。ホットバー9枠は通常表示を維持する。
- 上記 GUI のチェスト側には閉じるアイコンを置かず、背景アイテムをクリックしても閉じる処理を発火させない。
- 共通ページング GUI はチェスト側に閉じるアイコンを持たず、閉じる操作はプレイヤーインベントリ側の共通ホットバーへ統一する。
- 表示した操作アイコンとイベントの発火対象を一致させ、不可視の操作スロットを作らない。ただし、明示的に設計された管理者向け隠し操作は除く。

## 1. メインメニュー表示

クラス名: `MenuView`
物理名: `open`

責務:

メインメニュー GUI を生成してプレイヤーへ開く。

処理内容:

1. `MenuInventoryHolder` に `MenuScreen.MAIN` を設定した inventory を作成する
2. `CurrencyService.getGoldAmount` で現在のゴールド所持量を取得する
3. `MainMenuScreenView.render` で画面を描画し、通貨アイコンの lore に `ゴールド: <amount>` を表示する
4. `slot 25` に `BEACON` の `帰還` アイコンを配置し、lore に「3 秒静止で帰還」と `100 x Lv` の必要ゴールド量を表示する
4. `player.openInventory` を呼び出す

## 2. アカウント情報表示

クラス名: `MenuView`
物理名: `openStatus`

責務:

選択中アカウントの基本情報、アカウント統計、現在の [[07_1.00-モデル定義]].ステータススナップショット を GUI で表示する。

処理内容:

1. `MenuScreen.STATUS` の inventory をタイトル `アカウント情報` で作成する
2. `StatusScreenView.render` でアカウント名、Lv、累計経験値、初ログイン日、最終ログイン日、プレイ時間、アカウント作成日、現在 HP / MP / EN とカテゴリ別ステータスを描画する
3. back slot からメインメニューへ戻れるようにする

## 3. 装備 GUI 表示

クラス名: `MenuView`
物理名: `openEquipmentGui`

責務:

装備・アクセサリ編集用 GUI を表示する。

処理内容:

1. `MenuScreen.EQUIPMENT_GUI` の inventory を作成する
2. 頭・胴・脚・足・アクセサリ 1〜7 の slot に装備 ItemStack または placeholder を描画する
3. back slot を配置し、閉じる操作はプレイヤーインベントリ側の BAG 制御列中央 `slot 26` に限定する
4. クリック時の保存・検証は inventory feature のイベントへ委譲する

## 3.1 装備強化 GUI 表示

クラス: `EquipmentEnhancementService`
対象: `open`

概要

NPC interaction など明示導線から開く装備強化用 GUI を表示する。装備 GUI からの遷移アイコンと装備強化 GUI の戻るボタンは持たず、強化時の自動オープンは行わない。

処理の要点:

1. `MenuScreen.EQUIPMENT_ENHANCE` の 54 スロット inventory を作成する。
2. GUI 表示中も統合 `BAG` と通常ホットバーを維持し、BAG／ホットバー内の装備クリックを強化対象選択として優先する。対象外アイテムは通常のインベントリ操作へ委譲する。
3. 装備をセットした時点で item master の `equipment.enhance.levels[*].requiredMaterials` / `requiredCurrency` / `successRate` / `failAction` を参照し、次強化段階が存在する場合のみ必要素材・必要ゴールド・成功率・失敗時挙動を日本語で表示する。強化不可の場合は理由を実行アイコンへ表示する。
4. 必要素材は inventory feature の所持数を参照し、GUI 上の消費アイテム表示用アイテム 1 個の lore に「必要数 / 所持数」の一覧として表示する。実行ボタン押下時にのみ消費する。
5. 実行時は補償用 inventory state を取得してから必要素材と Gold を消費し、その後に装備強化 API を呼ぶ。支払いまたは API 更新失敗時は支払い前 state へ復元する。
6. API の装備強化結果に応じて成功 / 失敗 / 降格 / 破壊を反映し、プレイヤーメッセージとサウンド・パーティクルを再生する。
7. 戻る / ホットバー閉じる時はセット中の装備をプレイヤー所持へ戻し、下部表示インベントリも元の表示種別へ復元する。GUI 内に専用の閉じるボタンは配置しない。

## 3.2 装備修理 GUI 表示

クラス: `EquipmentRepairService`
対象: `open`

概要

NPC interaction から開く装備修理用 GUI を表示する。メインメニューには修理導線を置かず、拠点などに配置された修理 NPC からのみ開く。

処理の要点:

1. `MenuScreen.EQUIPMENT_REPAIR` の 54 スロット inventory を作成する。
2. GUI 表示中も統合 `BAG` と通常ホットバーを維持し、BAG／ホットバー内の装備クリックを修理対象選択として優先する。対象外アイテムは通常のインベントリ操作へ委譲する。
3. 対象装備の `durabilityValue` / `durabilityMax` から回復耐久と必要 Gold を表示し、耐久値なし・最大耐久済み・所持 Gold 不足は実行不可として表示する。
4. 実行時は補償用 inventory state を取得して Gold を消費し、装備耐久更新 API を呼ぶ。API 失敗時は支払い前 state へ復元し、更新と支払いの両方が成功した場合だけ GUI と保存処理へ反映する。
5. 閉じる時はセット中の装備をプレイヤー所持へ戻し、下部表示インベントリも元の表示種別へ復元する。

## 4. 通貨 GUI 表示

クラス名: `MenuView`
物理名: `openCurrency`

責務:

通貨 ItemStack 一覧をページング GUI として表示する。

処理内容:

1. `MenuScreen.CURRENCY` の inventory を作成する
2. currency feature から通貨 ItemStack 一覧を取得する。`gold` は Plugin 組み込みマスタとして扱い、未所持でも `0` の表示対象に含める。`astrald` は Plugin 組み込みマスタとして扱い、所持エントリがある場合に表示対象へ含める
3. pageIndex に対応するアイテム範囲を描画する。各通貨 ItemStack の lore には装飾付きの所持量表示を追加する
4. 前ページ、次ページ、戻るボタンを配置し、閉じる操作はプレイヤーインベントリ側の BAG 制御列中央 `slot 26` に限定する

## 5. バフ GUI 表示

クラス名: `MenuView`
物理名: `openBuff`

責務:

現在付与されているバフ一覧を GUI で表示する。

処理内容:

1. `MenuScreen.BUFF` の inventory を作成する
2. `BuffScreenView.render` で `ActiveBuff` の表示名、ID、種別、残り時間、補正概要を描画する
3. バフ GUI を開いている間は `MenuOpenEventHandler` が 20 tick ごとに有効バフ一覧を再取得し、残り時間を再描画する
4. バフが存在しない場合は「バフなし」アイコンを表示する
5. back slot からメインメニューへ戻れるようにする

## 6. ショートカットスロット選択画面表示（廃止済み）

クラス名: `MenuView`
物理名: `openShortcutSlotSelector`

責務:

旧プレイヤー別ショートカット設定の GUI。現行仕様では表示しない。

処理内容:

1. 新規実装では呼び出さない
2. 固定 4 スロットの変更は [[09_1.00-モデル定義]].固定メニューショートカット設定 を更新する

## 7. ショートカット項目選択画面表示（廃止済み）

クラス名: `MenuView`
物理名: `openShortcutActionSelector`

責務:

旧プレイヤー別ショートカット設定の GUI。現行仕様では表示しない。

処理内容:

1. 新規実装では呼び出さない
2. `MenuInventoryHolder.shortcutSlotIndex` は互換フィールドとして残るが、固定ショートカットでは使用しない

## 8. クラフトショートカット描画

クラス名: `MenuView`
物理名: `renderCraftShortcuts`

責務:

プレイヤーの 2x2 クラフト欄へショートカットアイコンを描画する。

処理内容:

1. `CraftShortcutView.createCraftResultIcon` で成果物スロットのメニューアイコンを作成する
2. `MenuShortcutSettings.defaults()` の固定 4 action を 2x2 スロットへ配置する
3. 現在表示中インベントリと一致する action には glow を付ける
4. 描画抑止フラグが立っているプレイヤーは描画しない
5. `STATUS` のクラフトスロットはプレイヤーヘッドで表示し、表示名を `アカウント情報` とする。lore には選択中アカウント名、Lv、アカウントスロット、モード、累計経験値、主要ステータス、開く操作を表示し、glow を付ける

備考:

- クラフト欄ショートカットは「アイコン表示のみ」であり、実クラフト処理は menu feature では行わない。クラフト欄からのアイテム生成・素材消費は発生しない前提とする。
- 描画抑止フラグはメインメニュー／通貨 GUI の遷移中に一時的に有効化し、遷移完了後に解除する（[[09_3.01-イベント]] 参照）。

## 追記（ゴミ箱GUI/View）
### ゴミ箱GUI表示
クラス名: MenuView
物理名: openTrash
- タイトル: ゴミ箱。
- ページングあり（0-44内容、45前ページ、53次ページ）。
- 戻るボタンあり（slot 49）。
- 下段左から3番目（slot 47）にゴミ箱ガイドアイテムを設置し、以下のクリック仕様と閉じる時の動作を記載する。
  - 左クリック: 1 個ゴミ箱へ
  - Shift+左クリック: すべてゴミ箱へ
  - 右クリック: 半分（端数切り上げ）ゴミ箱へ
  - ゴミ箱→インベントリ方向も同じクリック仕様
  - GUI を閉じるとゴミ箱内のアイテムはインベントリへ戻り、返却対象がある場合は戻した旨のメッセージを表示する
  - 廃棄ボタン（slot 51）で廃棄確認画面へ遷移する
- slot 51 は「廃棄」アイコン（Material.BARRIER, 赤文字）で、押下時に廃棄確認画面を開く。

### ゴミ箱確認GUI表示
クラス名: MenuView
物理名: openTrashConfirm
- タイトル: 本当に廃棄しますか？（赤文字）。
- ページングあり、内容閲覧のみ。
- 戻るボタン非表示。
- 非表示戻る枠の左右（slot 48/50）に廃棄アイコンとゴミ箱GUIに戻るアイコンを表示。

## 追記（2026-05-27）メニューからゴミ箱へ遷移

- MainMenuScreenView にゴミ箱アイコン（slot 23）を配置する。
- MenuView.TRASH_SLOT で参照し、イベントハンドラから openTrash に遷移する。

## 追記（2026-05-30）メニューからバフ・通貨GUIへ遷移

- MainMenuScreenView にバフアイコン（slot 30）と通貨アイコン（slot 32）を配置する。

- バフアイコン押下時は現在有効なバフ一覧を `MenuView.openBuff` で開く。
- 通貨アイコン押下時は `MenuView.openCurrency` で通貨ページング GUI を開く。
- 通貨アイコンの lore にはゴールド所持量を表示する。

## 追記（2026-06-13）メニューから拠点へ帰還

- MainMenuScreenView に `帰還` アイコン（slot 25, `BEACON`）を配置する。
- `帰還` アイコンの lore には、3 秒間移動しなければ帰還することと、`100 x Lv` で計算した必要ゴールド量を表示する。
- クリック時は GUI を閉じ、world feature の `ReturnToBaseService` がタイトル・サブタイトル・ボスバー・パーティクル・サウンド付きの 3 秒カウントダウンを開始する。
## 追記（2026-05-28）ゴミ箱GUIのスタック個数表示

- `openTrash` / `openTrashConfirm` で表示する ItemStack が 2 個以上のスタックである場合は、Lore に `個数: <amount>` を追記して表示する。
## 追記（2026-05-29）ゴミ箱GUIの空スロット表示とGUI内スタック集約

- `openTrash` / `openTrashConfirm` の 0-44 スロットでアイテム未配置の枠は、AIR ではなく `GRAY_STAINED_GLASS_PANE` のプレースホルダーを表示する。
- プレースホルダーは表示専用であり、ゴミ箱内アイテムの保持・返却・廃棄対象には含めない。
- ゴミ箱GUIでは `maxStack > 1` の同種アイテムを GUI 内で 1 スロットに集約し、通常の Minecraft スタック上限を超えて保持できる。
- 集約表示されたアイテムには Lore で `スタック: <amount>` を追記し、GUI 上の合計保持数を確認できるようにする。
## 追記（2026-05-29）ゴミ箱GUIの通常スタック上限準拠

- ゴミ箱GUIの同種アイテム集約は、通常の Minecraft スタック上限までを同一スロットにまとめる。
- 上限を超える個数は別スロットへ分割表示する。
- Lore の `スタック: <amount>` は、各表示スロットに入っている個数を表す。

## 追記（2026-05-30）メニュー系 GUI の下部インベントリとパーティー GUI

- 通常メニュー系 GUI 表示中はプレイヤー下部インベントリ 36 スロットをダミーアイテムで埋め、GUI を閉じると inventory feature の表示状態へ復元する。
- 装備 GUI、ゴミ箱 GUI、スキル選択など、プレイヤー側アイテムを選択・移動する必要がある GUI は下部ダミー埋めの対象外とする。
- パーティー GUI は 54 スロット GUI とし、背景は他メニュー同様にダミーで埋める。
- パーティー GUI の戻るボタンは slot 49、パーティー離脱/解散ボタンは slot 51 に配置する。
- パーティーメンバーおよび招待元の表示アイコンは対象プレイヤーの `PLAYER_HEAD` として描画する。
## 追記（2026-06-01 プレイヤー一覧・パーティーGUI）

### PlayerListGui 表示
クラス: `PlayerListGui`
- 54 スロット GUI を使用し、一覧表示は `slot 0-44` に配置する。
- ページ送りは `slot 45` 前ページ、`slot 53` 次ページとする。
- 戻るは `slot 49` とし、呼び出し元に応じてメインメニューまたはパーティー GUI へ戻す。
- 一覧用途は `PLAYER_INFO` と `PARTY_INVITE` を持ち、用途ごとにタイトル・説明・クリック動作を切り替える。
- プレイヤーヘッド lore には名前、UUID、現在ワールド名、現在アカウント、レベルを表示する。ワールドがパス形式で管理されている場合でも、表示は末尾のワールド名のみとする。

### PlayerDetailGui 表示
クラス: `PlayerDetailGui`
- 54 スロット GUI を使用する。
- 上段中央 `slot 4` に対象プレイヤーヘッドを表示する。
- `slot 10` に基本情報、`slot 12` にアカウント情報、`slot 14` にクラス詳細、`slot 16` に所持 Gold / プレイ時間 / 現在位置を表示する。
- `slot 28-32` にリソース、基本能力値、攻撃、防御、ユーティリティの `StatusType.Category` 別ステータスを表示する。補正値があるステータスは合計値に加えて基礎値と補正値を表示する。
- `slot 34` に現在有効なバフ一覧を表示する。
- `slot 38` にトレード申請、`slot 42` にパーティー招待を配置する。対象が閲覧者自身の場合は利用不可表示とし、クリック時も拒否する。
- 戻るは `slot 49` とし、プレイヤー一覧またはパーティー GUI の呼び出し元へ戻る。閉じる操作はプレイヤーインベントリ側の BAG 制御列中央 `slot 26` を使用し、チェスト側に閉じる slot は置かない。

### PartyGui 改修
クラス: `PartyGui`
- リーダーは `slot 13` 固定表示とする。
- メンバーは `slot 31, 30, 32, 29, 33` の順で下段中央から埋める。
- 招待導線は `slot 15` に表示し、パーティー未所属プレイヤー一覧 GUI を開く。
- リーダーがメンバー頭をクリックした場合は `PartyMemberActionGui` を開く。

### PartyMemberActionGui 表示
クラス: `PartyMemberActionGui`
- 27 スロット GUI を使用する。
- 対象メンバー頭を中央表示し、`slot 11` にリーダー昇格、`slot 15` にキックを配置する。
- 戻るは `slot 22` とし、パーティー GUI へ戻る。

### BuffScreenView 表示補正
クラス: `BuffScreenView`
- バフ名表示はカラーコードを除去した表示名を使用する。
- メインメニューのバフアイコン lore には現在獲得中バフ名を複数行で表示する。

## 追記（2026-06-02）売却GUI/View

### 売却GUI表示
クラス名: MenuView
物理名: openSell
- `MenuScreen.SELL` の 54 スロット GUI を作成する。
- 0-44 スロットを売却対象領域とし、空枠は `GRAY_STAINED_GLASS_PANE` のプレースホルダーで表示する。
- 表示アイテム lore には `スタック: <amount>`、`売値: <unit> ゴールド`、`合計売値: <total> ゴールド` を追加する。
- 下段には前ページ、売却ガイド、戻る、確認、次ページを配置する。

### 売却確認GUI表示
クラス名: MenuView
物理名: openSellConfirm
- `MenuScreen.SELL_CONFIRM` の 27 スロット確認 GUI を作成する。
- 確定ボタンは売却実行、キャンセルボタンは売却GUIへ戻る。

## 追記（2026-06-02）ストレージGUI/View

### ストレージGUI表示
クラス名: MenuView
物理名: openStorage
- `MenuScreen.STORAGE` の 54 スロット GUI を作成する。
- 0-44 スロットをストレージ内容領域とし、空枠は `GRAY_STAINED_GLASS_PANE` のプレースホルダーで表示する。
- 初期表示は収納順の昇順とし、収納時に採番した `slotIndex` を使う。
- 下段には前ページ、カテゴリフィルタ、レア度フィルタ、並び順、昇順/降順、ガイド、次ページを配置する。戻るボタンは配置しない。
- 並び順は収納順、獲得順、レア度順、売値順を循環し、昇順/降順ボタンで方向を切り替える。
- ストレージ内アイテムの lore にはカテゴリ、レア度、売値、獲得日時を追加し、カテゴリは `ItemCategory.displayNameJa` の日本語名で表示する。数量は通常スタック上限で分割せず 1 entry の数量として表示する。通常アイテムは同一 itemId / itemCategory の entry へ統合され、装備・ルーンなど instanceId と紐づくアイテムは統合されない。
- 下部プレイヤーインベントリの表示中アイテムをクリックするとストレージへ収納し、ストレージ内容領域のアイテムをクリックすると所持インベントリへ取り出す。
- 収納・取り出し後は保存を即時実行せず、開いている間のストレージ表示情報をメモリ上で更新してから開いているページを再描画する。変更がある場合、保存はストレージ GUI を閉じる時にまとめて即時実行する。
- クリック数量は左クリック 1 個、右クリック 半分、Shift+左クリック 全部とする。

## 追記: 2026-06-13 クラスGUI

- `MenuView.openClass` は `ClassViewEntry` の表示用データを使ってクラス GUI を描画する。
- クラス GUI は外周を黒系ダミーアイテムで埋め、中央 7x4 の表示領域に 1次職を配置する。
- 1次職の配置は中央 7x4 の上段とし、`adventurer` を左上、`swordsman` / `hunter` / `mage` / `acolyte` を右上の 1 つ左から左方向へ並べる。
- クラス説明の lore では、ロール、種別、基本ステータス、レベル成長、初期習得スキル、レベル習得スキルを日本語名称で表示する。
- クラス GUI には戻るボタンを配置しない。
- 転職条件が不足しているクラスは「クリックで転職」ではなく転職できない旨を表示し、不足している職業条件の職業名を赤色で表示する。管理者権限ユーザーも表示は同じとするが、クリック時の転職実行は許可する。
- ステータス名と色は `StatusType` の表示名とカテゴリ色を使う。
- 2次職以降のアイコン配置は未使用スロット扱いとし、この時点では GUI に配置しない。
## 追記（2026-06-17）売却GUIレイアウト更新

### 売却GUI表示
- 売却GUIの下段は、左端 `slot 45` を前ページ、右端 `slot 53` を次ページに使う。
- `slot 49` は戻るボタンではなく売却確認への遷移アイコンを表示する。
- 売却GUIから確認GUIへの遷移では、GUI 切り替えのためだけの close sound は再生しない。
- `MenuView.renderSell` は既存の売却 GUI inventory に内容とナビゲーションを再描画する。アイテム移動・返却・ページングの反映では `openSell` で GUI を開き直さない。

### 売却確認GUI表示
- 売却確認GUIは 27 スロット構成を維持し、3行目中央 `slot 22` に戻る、3行目右下 `slot 26` に売却実行を配置する。
- 売却確認アイテムの説明欄には、売却対象を同一 ItemStack ごとに集約したうえで `・ <アイテム名> ×<個数> (<その行の合計Gold> ゴールド)` の箇条書きで表示する。中央領域には売却対象 ItemStack を直接表示しない。
- 売却確認GUIから売却GUIへ戻る遷移でも、GUI 切り替えのためだけの close sound は再生しない。
