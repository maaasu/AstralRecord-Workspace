# 08_3.02-サービス

## 1. state 参照

クラス名: `InventoryService`
物理名: `getInventories`, `getInventory`, `getEntries`, `getPersistence`, `getStateRegistry`

説明

inventory feature の読み取り系は API を直接呼ばず、`PlayerInventoryStateRegistry` 上の state を参照する。

処理手順:

1. `accountId` から `PlayerInventoryState` を取得する
2. inventory 一覧、inventory 単体、entry 一覧を state から返す
3. state 未登録時は空一覧または `null` を返す

## 2. 通常インベントリアイテム追加

クラス名: `InventoryService`  
物理名: `addItemToNormalInventory`

説明

取得アイテムを統合 `BAG` へ追加し、state を更新する。通貨だけは `CURRENCY` を使用する。BAG の `slotCapacity` に空きがなければ追加数0を返す。

処理手順:

1. item category から格納先 `InventoryType` を解決する
2. 対象 inventory が未存在なら `ensureInventory` で API に同期作成する
3. 装備・ルーンは必要に応じて instanceId を生成して新規 entry を追加する
4. 通常スタック可能アイテムは既存 entry へ加算するか、空き slot へ追加する
5. 追加成功時は `state.replaceEntries(...)` により dirty を立てる
6. 対象 inventory が表示中なら GUI を再描画する
7. 入りきらない場合は `PlayerMsgId.P_5241` を返す

## 3. ビルダー/管理者インベントリ保存・反映

クラス名: `InventoryService`  
物理名: `saveToolInventorySnapshot`, `applyToolInventoryToGui`

説明

`InventoryProfile.BUILDER` 用 `BAG` inventory の `metadataJson` に `builder` / `admin` キーごとの Bukkit 実インベントリスナップショットと `GameMode` を保持し、現在のアカウントモードに応じて個別に保存・反映する。

## 4. 表示インベントリ GUI 反映

クラス名: `InventoryService`  
物理名: `applyInventoriesToGui`, `applyInventoryToGui`, `getDisplayedInventoryType`, `clearGuiInventory`

説明

表示対象 inventory を Bukkit `PlayerInventory` の管理スロットへ反映する。

処理手順:

1. state 上の `displayedType` と対象 inventory を解決する
2. `InventoryItemStackResolver` で entry を `ItemStack` へ変換する
3. 管理対象スロットだけを書き換え、不要な再配置を避ける
4. GUI 反映対象 mode でない場合は何もしない

## 5. 装備ロードアウト取得・保証

クラス名: `InventoryService`  
物理名: `getEquipmentLoadouts`, `getActiveEquipmentLoadout`, `ensureActiveEquipmentLoadout`

説明

装備ロードアウトは state を参照する。active loadout 未存在時のみ API に同期作成して state へ取り込む。

## 6. 現在装備同期

クラス名: `InventoryService`  
物理名: `syncCurrentEquipmentState`

説明

Bukkit の現在装備状態を active loadout に同期する。

処理手順:

1. 頭 / 胴 / 脚 / 足 / オフハンドの `ItemStack` から装備インスタンス ID を読む
2. `PlayerInventoryState.upsertActiveLoadoutSlot(...)` で loadout slot を更新する
3. loadout 更新により dirty を立てる

## 7. 装備 GUI 保存

クラス名: `InventoryService`  
物理名: `saveEquipmentGui`

説明

装備 GUI 上の ItemStack を `EQUIP_SLOT` / `ACCESSORY_SLOT` inventory と active loadout へ反映する。

処理手順:

1. GUI スロットの ItemStack を inventory entry へ変換する
2. 防具・アクセサリ inventory を `state.replaceEntries(...)` で置き換える
3. active loadout slot も同時に更新する
4. Bukkit 現在装備状態を更新する

## 8. 装備・ホットバー・アクセサリのスナップショット保存

クラス名: `InventoryService`  
物理名: `saveEquipSlotSnapshot`, `saveHotbarSnapshot`, `saveAccessorySlotSnapshot`

説明

保存契機時に Bukkit 側の最新状態を state へ取り込む。いずれも `state.replaceEntries(...)` を呼ぶため dirty が立つ。

## 9. 装備・ホットバー・アクセサリの GUI 反映

クラス名: `InventoryService`  
物理名: `applyEquipSlotInventoryToGui`, `applyHotbarInventoryToGui`, `applyAccessorySlotInventoryToGui`

説明

対応 inventory の entry 一覧を Bukkit 側へ反映する。

## 10. ホットバー操作

クラス名: `InventoryService`  
物理名: `handleHotbarSlotClick`, `moveToHotbar`, `setHotbarShortcutMode`, `isHotbarShortcutMode`, `handleHotbarShortcutClick`, `hasHotbarEntry`

説明

ホットバー entry の選択と Bukkit スロット操作を扱う。
- ホットバーは Bukkit slot `0..8` の9枠すべてをアイテム割当へ使用する。
- カスタムGUI表示中もホットバー割当アイテムを維持し、閉じる操作は BAG 制御列中央の Bukkit slot `26` で扱う。
- `HotbarShortcutGuiHolder` 対象 GUI の閉じる表示と発火対象は BAG 制御列中央の Bukkit `slot 26` を正本とし、ホットバー9枠は通常表示を維持する。
- lore は `→ 通常[所持アイテム数]` / `→ 装備[所持アイテム数]` / `→ ルーン[所持アイテム数]` を表示し、選択中の行のみ太字にして末尾へ黄色の ` (選択中)` を付ける。
- 空スロット選択は「割当先選択」とみなし、呼び出し元ではクリック音を再生する。
- 表示中インベントリからホットバーへ割り当てる操作は移動扱いとし、HOTBAR entry を更新したあと元インベントリ entry を削除する。
- ホットバーへ割り当てた直後も BAG の `bagScrollRow` を維持する。
- ホットバーからアイテムを外した場合は BAG へ entry を返却する。

## 11. 装備移動とアクセサリ移動

クラス名: `InventoryService`  
物理名: `equipToSlot`, `moveDisplayedItemToEquipmentGui`, `moveToAccessorySlot`, `returnItemToOwnedInventory`, `compactInventoryEntries`

説明

表示中 inventory と装備 GUI / 防具スロット / アクセサリスロット間の移動を行う。

処理手順:

1. 移動元 entry または Bukkit `ItemStack` を解決する
2. 必要に応じて instanceId を保持したまま戻し先 inventory へ追加する
3. 移動後の inventory は `compactInventoryEntries(...)` で詰め直す
4. 変更結果を state に反映し、dirty を立てる
5. BAG の現在スクロール位置を維持して GUI を再描画する

## 12. metadata 更新

クラス名: `InventoryService`  
物理名: `updateMetadataAsync`

説明

inventory の `metadataJson` を state 上で更新する。  
即時 API 書き込みは行わず、次回 save 時に反映される。

## 13. 旧 flush API の互換フック

クラス名: `InventoryService`  
物理名: `flushPendingCoalescedWrites`, `awaitPendingWrites`

説明

旧 coalescer 実装との互換のために残しているフック。  
現行実装では `flushPendingCoalescedWrites` は no-op、`awaitPendingWrites` は `InventoryPersistence.awaitShutdown(...)` を呼ぶだけである。

即時整合性が必要な処理は `InventoryPersistence.saveNow(PlayerInventoryState)` を使う。

## 14. ストレージ操作

クラス名: `InventoryService`
物理名: `getStorageViewEntries`, `moveDisplayedItemToStorage`, `withdrawStorageEntry`

説明

ストレージ GUI 用に `STORAGE` inventory の表示一覧取得、表示中インベントリからの収納、ストレージから所持インベントリへの取り出しを行う。

処理手順:

1. `getStorageViewEntries` は state 上の `STORAGE` entry を `StorageViewEntry` に変換する
2. カテゴリフィルタとレア度フィルタが指定されている場合は、`ItemModel.category` / `ItemModel.rarity` で絞り込む
3. 並び順は収納順（`slotIndex`）、獲得順（`metadataJson.acquiredAt`）、レア度順、売値順から選択し、昇順・降順を適用する
4. `moveDisplayedItemToStorage` は表示中 inventory の Bukkit slot から entry を解決し、`instanceType` / `instanceId` を持たない通常アイテムは同一 itemId / itemCategory の既存 storage entry へ数量加算する。装備・ルーンなど instanceId と紐づくアイテムは統合せず新しい `STORAGE` entry を追加する
5. 収納時は `slotIndex` に次の収納順を採番し、収納元 entry の `createdAt` を `metadataJson.acquiredAt` に保存する
6. `withdrawStorageEntry` は対象 storage entry を ItemStack 化し、`returnItemToOwnedInventory` で通常の所持インベントリへ戻す
   通常スタック可能アイテムは 1 回の取り出し量を Minecraft の通常スタック上限までに制限し、残数がある限り storage entry は減算のみ行う
7. 収納・取り出し後は対象 entry の数量を減算または削除し、state の dirty を立てる
8. ストレージ GUI 経由の収納・取り出しでは操作ごとに `saveNow` を呼び、即時整合性を確保する

## 15. クリッククールタイム

クラス名: `InventoryClickGuard`  
物理名: `tryAcquire`

説明

accountId と click action 単位で 100ms のクールタイムを持ち、重複クリックを無視する。

## 補足

- 表示インベントリを Bukkit `PlayerInventory` へ反映する際、管理対象スロット `9`〜`35` の空き枠は `GRAY_STAINED_GLASS_PANE` で敷き詰める。
## 追記（2026-06-24）tool mode inventory snapshot 保存

- `BUILDER` / `ADMIN` の Bukkit inventory と `GameMode` は、`InventoryProfile.BUILDER` の `BAG` inventory の `metadataJson` に `builder` / `admin` key として保持する。
- `saveToolInventorySnapshot` は metadata 変更を `PlayerInventoryState` 上で dirty tracking し、`InventoryPersistence.save` は未保存 metadata に対して `InventoryRepository.updateMetadata` を呼び出す。
- autosave は Bukkit API 参照をメインスレッドで行うため、先に online の `BUILDER` / `ADMIN` player の tool snapshot を同期 capture し、その後 API 永続化を async task へ渡す。
