# 13_5.00-例外・ログ・運用

## 1. 共通エラー方針

| ステータス | 想定トリガー | 対応 |
|---|---|---|
| API スキル一覧取得失敗 | API 一時障害、通信失敗 | `E_5801` を記録し、built-in 定義のみで継続する |
| API スキル詳細取得失敗 | 個別スキル詳細の欠落、通信失敗 | 対象 `skillId` のみ読込失敗として除外する |
| 実装クラス未登録 | 実装漏れ、`implementationId` 不一致 | 対象スキルのみ除外し `W_5801` を記録する |
| `params` 入力不正 | 必須キー不足、型不一致 | 対象スキルのみ除外し `W_5801` を記録する |
| 起動前条件不足 | レベル不足、MP/EN 不足、クールダウン中 | 起動元へ対応するプレイヤーメッセージを表示する |
| 実行時例外 | 実装クラス内部例外 | `E_5802` を記録する |

## 2. ログ一覧

| 項目 | 内容 |
|---|---|
| ID | `LogId.I_5800` |
| レベル | INFORMATION |
| テンプレート | スキル定義を読み込みました: count=%s |
| 出力条件 | 定義一覧読込成功時 |
| 引数 | 読込件数 |

| 項目 | 内容 |
|---|---|
| ID | `LogId.E_5801` |
| レベル | ERROR |
| テンプレート | スキル一覧の取得に失敗しました |
| 出力条件 | API スキル一覧取得失敗時 |
| 引数 | - |

| 項目 | 内容 |
|---|---|
| ID | `LogId.W_5801` |
| レベル | WARNING |
| テンプレート | スキル定義を読み込めませんでした: skillId=%s, implementationId=%s, reason=%s |
| 出力条件 | 詳細取得失敗、実装クラス未登録、`params` 検証失敗時 |
| 引数 | スキル ID、`implementationId`、理由 |

| 項目 | 内容 |
|---|---|
| ID | `LogId.E_5802` |
| レベル | ERROR |
| テンプレート | スキル起動に失敗しました: skillId=%s, implementationId=%s |
| 出力条件 | `SkillExecutor.cast` で例外発生時 |
| 引数 | スキル ID、`implementationId` |

| 項目 | 内容 |
|---|---|
| ID | `LogId.W_5802` |
| レベル | WARNING |
| テンプレート | 実装クラスは既に登録されています: implementationId=%s |
| 出力条件 | `registerExecutor` に同一 `implementationId` を重複登録した時 |
| 引数 | `implementationId` |

## 3. 運用上の方針

- `implementationId` と実装クラスの対応は plugin 側の責務とする。
- `params` の詳細構造は plugin 側で定義し、API や filebase の共通スキーマには持ち込まない。
- built-in スキル定義は API マスタ未登録でも起動できるが、通常の executor 登録と同じ検証を通す。
- 通常攻撃用 built-in は `normal_attack_melee`、`normal_attack_bow`、`normal_attack_magic` を用意する。
- `resourceType` により消費対象を切り替える。
  - `MANA`: 現在 MP を参照・消費する。
  - `ENERGY`: 現在 EN を参照・消費する。
- reload 実行時は、前回の定義一覧を丸ごと更新する。
- `SkillService` が保持するクールダウンキャッシュは `起動元 UUID + skillId` 単位で管理する。
- プレイヤー離脱時は `PlayerQuitEvent` で対象 UUID のキャッシュを解放する。
- 期限切れクールダウンは参照時に掃除する。

## 4. TPS 低下防止チェックリスト

スキルおよびスキルツリーの実装では、メインスレッドで行う処理と非同期で行う処理を次の基準で分離する。

| 観点 | 同期処理で行うもの | 非同期処理で行うもの |
|---|---|---|
| Bukkit / Paper API | `Player`、`Inventory`、`World`、サウンド、HUD、パーティクルなどサーバ状態へ触れる処理 | 原則禁止。必要な場合は結果だけをメインスレッドへ戻す |
| API / DB / ファイル IO | クリックイベント、tick、HUD描画、戦闘処理から直接呼ばない | `HttpClient#send`、DBアクセス、永続化ファイル読み書き |
| キャッシュ | 読み取り・即時のローカル状態更新 | APIからの初回ロード、dirty状態の保存 |
| クリック操作 | キャッシュ済み状態の判定、CP/PP/Goldの即時反映、dirtyマーキング | APIへの保存リクエスト送信 |

チェック項目:

- `PlayerInteractEvent`、`InventoryClickEvent`、tick タスク内で `HttpClient#send` や repository の同期 API 通信を呼ばない。
- スキルツリー進行状態はアカウント単位でメモリキャッシュし、未ロード時は操作を保留してロード中メッセージを返す。
- 初回ログイン時はプレイヤーデータロード完了扱いにする前に、スキルツリー進行状態を必ず読み切る。読み込み中はプレイヤーの視点・移動・ジャンプ・基本操作をロックし、title でロード中であることを表示する。ステータス初回計算はスキルツリー進行状態をキャッシュへ反映した後にだけ実行し、未ロード状態を空のスキルツリーとして扱わない。
- ノード解放・解除はローカルキャッシュを更新して dirty とし、API には定期タスクでスナップショットを非同期一括保存する。
- CP/PP は永続化せず、クラスレベル・プレイヤーレベルから算出する。マスタ変更でポイント不足になった解放済みノードは、ID値の高い順に効果停止中として表示し、スキル・ステータス反映から除外する。
- API 保存失敗時は dirty を戻して次回保存へ回し、クリック処理を通信待ちにしない。
- Bukkit API が必要な表示更新、ステータス再計算、パッシブスキル再同期はメインスレッドへ戻して実行する。
- 新規実装時に「同期でよいか迷う処理」は、Bukkit API 操作以外を非同期候補として扱い、同期化する理由を設計書またはコードコメントで明示する。

## 5. SkillTree TPS チェックリスト

- スキルツリー操作中でもプレイヤーのホットバーは切り替えない。通常のインベントリ状態を維持する。
- 注視ノードを毎 tick 監視しない。対象解決はプレイヤー操作イベント起点で行う。
- ノードの表示テキストと表示アイテムは、マスタデータ読込・再読込時にキャッシュする。
- スキルツリー表示設定はプレイヤー単位かつ拡張可能に保つ。表示距離とエッジ表示は service 層のオプションとして持ち、コマンドと将来の GUI/NPC 導線で共通状態を共有する。
- クライアント描画負荷を抑えるため、ラベル表示は距離別の段階表示にする。近距離は全文表示、中距離は簡略表示、遠距離は非表示とする。
- スキルツリー進行状態の保存は debounce でまとめる。解放・解除のたびに repository save を呼ばない。
- スキルツリー構造とプレイヤー別ノード表示は dirty 駆動で更新する。変更があった閲覧者と座標だけを再描画する。
- ノード変化時の解放済みスキル所持状態は差分で整合する。小さな変更ごとに全再構築しない。
- API・DB・ファイル IO は必ず非同期で扱う。メインスレッドは Bukkit/Paper の状態参照、表示更新、短時間のメモリ計算に限定する。
- 解放ノードから導出できる重い再計算はプレイヤー単位でキャッシュし、ノード状態変化時だけ無効化する。
- `skill_tree` ワールド滞在中のプレイヤーからは、他プレイヤーを AstralRecord 側で非表示にする。対象プレイヤーが `skill_tree` 以外へ移動した時点で、この可視制御は解除する。
