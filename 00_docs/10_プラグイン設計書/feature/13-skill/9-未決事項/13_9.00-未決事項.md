# 13_9.00 未決事項

## 1. スキル実装の登録方式

- DI ベースに寄せるか、明示登録を継続するかは未決

## 2. `params` の設計運用

- 各 `implementationId` ごとの `params` 契約をどこまで docs 化するかは未決

## 3. reload 戦略

- `skill reload` 時に発動中パッシブをどこまで即時再構築するかは運用を見て調整する

## 4. プレイヤーレベル判定

- `requiredLevel` を class level / account level / 専用スキル熟練度のどれで判定するかは未決

## 5. 所持スキル一覧 GUI

- スキル設定 GUI とは別に、プレイヤーの所持スキルだけを一覧表示する専用 GUI を今後追加する
- この GUI では、バインド不要パッシブを含む全所持スキルを閲覧できるようにする
- スキルツリー由来 / 装備由来 / ルーン由来 / セット効果由来など、所持元の表示方法は別途設計する
