# 13_9.00-未決事項

## 1. 発動入力経路

- プレイヤーがスキルを発動する UI をコマンド、GUI、アイテム使用のどれに寄せるかは未確定。

## 2. 実行クラス登録方式

- DI / 手動登録 / package scan のどれを採用するかは未確定。

## 3. `params` 契約の保管場所

- 実装別 `params` 契約を code comments、KDoc、別設計書のどこに置くかは未確定。

## 4. reload コマンド

- 運用コマンドとして `skill reload` を持つか、起動時同期のみに限定するかは未確定。

## 5. Mob 発動者実装

- `SkillCaster` 抽象は導入済みだが、Mob 用 `SkillCaster` 実装と AI からの呼び出し導線は未確定。

## 6. プレイヤーレベル概念

- スキル要求レベル `requiredLevel` の比較に用いる「プレイヤーレベル」をクラスレベル / アカウントレベル / スキル習熟度のどれに紐付けるかは未確定。
- 上記が決まるまでの暫定挙動として、`PlayerSkillCaster.level()` は `Integer.MAX_VALUE` を返し、`SkillService.canCast` での `requiredLevel` 検証を実質スキップする。バランス検証期間中はこの仕様で固定し、概念確定後に [[13_1.00-モデル定義]] と本項を同時更新する。
