# 01_9.00-未決事項

## 1. プレイヤー向けメッセージ管理の統一

現状の `UserPermissionCommand` はメッセージ文字列を直書きしている。  
プロジェクト方針（`MsgId` / `player.properties` 管理）へ統一するかを決定する必要がある。

## 2. permission 変更の監査粒度

`permission` 変更操作について、監査ログに次を含めるか未確定。

- 実行者 UUID
- 対象 UUID
- 変更前 permission
- 変更後 permission
- 実行経路（コンソール / プレイヤー）
