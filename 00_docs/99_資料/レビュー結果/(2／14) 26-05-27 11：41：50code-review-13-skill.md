## レビュー情報
- skill 名: `code-review`
- 対象パス: `E:\AstralRecord-Workspace\10_plugin\AstralRecord\src\main\java\io\github\maaasu\astralRecord\feature\skill`
- 指摘修正数 / 指摘数: `2 / 14`
- 完了状態: `未完了`

## 各指摘の修正状態
- `AR-CODE-001`: `未対応`
- `AR-CODE-002`: `修正済み`（docs 側を実装に合わせて update。13_1.00 / 13_3.02 反映済み）
- `AR-CODE-003`: `未対応`
- `AR-CODE-004`: `docs 修正済み`（13_5.00 運用方針に反映。code 側で `PlayerQuitEvent` listener 追加待ち）
- `AR-CODE-005`: `修正済み`（13_9.00 #6 として暫定仕様を正式記載）
- `AR-CODE-006`: `未対応`
- `AR-CODE-007`: `未対応`
- `AR-CODE-008`: `未対応`
- `AR-CODE-009`: `docs 修正済み`（13_1.00 #9 識別子規約を追加。code 側で正規化方針の統一待ち）
- `AR-CODE-010`: `未対応`
- `AR-CODE-011`: `未対応`
- `AR-CODE-012`: `未対応`
- `AR-CODE-013`: `未対応`
- `AR-CODE-014`: `未対応`

## 指摘一覧

### AR-CODE-001 [高] LogId.E_5803 と「既存定義が空時の skill feature 無効化」が未実装
- 種別: `仕様不整合`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:96`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/infrastructure/logging/LogId.java:773` / `10_plugin/AstralRecord/src/main/resources/logger.properties:228`
- 根拠: `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.02-サービス.md` のスキル定義同期 #7 と `00_docs/10_プラグイン設計書/feature/13-skill/5-例外・ログ・運用/13_5.00-例外・ログ・運用.md` の `LogId.E_5803` 定義（テンプレート: `スキル定義の取得に失敗し、既存キャッシュも空のため skill feature を無効化しました: reason=%s`）。
- 問題: `reloadDefinitions` の `findAll` 失敗ハンドラ（`SkillService.java:100-103`）は `LogId.E_5801` を出して既存件数を返すだけで、設計が要求する「既存定義が空 → `E_5803` 出力 + skill feature 無効化」の分岐が存在しない。`LogId.java` にも `logger.properties` にも `E_5803` 自体が定義されていない。
- 影響: API 一覧取得失敗かつキャッシュが空の状態でも警告級ログ（`E_5801`）が一回出るだけで feature 無効化が発火せず、後続の `castSkill` が常に `P_5803` を返す挙動になる。運用時の障害切り分けが困難になり、設計で意図した「初期化失敗を明示する」契約が守られない。
- 修正方針:
  1. `LogId.java` の `// region /feature/skill/` 内に `E_5803` を追加し、`logger.properties` にも `E_5803=スキル定義の取得に失敗し、既存キャッシュも空のため skill feature を無効化しました: reason=%s` を追記。
  2. `reloadDefinitions` の `catch` で `registry.definitionCount() == 0` の場合に `Logger.log(LogId.E_5803, e.getMessage())` を出力し、feature 無効化フラグを立てる（`AstralRecord` 側からの利用も含めて公開する）。`SkillService` に `boolean isDisabled()` を持たせ、`castSkill` 側で early return する設計を提案。
- 修正可否: `要確認`
- 確信度: `高`

### AR-CODE-002 [高] SkillCaster インターフェースが設計と乖離（メソッド名・欠落・追加）
- 種別: `仕様不整合`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/SkillCaster.java:16`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/PlayerSkillCaster.java:39` / `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:252`
- 根拠: `00_docs/10_プラグイン設計書/feature/13-skill/1-モデル定義/13_1.00-モデル定義.md` の 4 章「スキル発動者」項目表。
- 問題: 設計は `uuid()` / `cooldownRemainingTicks(skillId)` / `startCooldown(skillId, ticks)` / `location()` / `statusSnapshot()` を契約として規定するが、実装は以下のように乖離している。
  - `uuid()` を `casterId()` に改名。
  - `cooldownRemainingTicks(skillId)` と `startCooldown(skillId, ticks)` を契約から削除し、`SkillService.cooldownExpiryByCaster` に集中保持。
  - `location()` が欠落。`castSkill` 呼び出し側に `castLocation` 引数を要求する形に変更。
  - 設計外の `notify(messageId, args...)` を追加。
- 影響: 設計通りに `SkillCaster` を実装しようとする後続コード（Mob 用 caster 等）が契約のどれを満たせばよいか判断できない。docs と実装の双方を更新しないと、後続実装者は古い設計を信じて壊れたコードを書く。また `Object#notify()` をシャドウする命名は誤呼び出しを誘発する（AR-CODE-010 と関連）。
- 修正方針: 設計と実装のどちらを正本にするか確定させてから片側を合わせる。実装側を維持するなら `13_1.00-モデル定義.md` を編集（`$astralrecord-docs-fix` で取り扱う）。設計側を維持するなら caster へ cooldown 状態と `location()` を戻し、`SkillService` のマップを撤去する大規模変更が必要。
- 修正可否: `設計判断待ち`
- 確信度: `高`

### AR-CODE-003 [中] `replaceDefinitions` の `D_5801` と `reloadDefinitions` の `I_5800` が同一イベントを二重ログしている
- 種別: `ドキュメント不整合`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/registry/SkillRegistry.java:75`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:142` / `10_plugin/AstralRecord/src/main/resources/logger.properties:236` / `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/infrastructure/logging/LogId.java:805`
- 根拠: `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.06-レジストリ・キャッシュ.md`「スキル定義反映」と `13_5.00-例外・ログ・運用.md` のログ一覧。設計上「定義一括反映成功時」のログは `LogId.I_5800` のみ。
- 問題: `SkillRegistry.replaceDefinitions` が設計に存在しない `LogId.D_5801`（`スキル定義をレジストリに反映しました: count=%d`）を出力し、続いて `SkillService.reloadDefinitions` が `LogId.I_5800` を出力するため、`reloadDefinitions` 1 回につき同義のログが 2 行出力される。
- 影響: 監査ログのノイズ増加。設計外の `D_5801` を読み手が「別イベント」と誤読する余地が残る。`logger.properties` と `LogId.java` にも未承認の ID が混入している。
- 修正方針: 設計に合わせ `replaceDefinitions` 側の `Logger.log(LogId.D_5801, ...)` 呼び出しと `LogId.D_5801` / `logger.properties:236` を削除する。`I_5800` は `SkillService` 側のみで残す（設計の出力条件「定義一括反映成功時」に整合）。
- 修正可否: `自動修正可`
- 確信度: `高`

### AR-CODE-004 [中] cooldown 保持マップにプレイヤー切断時の掃除がなく長期稼働でメモリリークする
- 種別: `バグ/アルゴリズム`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:47`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:252` / `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:267`
- 根拠: 一般原則（Bukkit/Paper では `PlayerQuitEvent` でセッションスコープのキャッシュを解放するのが定石）。設計 `13_1.00` の caster 契約から外して `SkillService` 集中管理に寄せた以上、その寿命管理は本サービスの責務。
- 問題: `cooldownExpiryByCaster` は `Map<UUID, Map<String, Long>>` で発動者ごとに cooldown 終了時刻を保持するが、`PlayerQuitEvent` などのリスナがどこにもなく、ログアウト後もエントリが残り続ける。`isOnCooldown` で expire 済みエントリは内側 Map から個別削除されるが、内側 Map 自体や `UUID` キーは永遠に残る。
- 影響: 長時間稼働サーバで `UUID → Map` エントリが累積し、徐々にメモリを圧迫する。さらに同一 `skillId` を発動しないキャラが多いとほぼリーク同然の挙動になる。
- 修正方針:
  1. `Listener` を新設し `PlayerQuitEvent` で `cooldownExpiryByCaster.remove(uuid)` を呼ぶ。
  2. もしくは `isOnCooldown` 内で内側 Map が空になった場合に `cooldownExpiryByCaster.remove(uuid)` も実行する。
  3. 長期的には設計通り caster 側に cooldown 管理を戻す（AR-CODE-002 と統合検討）。
- 修正可否: `要確認`
- 確信度: `中`

### AR-CODE-005 [中] PlayerSkillCaster.level() が常に Integer.MAX_VALUE を返し requiredLevel 検証を無効化している
- 種別: `仕様不整合`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/PlayerSkillCaster.java:46`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:155`
- 根拠: `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.02-サービス.md` 実行可否判定 #1「`requiredLevel` を発動者レベルと比較する」、および `13_9.00-未決事項.md`（`SkillCaster` 抽象は存在するが「レベル概念検証を常時スキップする」とは明記されていない）。
- 問題: `level()` が常に `Integer.MAX_VALUE` を返す実装のため、`SkillService.canCast` の `caster.level() < skill.getRequiredLevel()` が常に偽となり、要求レベル未達による失敗（`PlayerMsgId.P_5800`）が一度も発火しない。コメントは未確定事項を理由に挙げているが、設計の正規仕様としては承認されていない。
- 影響: 要求レベルが本来 `120` のスキルを `Lv.1` でも発動できる。MMO バランスに対するクリティカルな影響だが、設計と実装の双方で「未確定」と認知されているため、設計判断確定までは本指摘の修正は保留扱いになる。
- 修正方針:
  1. `13_9.00-未決事項.md` または `13_1.00-モデル定義.md` に「Lv 概念未確定までは `level()=Integer.MAX_VALUE` を返し検証スキップ」を正規仕様として追記（`$astralrecord-docs-fix`）。
  2. もしくは `AstPlayer` 側に暫定 Lv API を追加して反映。設計と実装のどちらを正にするかは設計判断待ち。
- 修正可否: `設計判断待ち`
- 確信度: `高`

### AR-CODE-006 [中] reloadDefinitions が validateParams の SkillParameterException 以外の RuntimeException を捕捉しない
- 種別: `バグ/アルゴリズム`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:131`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/13-skill/5-例外・ログ・運用/13_5.00-例外・ログ・運用.md`
- 根拠: 設計 13_5.00「`params` 型不正 / 定義単位で無効化」「起動不能より部分無効化を優先し、不正定義はスキル単位で隔離する」。
- 問題: `executor.validateParams(definition)` を `catch (SkillParameterException e)` のみで捕捉している。実装側で `IllegalArgumentException` 等の汎用例外が漏れた場合、`reloadDefinitions` 全体が落ちて反映処理が中断する。
- 影響: 1 つの実装クラスのバグで全スキル定義のロードが停止し、設計の「定義単位で無効化」「全体停止は避ける」原則が破れる。
- 修正方針: `catch (SkillParameterException e)` を `catch (RuntimeException e)` に広げ、`W_5801` に reason として `e.getClass().getSimpleName() + ": " + e.getMessage()` を渡してスキップ扱いとする。`SkillParameterException` 専用ハンドラを残したい場合は二段 catch にする。
- 修正可否: `自動修正可`
- 確信度: `高`

### AR-CODE-007 [中] SkillRepository.toDefinition が null 許容項目を Plugin 側既定値で補完している
- 種別: `仕様不整合`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/repository/SkillRepository.kt:104`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/SkillDefinition.kt:29`
- 根拠: `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.04-リポジトリ.md` API レスポンス変換 #4「`null` 許容項目は Plugin 側既定値へ補完せず、原則 API 値を尊重する」。
- 問題: `toDefinition` が `cooldownTicks ?: 0L`、`manaCost ?: 0.0`、`castTimeTicks ?: 0L`、`requiredLevel ?: 1` と Plugin 側既定値を埋めている。`SkillDefinition` 側のフィールド型も `Long`/`Double`/`Int`（non-null）になっており、API 値を尊重できる構造になっていない。
- 影響: API レスポンスが `null` の場合に「未指定」と「0/1 と明示」が区別できず、設計が想定した「API 値を尊重」の検証ロジック（例えば `requiredLevel` 未指定なら定義無効扱いにする等）を Plugin 側に書けなくなる。
- 修正方針: 設計を厳守するなら `SkillDefinition` のこれら数値項目を nullable 化し、`toDefinition` の `?:` フォールバックを除去する。`SkillService` 側で null をどう扱うかは設計に追記して合意（`13_3.04` か `13_5.00` に補足）。アプリ要件上「API 必須項目として保証」する方針なら、設計側に「これらは API 必須」と明記し、null 時は `findById` で例外化する方がフォールバックより正しい。
- 修正可否: `設計判断待ち`
- 確信度: `中`

### AR-CODE-008 [中] PlayerSkillCaster.consumeMana が下限クランプを行わず MP が負値になり得る
- 種別: `バグ/アルゴリズム`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/PlayerSkillCaster.java:64`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/SkillCaster.java:55`
- 根拠: `SkillCaster.java:53` の JavaDoc が「上限・下限のクランプは呼び出し先の責務」と明示している。本 PlayerSkillCaster がその呼び出し先である以上、クランプは本実装の責務。
- 問題: `consumeMana(amount)` は `snapshot.getCurrentMp() - amount` をそのまま `withCurrentValues` に渡す。`canCast` を経由しないシステム発動（trigger=SYSTEM 等）や、`getStatusSnapshot()` のスナップショットが古く実 MP が `amount` 未満になった場合に `currentMp` が負値になる。
- 影響: MP 表示や MP 回復ロジックが負値を起点に走り、UI 異常や回復不能状態を引き起こす可能性がある。
- 修正方針: `consumeMana` 内で `Math.max(0.0, snapshot.getCurrentMp() - amount)` にクランプする。上限側（最大 MP）はステータス側で別途扱うため不要。
- 修正可否: `自動修正可`
- 確信度: `高`

### AR-CODE-009 [低] cooldown キーは小文字化されるが definitionsById は元の casing を維持しており参照経路が不一致
- 種別: `可読性/保守性`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:298`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/registry/SkillRegistry.java:85`
- 根拠: 一般原則（同じ識別子に対する正規化方針はモジュール内で統一する）。
- 問題: `SkillService.normalize` が cooldown マップキーのみ `trim() + toLowerCase(Locale.ROOT)` する一方、`SkillRegistry.definitionsById` は API から取得したそのままの `skillId` をキーにする。`castSkill(caster, "FIRE_BALL_A", ...)` のような呼び出しでは、定義は見つかるが cooldown 判定は別キーで管理される。
- 影響: 現状 skill ID は filebase 上常に小文字想定なので顕在化していないが、将来大文字混じり ID を許容したり、定義側で trim 漏れがあった場合に「クールダウン中なのに次回も発動できる」「定義はあるのに毎回未定義扱い」のような不整合が起きる。
- 修正方針: `SkillService.castSkill` 入口で `skillId` を一度 normalize し、その後 `registry.getDefinition` も `cooldown` も同じ正規化値で扱う。もしくは normalize を廃止し、両者とも入力値そのままで扱う（API 仕様で casing 統一を担保する）。
- 修正可否: `要確認`
- 確信度: `中`

### AR-CODE-010 [低] SkillCaster.notify は Object.notify とシグネチャ衝突しやすく誤呼び出しを誘発する
- 種別: `可読性/保守性`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/SkillCaster.java:64`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/PlayerSkillCaster.java:74` / `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:284`
- 根拠: 一般原則（Java 標準の `Object.notify()` をシャドウする命名は IDE 警告の対象であり、誤って引数なしで呼ぶと `IllegalMonitorStateException` を踏む）。
- 問題: 抽象メソッドが `notify(PlayerMsgId, Object...)`。サブクラスや利用側で誤って `caster.notify()` を呼ぶと、Java は `Object.notify()` に解決して `IllegalMonitorStateException` を投げる。
- 影響: 直近の事故は少ないが、将来の保守者が踏みやすい罠。
- 修正方針: `notify` を `notifyMessage` または `sendMessage` に改名する。`SkillService.notifyIfFailed` 内の呼び出しも合わせて改名する。
- 修正可否: `自動修正可`
- 確信度: `中`

### AR-CODE-011 [低] PlayerSkillCaster がどこからも new されておらず、プレイヤー発動経路が未配線
- 種別: `死コード/重複`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/PlayerSkillCaster.java:16`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/AstralRecord.java:230`
- 根拠: 設計 `13_4.00-統合フロー.md` 2 章「プレイヤー発動」と `13_9.00-未決事項.md` #1「発動入力経路未確定」。
- 問題: 全コードベースを grep しても `new PlayerSkillCaster(` は存在せず、`SkillService.castSkill` の呼び出し元も存在しない。`AstralRecord.java:230` は `new SkillService()` をするだけで `registerExecutor` も発動経路も無い。
- 影響: 起動時に `reloadDefinitions` は走るが、executor が 1 件も登録されていないため全スキル定義が `W_5801`（実行クラス未登録）で隔離されレジストリは空のままになる。skill feature が現状「ロード処理だけ動いて発動不能」な状態であることを開発者が認知しているか確認したい。
- 修正方針: `13_9.00-未決事項.md` の発動入力経路（コマンド/GUI/アイテム）確定後に command/event ハンドラを追加し、そこで `PlayerSkillCaster` を生成→`skillService.castSkill` を呼ぶ。確定までは現状維持で良いが、レビュー結果として明記する。
- 修正可否: `設計判断待ち`
- 確信度: `高`

### AR-CODE-012 [情報] SkillParamReader と SkillCastTrigger の各値は現状利用元なし
- 種別: `死コード/重複`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/SkillParamReader.java:14`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/SkillCastTrigger.java:10`
- 根拠: 一般原則（公開 API は最低 1 か所から呼ばれる前提）。一方、設計 `13_1.00` および `13_3.09` で「実装側が使用する」と宣言された SDK である。
- 問題: `SkillParamReader` は `SkillExecutor` 実装が利用する想定だが、AR-CODE-011 の通り executor 実装ゼロのため呼び出し元なし。`SkillCastTrigger` の `PLAYER_COMMAND`/`AUTO_ATTACK`/`MOB_AI`/`SYSTEM` も `castSkill` の引数として渡されるが渡す側が居ない。
- 影響: 動作上の問題は無いが、未使用 API としてダウンストリームの dead code 警告対象になる。設計と整合した「ハンドラ未配線」の影響であり、AR-CODE-011 の解消に追従する。
- 修正方針: AR-CODE-011 と一括で扱う。executor サンプル実装を追加するタイミングで参照が増える前提でこのまま残してよい。指摘としては「未使用」の事実を記録するに留める。
- 修正可否: `設計判断待ち`
- 確信度: `中`

### AR-CODE-013 [低] SkillCastContext のコンパクトコンストラクタが @NotNull 契約と矛盾する null 補完を行う
- 種別: `可読性/保守性`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/model/SkillCastContext.java:42`
- 関連箇所: `なし`
- 根拠: 一般原則（@NotNull で宣言した引数は呼び出し側責務として null を渡さない契約。コンストラクタ内で null フォールバックを書くと契約が曖昧になる）。
- 問題: `targets` を `@NotNull` で宣言しているのに、コンパクトコンストラクタが `targets == null ? List.of() : List.copyOf(targets)` と null を許容している。@NotNull アノテーションと挙動が矛盾。
- 影響: 静的解析（IntelliJ inspection 等）の警告が出る。呼び出し側が「null を渡してよい」と誤解する。
- 修正方針: null 補完を撤去し `targets = List.copyOf(targets);` だけ残す。null を許容したい場合は @Nullable に変更する（こちらは設計判断が必要）。
- 修正可否: `自動修正可`
- 確信度: `中`

### AR-CODE-014 [低] 同一 HTTP 失敗で E_5801 がリポジトリ層とサービス層から二重出力される
- 種別: `ドキュメント不整合`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/repository/SkillRepository.kt:39`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/service/SkillService.java:100`
- 根拠: `00_docs/10_プラグイン設計書/feature/13-skill/5-例外・ログ・運用/13_5.00-例外・ログ・運用.md` のログ一覧。`E_5801` の出力条件は「`reloadDefinitions` で API 一覧取得失敗」と単発条件で示されている。
- 問題: `SkillRepository.findAll` は HTTP 非 200 時に `Logger.log(LogId.E_5801, ...)` を出してから `IOException` を投げる。それを受けた `SkillService.reloadDefinitions:101` も `Logger.log(LogId.E_5801, e, "reloadDefinitions")` を出力する。結果として 1 回の HTTP 失敗で `E_5801` が 2 件並ぶ。
- 影響: ログのノイズ増加とアラート重複。エラー件数を監視しているとカウンタが 2 倍になる。
- 修正方針: リポジトリ層は通信エラーを例外として伝播するだけにし、設計上の `E_5801` 出力はサービス層に集約する。`SkillRepository.findAll` 内の `Logger.log(LogId.E_5801, ...)` を削除（または `D_5800` 等の参考ログにダウングレード）して、サービス層の 1 か所のみで出す。
- 修正可否: `自動修正可`
- 確信度: `高`

## 未確認/質問

### Q-CODE-001
- 関連指摘: `AR-CODE-002`
- 確認事項: `SkillCaster` インターフェースの正本は設計 `13_1.00` と現実装のどちらか。`notify` を契約に残すか、`location()` / cooldown 系メソッドを caster に戻すか、設計者の意図を確認したい。
- 判断が必要な理由: cooldown 管理の所在（caster or service）はメモリリーク対応（AR-CODE-004）や Mob caster 実装方針にも波及するため、レビューだけでは確定不能。

### Q-CODE-002
- 関連指摘: `AR-CODE-005` / `AR-CODE-011`
- 確認事項: `Lv 概念は未確定」「発動入力経路は未確定」を 13_9.00 のとおり保留扱いで継続するか、本サイクルで暫定実装まで踏み込むか。
- 判断が必要な理由: 暫定実装に踏み込むなら `level()` の値元と、コマンド/GUI/アイテムどれを起点にするかの選択が必要。

### Q-CODE-003
- 関連指摘: `AR-CODE-007`
- 確認事項: API レスポンスにおける `cooldownTicks`/`manaCost`/`castTimeTicks`/`requiredLevel` を「必須」か「nullable」のどちらと定義するか。
- 判断が必要な理由: API 側 (`00_docs/20_API設計書/feature/11-skill`) 仕様を確認しないと、Plugin 側で nullable に直すか API 側に必須化を要請するかが決められない。

### Q-CODE-004
- 関連指摘: `AR-CODE-009`
- 確認事項: skill ID の casing 規約。filebase / API / Plugin で常時小文字を強制してよいか、あるいは casing-sensitive で扱うか。
- 判断が必要な理由: 正規化方針が未定だと cooldown / definition の参照経路を揃えられない。

## 修正スキル入力サマリ
- 自動修正候補: `AR-CODE-003`, `AR-CODE-006`, `AR-CODE-008`, `AR-CODE-010`, `AR-CODE-013`, `AR-CODE-014`
- 要確認: `AR-CODE-001`, `AR-CODE-002`, `AR-CODE-004`, `AR-CODE-005`, `AR-CODE-007`, `AR-CODE-009`, `AR-CODE-011`, `AR-CODE-012`, `Q-CODE-001`, `Q-CODE-002`, `Q-CODE-003`, `Q-CODE-004`
- 推奨修正順: `AR-CODE-014` -> `AR-CODE-003` -> `AR-CODE-006` -> `AR-CODE-013` -> `AR-CODE-008` -> `AR-CODE-010` -> `AR-CODE-001` -> `AR-CODE-007` -> `AR-CODE-009` -> `AR-CODE-002` -> `AR-CODE-004` -> `AR-CODE-005` -> `AR-CODE-011` -> `AR-CODE-012`
- 対象範囲: `E:\AstralRecord-Workspace\10_plugin\AstralRecord\src\main\java\io\github\maaasu\astralRecord\feature\skill`

## 確認した範囲
- 対象プロジェクト: AstralRecord Plugin (`10_plugin/AstralRecord`)
- 読んだ設計書/ルール:
  - `E:\AstralRecord-Workspace\AGENTS.md`
  - `E:\AstralRecord-Workspace\PLUGIN_GUIDE.md`
  - `E:\AstralRecord-Workspace\.codex\skills\astralrecord-code\references\plugin-code.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/13_README.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/0-概要/13_0.00-概要.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/1-モデル定義/13_1.00-モデル定義.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/2-ユースケース/13_2.00-ユースケース.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.00-索引.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.02-サービス.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.04-リポジトリ.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.06-レジストリ・キャッシュ.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/3-メソッド仕様/13_3.09-モデル操作.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/4-統合フロー/13_4.00-統合フロー.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/5-例外・ログ・運用/13_5.00-例外・ログ・運用.md`
  - `00_docs/10_プラグイン設計書/feature/13-skill/9-未決事項/13_9.00-未決事項.md`
- 読んだソース:
  - `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/skill/**` 全 13 ファイル
  - `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/AstralRecord.java`（skill 関連箇所のみ）
  - `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/infrastructure/logging/LogId.java`（5800 系のみ）
  - `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/player/PlayerMsgId.java`（5800 系のみ）
  - `10_plugin/AstralRecord/src/main/resources/logger.properties`（5800 系のみ）
- 実行した検査: なし（本 skill はソース編集禁止のため、ビルド・テスト・静的解析は未実行）

## 対象外
- API 側 (`20_api/AstralRecordApi`) のスキル API 実装 — 本レビューは Plugin 配下が対象。API↔Plugin 契約に関わる質問は `Q-CODE-003` として記録。
- filebase 側 YAML (`40_filebase/30.features.skill`) の整合 — Plugin スキーマと別軸のため対象外。必要なら別途 `$astralrecord-code-review` または `$astralrecord-docs-review` で扱う。
- `infrastructure/util/ApiRequestUtil` の実装詳細 — skill feature 直下ではないため対象外。
