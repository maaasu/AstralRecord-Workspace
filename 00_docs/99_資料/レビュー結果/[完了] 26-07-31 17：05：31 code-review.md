# AstralRecord レビュー記録
- フォーマット版: `1`
- 使用スキル: `code-review`
- 対象パス: `.`
- 作成日時: `2026-07-31T17:05:31+09:00`
- 完了状態: `完了`
- 指摘修正数 / 指摘数: `33 / 33`

## 指摘一覧

### AR-CODE-001 [高] 設計済みの即時保存キュー入口テストを削除している
- 種別: `テスト不足`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/inventory/service/InventoryServiceSaveQueueTest.java`
- 関連箇所: `00_docs/10_Plugin設計書/feature/08-inventory/3-メソッド仕様/08_3-サービス.md:160`
- 根拠: `08_3-サービス.md` の「## 12. 即時保存」は、`InventoryService.saveNow` が要求を `InventorySaveCoordinator` のアカウント別保存キューへ登録することを line 167 で明示している。削除前のテストは、戻り値の future が coordinator の future と同一であること、coordinator を呼ぶこと、`InventoryPersistence` を直接呼ばないことを唯一直接検証していた。現存する `InventorySaveCoordinatorTest` は coordinator 内部だけを対象とし、`rg -n 'saveCoordinator\.saveNow|saveNow\(accountId\)' 10_plugin/AstralRecord/src/test` でも入口配線を検証する代替テストは見つからない。
- 問題: 設計外の内部詳細ではなく、採用済み設計に明記された保存順序の入口契約まで低価値テストとして削除している。
- 影響: 将来 `InventoryService.saveNow` が coordinator を迂回して直接永続化する退行が入ってもテストが成功し、同一 account lane の直列化・reconciliation 境界・即時整合性を壊してデータ競合を招き得る。
- 修正方針: 削除したテストを復元し、各 test method の Javadoc を `08_3-サービス.md > ## 12. 即時保存` と「coordinator の future を返し persistence を直接呼ばない」契約へ正確に対応させる。
- 修正対象候補: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/inventory/service/InventoryServiceSaveQueueTest.java`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-002 [中] 耐久値 pending のテストが契約のない章を参照している
- 種別: `ドキュメント不整合`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/inventory/state/InventoryPersistenceTest.java:19`
- 関連箇所: `00_docs/10_Plugin設計書/feature/08-inventory/3-メソッド仕様/08_3-タスク・補助.md:95`
- 根拠: test comment は `## 5. 永続化制御` を指定するが、「inventory 本体の保存後も equipment durability の pending write が残る場合、saveNow は成功を返さない」という検証契約は line 100 の `## 6. アカウント別保存調停` にだけ存在する。さらに test の `times(2)` という再照会回数は、参照中のどの設計入力にも固定されていない。
- 問題: ユーザーが要求した「どの章のどの記載が期待値か」という追跡先が一章ずれており、実装上の呼出回数まで設計契約であるかのように固定している。
- 影響: コメントから期待値の根拠へ到達できず、同じ結果を保つ内部リファクタリングでも不必要にテストが壊れる。
- 修正方針: 当該 method の設計 pair を実契約がある `## 6. アカウント別保存調停` へ合わせる。`InventoryPersistence.saveNow` 自身の契約として維持するなら line 100 の内容を `## 5` へ移すか相互参照を追加し、再照会回数は設計へ明記する必要がない限り結果・pending 状態中心の assertion にする。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-003 [中] 恒久テストに source 正本と未記載メモが残っている
- 種別: `仕様不整合`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/combat/service/LevelDifferenceCalculatorTest.java:23`
- 関連箇所: `00_docs/10_Plugin設計書/feature/14-combat/3-メソッド仕様/14_3-サービス.md:112`
- 根拠: line 25 の検証契約は「現設計は具体式をsource正本としており、設計書へ明記要」と記す一方、参照先 line 114 には既に「正の基礎 EXP は level difference 補正後も最低 1」と明記されている。また同じ method の `100 -> 50` は `12_3-戦闘.md:258` の 5% 減衰式に依存し、基礎 EXP 0 の期待値は現在の参照節にない。
- 問題: 設計書を入力とする恒久テストの comment が実装を正本と宣言しており、method 内の複数 assertion と列挙した設計入力も一致していない。
- 影響: 現在の設計状態を誤って伝え、設計変更時にどの assertion を更新すべきか判断できない。validator が成功しても実質的なトレーサビリティが成立しない。
- 修正方針: stale な括弧書きを削除し、最低 1 の契約だけを当該 method に残す。`100 -> 50` は既存の倍率テストへ統合するか `12_3-戦闘.md` の design pair を隣接追加し、0 の扱いは採用済み設計へ明記する価値がある場合だけ別契約として残す。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-004 [中] ad-hoc 残留検出を Surefire の別接尾辞で回避できる
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:192`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/SKILL.md:86`
- 根拠: `_is_surefire_name` は `*Test`、`*Tests`、`*TestCase` を実行対象として認める一方、`_is_ad_hoc_test_name` は `AdHoc.*Test` と `.*OneShotTest` だけを拒否する。読み取り専用 probe では `AdHocFooTests`、`AdHocFooTestCase`、`FooOneShotTests`、`FooOneShotTestCase` がすべて `surefire=True, adhoc=False` になった。unit test は `AdHocProbeTest` と `NestedProbeOneShotTest` しか扱っていない。
- 問題: 一時診断 class の末尾を Surefire の別既定名へ変えるだけで、実行される ad-hoc テストを恒久 suite に残せる。
- 影響: 「一時テストは実行確認後に削除し、最終 diff に残さない」という今回の中核要件を validator が保証できない。
- 修正方針: Surefire 接尾辞を除いた論理名、または全許可接尾辞を含む一貫した regex で `AdHoc` / `OneShot` を検出し、file・最外 class・nested class の各 suffix variant を negative unit test に追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-005 [中] validator が設計 pair の順序と採用済み本文を検証していない
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:158`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/SKILL.md:72`
- 根拠: `_parse_javadoc_fields` は field を種別別 list へ分解して件数だけ比較し、line 424 で zip するため、`設計入力 A, 設計入力 B, 章 A, 章 B` という非隣接順序の probe も `pair_errors=[]` になった。TODO 検査は line 412 で Javadoc field 値だけを対象とし、参照見出し配下の設計本文は検査しない。具体契約判定も完全一致 8 文と 3 fragment だけで、`処理結果が期待どおりになることを確認する。` の probe と AR-CODE-003 の「source正本・明記要」を問題なく通した。unit test は隣接した正常 pair と field 自体の TODO しか持たない。
- 問題: skill が必須とする path-heading pair の隣接、TODO を期待値にしないこと、具体的な現在契約であることを validator の PASS が保証していない。
- 影響: 章の取り違え、未決仕様、汎用文、監査用メモを含む恒久テストが全件追跡済みとして報告され、今回と同じ手動監査を将来も繰り返すことになる。
- 修正方針: field token の出現順を保持して `設計入力` の直後が対応 `章・見出し` か検証する。参照見出しの本文範囲も解析して TODO / 未決カテゴリを拒否し、汎用文の言い換えと `source正本`・`明記要` 等の未解決メタ文を拒否する negative unit test を追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-006 [中] CHEST NPC テストの comment が assertion 範囲を過大に主張している
- 種別: `テスト不足`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/mob/service/MobEntityControllerTest.java:58`
- 関連箇所: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/mob/service/MobEntityController.java:272`
- 根拠: 検証契約は CHEST 系を「元 Material の ItemStack を保持する ItemDisplay で描画する」とするが、line 64-66 の assertion は `usesItemDisplayBlockMaterial(...)` が true であることしか確認しない。実際の entity 種別選択は production line 278-281、元 Material の `ItemStack` 設定は line 320-337 に分かれており、どちらが退行しても現テストは成功する。設計書 `12_3-サービス.md:140` は entity 種別・元 Material・transform のすべてを契約としている。
- 問題: comment と test name が描画 pipeline 全体を保証するように見える一方、テストしているのは material 分類 helper 一つだけである。
- 影響: CHEST 系が誤った `BlockDisplay` や `BARREL` ItemStack へ戻る表示退行を検出できず、レビュー時にはカバー済みと誤認する。
- 修正方針: 少なくとも comment を「ItemDisplay 経路の対象 material と判定する」へ狭める。設計上重要な回帰防止を残すなら、spawn seam または純粋 helper を用意し、生成 entity class と設定 `ItemStack` の Material まで別 test で観測する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-007 [低] 無関係な六つのメッセージ契約を一 method と test 由来の表へ束ねている
- 種別: `可読性/保守性`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/player/PlayerMsgResourceTest.java:73`
- 関連箇所: `00_docs/10_Plugin設計書/feature/03-player/03_1-モデル定義.md:97`
- 根拠: 一つの method が account mode、表示監査、player-setting、autosave、guide、未読件数という別機能の六 ID を異なる引数・識別語で検証する。同じ差分で追加した設計表もその六ケースだけを同じ順で列挙しており、選定基準や共通不変条件がない。skill line 73 は一 method 一設計契約を原則とし、複数 case をまとめるのは同じ記載の同じ契約を検証するときに限定している。
- 問題: 設計からテストを導出した形ではなく、既存 test fixture を正当化するための設計表に見え、各 ID の本来の feature 契約を追跡できない。
- 影響: メッセージ変更時の影響範囲が不明瞭になり、player model 設計が横断的なテストケース台帳へ肥大化する。
- 修正方針: properties の存在・ID parity は既存 resource checker に委ねる。文言や引数が UX 契約として重要な ID だけを所有 feature ごとの method へ分割して該当設計を参照し、重複する低価値 probe と test のためだけの表行は削除する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `中`
- 修正状態: `修正済み`

### AR-CODE-008 [低] ray entry の同一契約を model と service に重複記載している
- 種別: `死コード/重複`
- 対象: `00_docs/10_Plugin設計書/feature/28-player-interaction/28_1-モデル定義.md:58`
- 関連箇所: `00_docs/10_Plugin設計書/feature/28-player-interaction/3-メソッド仕様/28_3-サービス.md:134`
- 根拠: direction 正規化、sphere/AABB の内部 0、miss・後方・最大距離超過時 null の四不変条件を、model §3 と `PlayerInteractionRayTrace` の service §9 へほぼ同文で追加している。一方、`PlayerInteractionRayTraceTest` の全五 method は model §3 だけを参照し、物理 class を明記した service §9 はどのテストからも参照されない。
- 問題: 同一物理 method の期待値に二つの正本候補が生じ、今回の「設計入力箇所を一意に分かるようにする」目的を弱めている。
- 影響: 片方だけ更新された場合に設計書同士と test comment がドリフトする。
- 修正方針: 詳細な ray 計算契約を service §9 に一本化して五 method の参照をそちらへ変更し、model §3 は共通 ray を保持する事実と service 節への短い参照だけにする。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-009 [低] 三つの method Javadoc だけインデントが崩れている
- 種別: `可読性/保守性`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/shared/effect/ParticleDisplayArchitectureTest.java:25`
- 関連箇所: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/combat/service/DamageServiceMobDesignTest.java:241`
- 根拠: `ParticleDisplayArchitectureTest.java:25` と `DamageServiceMobDesignTest.java:241,285` の Javadoc 開始行だけが method と同じ 4 space ではなく 8 space で、続く `*` 行と `@Test` は 4 space になっている。差分内の test Javadoc を機械走査した結果、この不一致は三箇所だけだった。
- 問題: 大規模に統一した trace comment のうち三箇所だけ構造的な字下げが異なり、今回求められた「残るテストを綺麗な状態に保つ」を満たしていない。
- 影響: formatter 適用時の不要差分とレビュー上のノイズが残る。
- 修正方針: 三つの `/**` を周囲の method Javadoc と同じ 4 space へ揃える。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-010 [低] BAG 容量テストが runtime と persisted に同じ値を使っている
- 種別: `テスト不足`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/inventory/service/ItemInventoryStatusDesignTest.java:126`
- 関連箇所: `00_docs/10_Plugin設計書/feature/08-inventory/08_1-モデル定義.md > ## 3. インベントリ種別`
- 根拠: 当該 method は「BAG 容量は persisted slotCapacity でなく runtime INVENTORY_SLOTS を正本とする」と掲げるが、line 137 で persisted 容量を 24 にし、fixture が作る runtime 側も既定の 24 のまま、24 件成功・25 件目失敗だけを確認している。読み取り専用で値を追跡すると、実装が誤って persisted 容量を使う状態へ退行しても同じ結果になる。`EquipmentOperationInventoryStateTest.java:121-140` には runtime 2 と persisted 32 を分けた識別可能な同種テストが既にある。
- 問題: 設計上どちらを正本にするかを判別できない入力で、同じ契約のより強い既存テストとも重複している。
- 影響: persisted slotCapacity を再び採用する退行をこの method は検出せず、件数だけ増やして suite の保守コストを上げる。
- 修正方針: 恒久的に残すなら runtime と persisted を異なる値にして runtime 値が採用されることを直接確認する。既存の識別可能なテストで契約を十分に覆うなら、弱い当該 method を削除する。
- 修正対象候補: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/inventory/service/ItemInventoryStatusDesignTest.java`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-011 [中] GUI 配置テストが production 定数を期待値に流用し列挙契約も網羅していない
- 種別: `テスト不足`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/menu/view/screen/MainMenuScreenViewTest.java:20`
- 関連箇所: `00_docs/10_Plugin設計書/feature/09-menu/09_2-ユースケース.md > ## 1. メインメニューを開く`
- 根拠: `MainMenuScreenViewTest.java:47-61` と `EquipmentMenuScreenViewTest.java:35-61` は「設計 slot どおり」と宣言しながら、描画実装自身が参照する `MainMenuScreenView.*_SLOT` / `EquipmentMenuScreenView.*_SLOT` を assertion の取得位置にも使うため、定数と描画が一緒に誤った slot へ移っても成功する。さらに `SkillBindGuiLayoutTest.java:15-25` は設計が列挙する preset 1〜6 のうち 2・5 と `BACK_SLOT == 49` を直接検証せず、`CurrencyGuiViewTest.java:61-65` は 7 額面のうち 4 slot の Material と 1 slot の逆引きしか確認していない。
- 問題: 設計書に固定された GUI table を独立した期待値にせず、列挙した全 case を検証していないため、comment の契約範囲が assertion より広い。
- 影響: slot 定数の誤変更、preset 2・5 の mapping、額面 2〜4 の Material や逆引きが壊れても、設計対応済みを名乗る恒久テストが成功し得る。
- 修正方針: 設計書の literal slot を expectation に使うか、production 定数が literal と一致する assertion を別に置く。列挙契約は parameterized test 等で全行と forward/reverse mapping を確認し、main menu の lore など別契約の assertion は対応する設計 pair の method へ分割する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-012 [中] 一時 buff テストの失効 assertion が参照節から外れ実時間 sleep に依存する
- 種別: `仕様不整合`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/status/service/StatusServiceTemporaryBuffTest.java:15`
- 関連箇所: `00_docs/10_Plugin設計書/feature/07-status/3-メソッド仕様/07_3-サービス.md > ## 1. StatusService メソッド仕様 > ### 一時固定値バフ適用`
- 根拠: 参照中の「一時固定値バフ適用」は追加、同 status の置換、失効時刻の保持を記載する一方、期限切れ entry を除去する契約は同文書の `getStatus` 節および `feature/05-buff/3-メソッド仕様/05_3-サービス.md` の失効処理にある。当該一 method は加算・置換・失効除去を一つの design pair で主張し、line 35 の `Thread.sleep(1_100L)` で実時間経過を待っている。
- 問題: assertion の一部が指定した設計箇所から導けず、一つの method に別 contract を束ねたうえ wall clock に依存している。
- 影響: コメントから失効期待値の正本へ到達できず、負荷や scheduler の揺らぎで遅く不安定なテストになる。
- 修正方針: 適用・置換と失効除去を設計契約ごとに分割して、それぞれ正しい path-heading pair を付ける。失効を恒久テストする場合は注入可能な clock 等の時間 seam で境界前後を決定的に進める。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-013 [低] ad-hoc 検出を Surefire 接尾辞の重ね掛けで再び回避できる
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:237`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/SKILL.md:84`
- 根拠: AR-CODE-004 の修正は `TestCase` / `Tests` / `Test` を一度だけ除去してから `OneShot` を判定する。読み取り専用 probe では `ProbeOneShotTestTests`、`ProbeOneShotTestsTest`、`ProbeOneShotTestCaseTest`、`TestProbeOneShotTestTests` がすべて `_is_surefire_name=True` かつ `_is_ad_hoc_test_name=False` になった。
- 問題: 許可接尾辞を二つ連結するだけで、一時診断名を持つ実行対象 source を恒久 suite に残せる。
- 影響: 「一時テストは実行確認後に削除する」という中核ルールを validator が保証できない。
- 修正方針: 接頭辞・許可接尾辞を安定するまで正規化するか、`OneShot` marker と接尾辞列を一つの式で判定する。重複 suffix の negative unit test を file、最外 class、nested class に追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-014 [中] Kotlin annotation alias と条件付き無効化を validator が見落とす
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:14`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/SKILL.md:68`
- 根拠: test annotation 検出は `@Test` 等の literal 名だけ、無効化検出は `@Disabled` / `@Ignore` だけを regex で探す。temp source probe では Kotlin の `import org.junit.jupiter.api.Test as Spec` と `@Spec` を使う test method が method_count 0・issue なしになり、Java の `@DisabledOnOs(...)` 付き `@Test` も method_count 1・issue なしになった。どちらも JUnit が実行・skip を解釈する有効な annotation である。
- 問題: Kotlin で別名を付けた test は trace comment の対象外となり、JUnit 5 の条件付き無効化は「無効化 annotation を残さない」ポリシーを迂回する。
- 影響: 設計入力のない恒久テストや環境依存で skip されるテストが、全件追跡済みとして PASS する。
- 修正方針: Kotlin import alias を解決して test annotation として扱うか alias を明示的に禁止する。JUnit condition package の無効化 annotation 群も拒否し、alias と各代表条件の negative unit test を追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-015 [中] HUD テストが解除・unregister 契約を掲げて再描画時 cleanup だけを検証している
- 種別: `仕様不整合`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/hud/view/PlayerHudViewTest.java:87`
- 関連箇所: `00_docs/10_Plugin設計書/feature/10-hud/3-メソッド仕様/10_3-View.md > ## 4. sidebar 描画・解除`
- 根拠: comment は「解除時に plugin 専用 astral_info objective だけを unregister」とするが、test は `renderSidebar` を呼び、`Score.resetScore()` と global `scoreboard.resetScores` の非呼出しを確認するだけで、`removeSidebar` を呼ばず `Objective.unregister()` も検証しない。production の解除処理は `PlayerHudView.java:319-323`、再描画前の score cleanup は line 326-331 に分かれている。
- 問題: test name・comment・設計契約と、実際に観測する production 経路が一致していない。
- 影響: `removeSidebar` が objective を unregister しなくなってもテストは成功し、逆に再描画 cleanup の期待値は設計入力のないまま固定される。
- 修正方針: `removeSidebar` を実行して plugin objective だけが unregister されることを検証する method へ置き換える。再描画 cleanup も恒久契約として残すなら設計書へ別記し、対応する別 method に分割する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-016 [低] Mob ドロップ手順の項番 11 が重複している
- 種別: `ドキュメント不整合`
- 対象: `00_docs/10_Plugin設計書/feature/12-mob/3-メソッド仕様/12_3-戦闘.md:144`
- 関連箇所: `00_docs/10_Plugin設計書/feature/12-mob/3-メソッド仕様/12_3-戦闘.md > ## 6. ドロップ処理`
- 根拠: 新しく追加した rate 表示・残数 world drop の手順を line 144 で `11.` とした直後、既存のレアドロップ通知も line 145 で `11.` のまま残っている。
- 問題: 同じ処理手順内に同一番号が二つあり、章内の記載箇所を番号で一意に参照できない。
- 影響: test comment、レビュー、将来の設計差分で「手順 11」がどちらを指すか曖昧になる。
- 修正方針: 後者のレアドロップ通知を `12.` へ繰り下げ、必要なら同番号を参照する箇所も同期する。
- 修正対象候補: `00_docs/10_Plugin設計書/feature/12-mob/3-メソッド仕様/12_3-戦闘.md`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-017 [低] OneShot 形式を許可しながら実行例が AdHoc class 名に固定されている
- 種別: `ドキュメント不整合`
- 対象: `.codex/skills/astralrecord-plugin-test/SKILL.md:86`
- 関連箇所: `00_docs/10_Plugin設計書/README.md:214`
- 根拠: skill と設計書は一時 class 名として `AdHoc<目的>Test` または `<目的>OneShotTest` の二形式を許可し、手順 1 でも両方を列挙するが、次の実行コマンドは常に `mvn -q -Dtest=AdHoc<目的>Test test` である。後者を選んだ場合は作成した class ではなく存在しない別名を指定する。
- 問題: 許可された OneShot 経路を記載手順どおり実行しても、追加した一時テストの検出・実行確認にならない。
- 影響: 診断テスト未実行のまま成功と誤認するか、Surefire の「指定 test なし」失敗で手順が止まる。
- 修正方針: コマンドを `-Dtest=<一時テストClass名>` の共通 placeholder にするか、選択した命名形式ごとのコマンドを併記して skill と設計書を同期する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-018 [低] 設計 path-heading pair の物理的な隣接を任意行で回避できる
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:181`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/SKILL.md:72`
- 根拠: AR-CODE-005 の修正は認識済み三 field の名前だけを `field_order` へ追加し、field に一致しない Javadoc 行を line 192-193 で無視する。読み取り専用 probe で `設計入力:` と `章・見出し:` の間に任意の prose 行を挿入しても `_parse_javadoc_fields` の errors は空になった。
- 問題: error 文と skill が要求する「設計入力の直後に対応見出し」という物理配置を validator の PASS が保証していない。
- 影響: pair の途中へ別設計の説明や stale memo が混入しても見落とされ、機械的に一対一対応を読み取れない comment が残る。
- 修正方針: field の元行位置と非 field 行を保持し、空の Javadoc 装飾行以外を挟まず `設計入力`、対応 `章・見出し` を並べることを検証する negative unit test を追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-019 [中] 設計書だけの変更時に traceability validator が品質ゲートへ入らない
- 種別: `仕様不整合`
- 対象: `.codex/skills/astralrecord-code/references/plugin-code.md:95`
- 関連箇所: `00_docs/10_Plugin設計書/README.md > ## 11. テスト設計とトレーサビリティ`
- 根拠: plugin 実装 worker の必須実行条件は `10_plugin/AstralRecord/src/test` の追加・変更・削除時だけである。しかし許可された設計入力の見出し変更・移動・削除だけでも、既存 test Javadoc 413 method の参照は壊れ得る。`pom.xml` に validator の build 連携もなく、docs-only や production source + docs の変更で test source に触れない場合は実行を要求する共通ゲートがない。
- 問題: 「残る恒久テストは常に設計書の具体箇所へ対応する」という状態が、設計書側からの変更では維持されない。
- 影響: Maven test が成功していても存在しない見出しや移動前の章を参照する Javadoc が develop へ入り、後日の test 変更まで発見されない。
- 修正方針: `src/test` だけでなく、validator が許可する設計入力およびテスト方針文書の変更時にも実行する共通品質ゲートへ広げる。可能なら Maven verify または CI に接続し、skill 経路に依存せず検査する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-020 [高] Kotlin typealias で test annotation と skip annotation を検査対象外にできる
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:45`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:635`
- 根拠: 現在の Kotlin 対策は `import org.junit.jupiter.api.Test as Spec` 形式だけを正規表現で拒否する。読み取り専用 fixture で `import org.junit.jupiter.api.Test`、`typealias Spec = Test`、`@Spec fun hidden() {}` を配置すると、validator は `methods=0, issues=[]` を返した。skip annotation も同じ typealias で literal scan から隠せる。
- 問題: Kotlin/JUnit が実行または条件付き skip として解釈する annotation を、import alias を使わず別名化するだけで全 method の Javadoc 検査と無効化禁止を迂回できる。
- 影響: 設計入力のない恒久テストや環境によって実行されないテストが存在しても、414 method を全件追跡したという validator の PASS が成立する。
- 修正方針: Kotlin の JUnit annotation に対する `typealias` を解決するか明示的に拒否し、test annotation と skip annotation の双方について negative unit test を追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-021 [高] 未決見出しの祖先と設計本文中の未決メタを採用済みとして受理する
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:398`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:523`
- 根拠: `_has_non_adopted_heading` は一致 hierarchy の `len(hierarchy)-1` 以降だけを検査するため、`# root > ## 9. 未決事項 > ### contract` の完全 hierarchy を渡しても false になる。本文検査も `TODO` literal だけで、「未決事項」「判断待ち」「要検討」を扱わない。現 suite でも `TextDisplayPlacementServiceTest.java:40` が参照する `12_5-例外・ログ・運用.md > ## 6` の line 111 と、複数の `QuestServiceDesignTest` が参照する `29_3-メソッド仕様.md > ## 11` の line 134 に未決事項への記載が混在するが validator は PASS している。
- 問題: 「8-実装予定・9-未決事項・TODO は恒久期待値にしない」という skill の規則を、子見出し指定または本文への未決メタ記載で回避できる。
- 影響: 採用されていない期待値へ恒久テストを結び付けても全件追跡済みとして扱われ、設計決定前の挙動が固定され得る。
- 修正方針: 指定 hierarchy の全祖先・対象・子孫を未決見出し検査の対象にし、参照 section 本文の未決メタ語も拒否する。採用済み契約と未決メモが同じ section にある現行二箇所は、見出しを分割するか採用済み部分だけを一意に参照できる構造へ整理し、negative unit test を追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-022 [中] 汎用的な検証契約を denylist の言い換えだけで通せる
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:65`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:580`
- 根拠: 具体契約判定は短い完全一致集合と有限 fragment の包含判定である。読み取り専用 fixture の `検証契約: 対象の挙動が正しいことを確認する。` は入力、条件、結果を含まないが `issues=[]` になった。既存 unit test は「期待どおり」「source正本」「明記要」の三表現だけを拒否している。
- 問題: path と heading に汎用文を添えるだけで、各 method が何を保証するかを説明しないコメントを有効なトレーサビリティとして登録できる。
- 影響: validator が PASS しても assertion と設計契約の意味対応をレビューできず、今回求められた「テストのインプット箇所と期待値が分かる状態」が維持されない。
- 修正方針: 「対象・処理・挙動」と「正しい・確認・検証」だけで構成される汎用メタ表現を拒否し、入力・条件・結果または不変条件がない代表的な言い換えを複数 negative unit test に追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-023 [中] Surefire の実設定によるテスト除外を検査せず POM 変更も gate 対象外である
- 種別: `仕様不整合`
- 対象: `.codex/skills/astralrecord-code/references/plugin-code.md:97`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:658`
- 根拠: validator は source file/class 名が Surefire の既定接頭辞・接尾辞に合うかだけを確認し、`10_plugin/AstralRecord/pom.xml` の Surefire `includes` / `excludes` を読まない。共通 traceability gate の trigger にも POM はないため、例えば `**/*DesignTest.*` を exclude しても validator は PASS し、POM だけの変更では gate 実行も要求されない。
- 問題: 「Surefire 対象外の名前へ変えて温存しない」という規則を実行設定側から回避でき、source 上存在するテストが Maven で本当に走ることを保証していない。
- 影響: 設計対応済みとして数えた多数の test class を静かに未実行へでき、テスト成功件数と traceability method 数の照合も将来崩れる。
- 修正方針: POM を共通 gate trigger に追加し、Surefire の custom include/exclude を禁止するか effective configuration と検出した test class を照合する。除外設定を含む negative fixture または専用 unit test も追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-024 [中] method Javadoc と @Test の間の通常 annotation を欠落と誤判定する
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:192`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:683`
- 根拠: `_nearest_direct_javadoc` は Javadoc 終端から検出した test annotation までに空白以外が一文字でもあれば `None` を返す。読み取り専用 fixture で正しい三 field Javadoc の後に `@DisplayName("boundary")`、その後に `@Test` を置くと `JAVADOC_MISSING` になった。同様に `@Tag` や `@Timeout` 等を test annotation より前へ置く通常の annotation stack も拒否される。
- 問題: Javadoc が method の annotation 群へ物理的に隣接していても、`@Test` そのものの直前でなければ無効になるという、Java/JUnit の通常構文より狭い未文書ルールを強制している。
- 影響: 有用な表示名・分類・timeout を付けた正当な恒久テストを追加すると品質 gate が偽陽性で止まり、回避のため annotation を不自然に並べ替える必要が生じる。
- 修正方針: Javadoc と test annotation の間が空白および同一 method に属する連続 annotation 群だけなら直接対応と認める parser にし、`@DisplayName` 等を含む positive unit test を追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-025 [中] plugin-test skill が task worktree でなく main workspace の設計を入力にする
- 種別: `仕様不整合`
- 対象: `.codex/skills/astralrecord-plugin-test/SKILL.md:14`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/SKILL.md:29`
- 根拠: Required Context の六パスはすべて `E:\AstralRecord-Workspace` に固定され、設計 README も main checkout を指す。一方、今回のような統合 workflow は専用 task worktree 内で設計書と恒久テストを同時に更新し、validator は実行 cwd の task-root にある設計を読む。
- 問題: worker が期待値を決める入力と、最終 validator が検証する入力が別 checkout になり、task 内で追加・修正した設計を読む前にテストを作り得る。
- 影響: stale な main 設計から期待値を導出したテストが、worktree 側の更新済み見出しへ形式上だけ対応して PASS するなど、設計起点のライフサイクルがねじれる。
- 修正方針: skill 冒頭で `git rev-parse --show-toplevel` により `<task-root>` を解決し、Required Context と task 対象の参照を `<task-root>` 相対にする。live server 等、本当に main 固定が必要な外部配置だけを明示的な例外に分ける。
- 修正対象候補: `.codex/skills/astralrecord-plugin-test/SKILL.md`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-026 [中] 通貨 GUI テストが locked と unlocked の表示差を識別していない
- 種別: `テスト不足`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/currency/view/CurrencyGuiViewTest.java:30`
- 関連箇所: `00_docs/10_Plugin設計書/feature/16-currency/16_0-概要.md:35`
- 根拠: method 名と検証契約は星核所持有無に対応するロック表示を掲げるが、false 描画後と true 描画後の両方で slot 51 の Material が `EMERALD` であることしか確認しない。production は `CurrencyGuiView.java:132-143` で表示名、色、lore を locked/unlocked に分岐しており、この分岐を削除して常に同じ表示を返しても当該 test は成功する。
- 問題: 設計の「所持条件に応じた利用可否表示」を確認せず、両状態でも変わらない導線 Material の assertion だけでロック表示をカバーしたと主張している。
- 影響: 星核未所持者へ利用可能表示を出す、または所持者へロック表示を出す UI 退行を検出できず、設計対応済みと誤認する。
- 修正方針: false/true それぞれの表示名・色・lore 等、利用不可と利用可を識別できる観測値を確認する。文言 literal を恒久契約にしない場合でも、少なくとも二状態の component が異なり必要な案内内容を含むことを設計記載に合わせて検証する。
- 修正対象候補: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/currency/view/CurrencyGuiViewTest.java`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-027 [低] HUD の unrelated objective 非解除 assertion が対象へ結び付いていない
- 種別: `テスト不足`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/hud/view/PlayerHudViewTest.java:92`
- 関連箇所: `00_docs/10_Plugin設計書/feature/10-hud/3-メソッド仕様/10_3-View.md > ## 4. sidebar 描画・解除`
- 根拠: AR-CODE-015 の修正で `removeSidebar` 実経路と `astralObjective.unregister()` は確認できるようになった。しかし `unrelatedObjective` は mock を生成するだけで scoreboard の取得結果や objective 集合へ一度も stub されず、line 103 の `never().unregister()` は production から到達不能な孤立 mock に対する検証である。全 objective を誤って巡回解除する退行を入れても、scoreboard の既定空集合を使う限りこの assertion は成功する。
- 問題: 検証契約の「astral_info objective だけ」のうち、他 objective を保持する排他条件が識別可能な fixture になっていない。
- 影響: 専用 objective の解除自体は守れても、他 plugin や別用途の sidebar objective まで解除する副作用を test が検出できない。
- 修正方針: unrelated objective を scoreboard の観測可能な取得経路へ結び付け、実装が astral_info の名前指定だけを使い全 objective を列挙しないことを検証するか、契約を「astral_info を unregister する」へ狭めて空疎な mock/assertion を削除する。
- 修正対象候補: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/hud/view/PlayerHudViewTest.java`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-028 [低] SkillBind テストの Javadoc 終端だけインデントが崩れている
- 種別: `可読性/保守性`
- 対象: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/skill/gui/SkillBindGuiLayoutTest.java:12`
- 関連箇所: `なし`
- 根拠: class 内の Javadoc 開始行と `*` 行は通常どおり 4 space + 1 space で揃うが、終端だけ `    */` となり `*` が一列左へずれている。`rg -n '^    \\*/$' 10_plugin/AstralRecord/src/test` ではこの一箇所だけが該当した。
- 問題: AR-CODE-009 で統一した test trace comment の字下げ不整合が別ファイルに一件残っている。
- 影響: formatter 適用時の不要差分とレビュー上のノイズが残り、「残るテストを綺麗な状態に保つ」という目的を満たし切らない。
- 修正方針: 終端を周囲と同じ `     */` へ揃える。
- 修正対象候補: `10_plugin/AstralRecord/src/test/java/io/github/maaasu/astralRecord/feature/skill/gui/SkillBindGuiLayoutTest.java`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-029 [中] Kotlin のバッククォート付き import alias でテスト検査を回避できる
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:46`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/tests/test_validate_test_traceability.py:568`
- 根拠: Kotlin は `import org.junit.jupiter.api.Test as `Spec Test`` と `@`Spec Test`` のようにバッククォート付き識別子を import alias と annotation 名へ使用できる。現行 `KOTLIN_JUNIT_ALIAS_PATTERN` は alias 部を `[A-Za-z_$][\w$]*` に限定し、`ANNOTATION_PATTERN` もバッククォート識別子を扱わない。読み取り専用 probe ではこの source に対して alias match と test annotation match がともに 0 件になり、既存 unit test も通常識別子の `as Spec` だけを対象としている。
- 問題: skill が明示的に禁止する Kotlin の JUnit import alias を表記だけ変えて残せ、alias を使った test method は設計 Javadoc、見出し、契約、Surefire class 名の全検査から外れる。
- 影響: 設計入力のない恒久テストや一時診断テストを Maven では実行される状態のまま、traceability validator 上は method 0 件として隠せるため、全 method 追跡済みという品質ゲートの保証が成立しない。
- 修正方針: Kotlin import alias の alias 部に `KOTLIN_IDENTIFIER` と同等のバッククォート識別子を許容して必ず拒否する。バッククォート付き alias と使用側 annotation を含む negative unit test を追加し、必要なら escaped annotation 名自体も test/skip annotation 検出へ含める。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-030 [高] Kotlin の Unicode alias・末尾セミコロン・backtick FQCN で JUnit 検査を回避できる
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:40`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/tests/test_validate_test_traceability.py:568`
- 根拠: 最終受入の独立 temp fixture で、`import org.junit.jupiter.api.Test as 試験` と `import org.junit.jupiter.api.condition.DisabledOnOs as 条件`、末尾 `;` を持つ `typealias SemiSkip = org.junit.jupiter.api.condition.DisabledOnOs;`、および fully-qualified annotation の末尾識別子 `Test` / `DisabledOnOs` をバッククォートで囲む表記を検証した。validator は Unicode alias と backtick FQCN の test を method 0・無指摘、Unicode alias・セミコロン typealias・backtick FQCN の skip を無指摘とした一方、一時 Maven/Kotlin/JUnit は test を各1件実行し、skip fixture は1件を skipped と報告した。`KOTLIN_IDENTIFIER` は通常識別子の先頭を ASCII に限定し、typealias pattern は合法な末尾 `;` を許容せず、test/skip annotation pattern は backtick 識別子を扱わない。
- 問題: AR-CODE-029 で空白入り backtick import alias は拒否できるようになったが、Kotlin の別の正規表記だけで設計 Javadoc 検査と無効化禁止を再び回避できる。
- 影響: Maven が実行する未追跡 test、または環境条件で skip する test が残っても、全 method を追跡済みとする品質ゲートが PASS する。
- 修正方針: Kotlin 通常識別子へ Unicode letter を許容するか import 文を token 化して JUnit alias を必ず拒否し、typealias の任意セミコロンと backtick を含む fully-qualified annotation 名も test/skip として認識する。test と skip の各経路へ Unicode alias、末尾 `;` typealias、backtick FQCN の negative unit test を追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-031 [中] 汎用的な検証契約をクラス名一語の追加だけで受理する
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:614`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/tests/test_validate_test_traceability.py:840`
- 根拠: 独立 fixture の `検証契約: SampleServiceの動作を確認する。` は入力、条件、結果、不変条件を一つも示さないが、validator は method 1・無指摘で受理した。`_is_generic_meta_contract` は既知の汎用語を除去した後に任意文字が残れば具体契約とみなすため、クラス名や feature 名を一語加えるだけで AR-CODE-022 の denylist を回避できる。既存 unit test は固有名詞を含まない汎用文だけを扱う。
- 問題: method が何を保証するか説明しないコメントを、対象名だけ添えて有効な設計トレーサビリティとして登録できる。
- 影響: validator が PASS しても assertion と設計契約の意味対応を判断できず、ユーザーが求めた入力条件・期待結果の分かる状態を維持できない。
- 修正方針: 固有名詞を除いた述部が「動作を確認する」等の汎用メタ表現だけなら拒否する判定へ改め、class/service/feature 名を含む代表的な汎用文を negative unit test に追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-032 [中] Surefire の system property を custom test filter と誤判定する
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:1011`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/tests/test_validate_test_traceability.py:883`
- 根拠: 独立 POM fixture で `maven-surefire-plugin/configuration/systemPropertyVariables` 配下へ正当な `<test>integration</test>` と `<groups>domain-a</groups>` を置くと、Maven は両値を system property としてテストから参照でき、1件成功した。一方 validator は `plugin.iter()` で任意階層の同名要素を走査し、`POM_SUREFIRE_FILTER` を2件返した。既存 unit test は実際の `<configuration><excludes>` だけを対象とする。
- 問題: test 選択設定ではない任意の system property 名まで filter と解釈し、正当な POM を品質ゲートで拒否する。
- 影響: integration profile 等で `test` や `groups` という一般的な system property を渡すだけで docs/test の無関係な変更も検証不能になり、利用者が gate を外す誘因になる。
- 修正方針: Surefire の test filter 判定を `configuration` 直下の既知 scalar 要素と、`includes` / `excludes` 等の正規構造に限定し、`systemPropertyVariables` や他の map/list 配下の同名キーを無視する positive unit test を追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

### AR-CODE-033 [中] 固有主語付きの汎用契約を名詞化した述部で再び受理できる
- 種別: `バグ/アルゴリズム`
- 対象: `.codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py:164`
- 関連箇所: `.codex/skills/astralrecord-plugin-test/scripts/tests/test_validate_test_traceability.py:941`
- 根拠: 最終受入の独立 temp fixture で `検証契約: SampleServiceの動作確認を行う。` を指定すると、入力、条件、結果、不変条件を一つも示さないにもかかわらず validator は method 1・無指摘で受理した。AR-CODE-031 の修正で追加した `GENERIC_NAMED_SUBJECT_CONTRACT_PATTERN` は `SampleServiceの動作を確認する。` のように「動作」と「確認」の間へ助詞がある形だけを扱い、「動作確認」を一語へ名詞化した同義の汎用述部を扱わない。`_is_generic_meta_contract` も固有名詞 `SampleService` が残るため拒否しない。
- 問題: 条件と期待結果がない「固有主語＋汎用述部」を、述部を名詞化するだけで有効な設計トレーサビリティとして登録できる。
- 影響: validator が PASS しても method が何を保証するか判断できない comment が残り、ユーザーが求める入力条件・期待結果の分かる状態を維持できない。
- 修正方針: `動作確認を行う`、`正常動作を確認する`、`正しく動作することを確認する` 等の代表的な固有主語付き汎用述部を正規化して拒否するか、固有主語を除いた後に具体的な条件・結果・不変条件が存在するかを判定する。これらの negative fixture と、空入力時の戻り値・cache 非更新を明示する positive fixture を unit test に追加する。
- 修正対象候補: `複数`
- 修正可否: `自動修正可`
- 確信度: `高`
- 修正状態: `修正済み`

## 未確認/質問

なし。

## 修正スキル入力サマリ
- 自動修正候補: `なし`
- 要確認: `なし`
- 推奨修正順: `なし`
- 対象範囲: `.`

## 確認した範囲
- 対象領域: AstralRecord Plugin の恒久テスト、Plugin 設計書、`astralrecord-plugin-test` skill と traceability validator
- 読んだルール/設計書: `AGENTS.md`, `PLUGIN_GUIDE.md`, `.codex/skills/README.md`, `.codex/skills/astralrecord-code/references/plugin-code.md`, `.codex/skills/astralrecord-plugin-test/SKILL.md`, `00_docs/10_Plugin設計書/README.md`, 差分対象の feature 設計書
- 読んだソース: `.codex/skills/astralrecord-plugin-test/scripts/**/*.py`, `10_plugin/AstralRecord/src/test/**/*.{java,kt}`, 指摘に関連する `10_plugin/AstralRecord/src/main/**/*.java`, 削除対象四ファイルの HEAD 内容
- 実行した検査: `python -X utf8 -B .codex/skills/astralrecord-plugin-test/scripts/validate_test_traceability.py --repo-root .` は 414 methods / 127 source files で PASS、validator unit は 29 tests で PASS、現行 Surefire report 125 件・418 tests は failure/error/skip なし、`git diff --check` は成功。読み取り専用 probe で Kotlin typealias、未決祖先・本文、汎用契約文、通常 annotation stack、POM exclusion の未検査を再現し、全414 method の comment/assertion、削除・復元対象、docs/resource/skill validation の成功結果を照合した。最終受入の独立 temp fixture では Unicode alias、末尾セミコロン付き typealias、backtick FQCN、固有名詞付き汎用契約、Surefire system property の検出結果を Maven/JUnit と照合し、AR-CODE-033 の代表的な汎用契約 10 件を全件拒否、条件・結果・数値制約・禁止副作用・不変条件を含む具体契約 6 件を全件受理した。

## 対象外
- 実サーバー clone、Purpur/Paper client 表示、外部 API/DB の実接続確認は今回の test/docs/skill 差分の静的・単体レビュー範囲外。
