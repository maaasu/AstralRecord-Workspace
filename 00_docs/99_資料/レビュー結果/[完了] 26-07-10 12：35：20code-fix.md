# Gathering 機能 コード修正結果

- 対象: `C:\AstralRecord-Workspace\10_plugin\AstralRecord\src\main\java\io\github\maaasu\astralRecord\feature\gathering`
- 対象プロジェクト: `10_plugin/AstralRecord` / Minecraft Plugin
- 使用 skill: `$astralrecord-code-fix`
- 修正元レビュー: `00_docs\99_資料\レビュー結果\(0・5) 26-06-24 22：15：11code-review.md`
- 実施日: 2026-06-24
- 指摘修正数 / 指摘数: `5 / 5`
- 完了状態: `完了`

## 指摘一覧

### AR-CODE-001 [高] 採集ツール判定が `equipment.tag` ではなくアイコン Material 推定になっている

- 修正状態: `修正済み`
- 修正可否: `要確認`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/gathering/service/GatheringService.java:252`
- 修正内容: API が返す `equipment.tag` を Plugin の `ItemEquipment` モデルへ取り込み、メインハンド ItemStack の item ID からロード済み item model を解決して採集ツールタグを判定する。PDC へのタグ重複保存とアイコン Material 推定は使用しない。

### AR-CODE-002 [中] スポナー reload 後にスポナー単位の生存数上限を超えて再湧きできる

- 修正状態: `修正済み`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/gathering/spawner/service/GatheringSpawnerService.java:75`
- 修正内容: `GatheringService.clearInstances()` を追加し、採集定義 reload と採集スポナー reload の前に出現中 instance と採集中 session を破棄するようにした。reload 後に旧 tracking の採集 instance が残り、スポナー単位上限が 0 扱いから再湧きする状態を解消した。

### AR-CODE-003 [中] スポナーアイテム付与で部分挿入後も失敗扱いになる

- 修正状態: `修正済み`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/gathering/command/GatheringCommand.java:87`
- 修正内容: `Inventory#addItem` の前に `canFitItem(...)` で全量が入るか確認し、不足時は `P_5241` を返して挿入を行わないようにした。これにより部分挿入後の失敗表示を避ける。

### AR-CODE-004 [低] 採集管理コマンドのプレイヤー向け文言が `PlayerMsgId` を経由していない

- 修正状態: `修正済み`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/gathering/command/GatheringCommand.java:38`
- 修正内容: 採集管理コマンドの直書き文言を `PlayerMsgId` / `player.properties` 経由に変更した。新規メッセージ ID として `P_5720` から `P_5725` を追加し、既存の `P_5708` / `P_5714` / `P_5241` も再利用した。

### AR-CODE-005 [低] gathering packet 例外が SkillTree 用 LogId で出力され、Throwable も失われる

- 修正状態: `修正済み`
- 対象: `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/gathering/service/GatheringPacketDisplay.java:147`
- 修正内容: gathering packet 用の `LogId.W_9010` と `logger.properties` 文言を追加し、`Logger.log(LogId.W_9010, exception, ...)` で Throwable を保持するようにした。

## 未確認・質問

なし。

## 修正スキル入力サマリ

- 自動修正候補: `AR-CODE-002`, `AR-CODE-003`, `AR-CODE-004`, `AR-CODE-005`
- 修正済み: `AR-CODE-002`, `AR-CODE-003`, `AR-CODE-004`, `AR-CODE-005`
- 要確認: なし（API item model の `equipment.tag` を参照する方針で確定）
- 推奨修正順: `AR-CODE-001` -> `AR-CODE-002` -> `AR-CODE-003` -> `AR-CODE-004` -> `AR-CODE-005`
- 対象範囲: `C:\AstralRecord-Workspace\10_plugin\AstralRecord\src\main\java\io\github\maaasu\astralRecord\feature\gathering`

## 確認した範囲

- 対象プロジェクト: `10_plugin/AstralRecord`
- 読んだ設計書/ルール:
  - `C:\AstralRecord-Worktrees\gathering-review-fix\AGENTS.md`
  - `C:\AstralRecord-Worktrees\gathering-review-fix\README.md`
  - `C:\AstralRecord-Worktrees\gathering-review-fix\PLUGIN_GUIDE.md`
  - `C:\AstralRecord-Worktrees\gathering-review-fix\.codex\skills\astralrecord-code-fix\SKILL.md`
  - `C:\AstralRecord-Worktrees\gathering-review-fix\.codex\skills\astralrecord-code\references\plugin-code.md`
- 読んだソース:
  - `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/gathering/**/*.java`
  - `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/feature/player/PlayerMsgId.java`
  - `10_plugin/AstralRecord/src/main/java/io/github/maaasu/astralRecord/infrastructure/logging/LogId.java`
  - `10_plugin/AstralRecord/src/main/resources/player.properties`
  - `10_plugin/AstralRecord/src/main/resources/logger.properties`
- 実行した検証: `git diff --check`。`mvn -q -DskipTests compile` は `mvn` が PATH 上になく未実行。

## 対象外

- `AR-CODE-001` の設計判断を伴う修正。
- API 側 `/api/gathering` / `/api/gathering-spawner` 実装の修正。
