# docs-review レビュー結果

- レビュー対象: `E:\AstralRecord-Workspace\00_docs\10_プラグイン設計書\feature\12-mob`
- skill: `docs-review`
- 完了状態: 完了
- 指摘修正数 / 指摘数: 9 / 9

## 指摘一覧

### AR-DOC-001 [高] メソッド仕様にログ/メッセージ本文が記載されていない
- 種別: `形式/命名`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.02-サービス.md`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.03-戦闘.md` / `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.04-リポジトリ.md` / `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.05-パケット表示.md` / `00_docs/10_プラグイン設計書/feature/12-mob/5-例外・ログ・運用/12_5.00-例外・ログ・運用.md`
- 根拠: 設計書運用ルール（[[README]]）「5. メソッド仕様内のログ・メッセージ記載ルール」では、ログ ID だけで終わらせず、出力する処理番号の直下にインデントで `ID / レベル / テンプレート / 出力条件 / 引数` の表を記載する。`12_5.00-例外・ログ・運用.md` 第 2 節では `loadAll` / `spawn` / `destroy` / `destroyAll` / `start` / `stop` / `tick` / `applyDamage` / `handleDeath` / `findAllSummaries` / `findById` / `spawn`(view) / `destroy`(view) / `move`(view) の各メソッドにログ出力ポイントが明示されているが、対応するメソッド仕様 (`12_3.02` / `12_3.03` / `12_3.04` / `12_3.05`) の処理内には 1 行のログ仕様も記載されていない。
- 問題: メソッド仕様にログ仕様が無く、メッセージテンプレート・出力条件・引数を読み取れない。実装時に `12_5.00` の表だけが正本となり、ルールが定める「処理手順直下に記載」を満たさない。
- 影響: 実装者が「どの処理ステップでログを出すか」「テンプレート文字列」を判断できず、`logger.properties` 整備と仕様が乖離する。未決事項 11 で予約された `5500-5599` の採番が進めにくい。
- 修正方針: 各メソッド仕様の該当処理番号直下に、運用ルール 5 の表形式でログ仕様を追記する。ID は採番完了までは `LogId.（未採番）` 等のプレースホルダで明示する。`12_5.00` の表はメソッド仕様の集約サマリとして残してよい。
- 修正対象候補: `複数`
- 修正可否: `要確認`

### AR-DOC-002 [高] 致死処理のキラー特定フォールバックを支えるフィールドがモデル定義に存在しない
- 種別: `不足`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.03-戦闘.md:157`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/1-モデル定義/12_1.00-モデル定義.md:195`
- 根拠: `12_3.03-戦闘.md` の `handleDeath` 仕様で「キラー決定: 脅威値テーブルの `top()` を使用。取得失敗時は最後にダメージを与えたプレイヤー UUID をフォールバックとして使用」と規定している。一方 `12_1.00-モデル定義.md` の `MobInstance.16.1 項目` には `lastAttackTick` はあるが「最後にダメージを与えたプレイヤー UUID」を保持するフィールドが無い。
- 問題: フォールバック判定に必要な状態（last attacker UUID）の保持先が設計書上に存在しない。`applyDamage` の処理内容にも `instance.lastAttackerUuid` 等の更新ステップが無いため、`handleDeath` のフォールバックが恒常的に失敗する設計になっている。
- 影響: 脅威値テーブルが空（例: ダメージはあるが脅威値が即座に減衰された等）の状況でキラーを特定できず、ドロップ・経験値・金銭が誰にも渡らない。
- 修正方針: `MobInstance` に `lastAttackerUuid: UUID?`（または同等項目）を追加し、`applyDamage` の処理ステップで更新する旨を明記する。あるいは `handleDeath` のフォールバック仕様を見直し、必ず脅威値テーブルに加算する設計に統一する。
- 修正対象候補: `複数`
- 修正可否: `要確認`

### AR-DOC-003 [高] `MobCommand` / `MobTabCompleter` のメソッド仕様が存在しない
- 種別: `不足`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/12_README.md:8`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/0-概要/12_0.00-概要.md:28` / `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.00-索引.md`
- 根拠: 12_README.md `対象実装パス` に `command/*` が含まれ、`12_0.00-概要.md` の構成要素にも `command/MobCommand`・`command/MobTabCompleter` が「`/mob load|list|spawn` を提供」と明記されている。一方 `3-メソッド仕様/` にはコマンド層のファイルが無く、責務マッピング (`12_3.00-索引.md`) にもコマンドの行が無い。
- 問題: 概要・README にコマンドが「構成要素」「対象実装パス」として明示されているのにメソッド仕様が無い。サブコマンド `load|list|spawn` のうち `list` はユースケースにもメソッド仕様にも対応する記述が無く、引数仕様・権限・タブ補完仕様が未定義。
- 影響: コマンド層の実装範囲が決まらず、ユーザインタフェース（管理者操作）の仕様レビューができない。`/mob list` の出力フォーマットや、権限・引数バリデーションも判断不能。
- 修正方針: `12_3.01-コマンド.md` を新設して `/mob load`・`/mob list`・`/mob spawn <mobId>` を 1 件ずつ規定する。最低限「クラス名 / 物理名 / サブコマンド / 引数 / 権限 / 処理内容 / プレイヤー向けメッセージ」を記載する。索引の責務マッピングにも追記する。
- 修正対象候補: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.01-コマンド.md`
- 修正可否: `要確認`

### AR-DOC-004 [高] ユースケースで参照される論理名「Mob 攻撃」がメソッド仕様に存在しない
- 種別: `矛盾`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/2-ユースケース/12_2.00-ユースケース.md:118`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.03-戦闘.md`
- 根拠: ユースケース 5「Mob がプレイヤーを攻撃する」の主シナリオで `[[12_3.03-戦闘]].Mob 攻撃 を呼ぶ` と参照しているが、`12_3.03-戦闘.md` には「Mob 攻撃」という論理名は存在せず、該当する処理は「攻撃 tick」(`tickCombat`) である。
- 問題: 別ファイルメソッド参照 `[[file]].論理名` で未定義の論理名を指している。
- 影響: ユースケースとメソッド仕様の対応が辿れず、レビュー・実装時に該当処理を特定できない。
- 修正方針: ユースケース 5 の参照を `[[12_3.03-戦闘]].攻撃 tick` に修正する。
- 修正対象候補: `00_docs/10_プラグイン設計書/feature/12-mob/2-ユースケース/12_2.00-ユースケース.md`
- 修正可否: `自動修正可`

### AR-DOC-005 [中] `MobService.destroyAll` 仕様に tick タスク停止が含まれず、概要と不整合
- 種別: `矛盾`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.02-サービス.md:118`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/0-概要/12_0.00-概要.md:67`
- 根拠: 概要 5「ライフサイクル登録点」の終了時に「`MobService.destroyAll` で全インスタンスのパケット破棄とキャッシュ破棄を行う。tick タスクを停止する。」と並記されているが、`destroyAll` の処理内容は (1) 反復 (2) 破棄パケット送出 (3) キャッシュクリア の 3 ステップで、`MobAiService.stop` の呼び出しは含まれない。
- 問題: 終了時シーケンスを誰が司るかが不明確。`destroyAll` が tick 停止まで責務として持つのか、core 側で別途 `stop` を呼ぶのかが読めない。
- 影響: 終了時に tick タスクが残ったまま `instances` 空で空回りする、または順序によっては破棄パケット送出と tick 駆動が競合する可能性。
- 修正方針: 概要側を「core 側で `MobAiService.stop` → `MobService.destroyAll` の順に呼ぶ」のように明示するか、`destroyAll` の処理内容に tick 停止呼び出しを追記する。どちらにするかは設計判断。
- 修正対象候補: `複数`
- 修正可否: `設計判断待ち`

### AR-DOC-006 [中] `updateViewers` のクラス名と責務マッピングが矛盾
- 種別: `矛盾`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.02-サービス.md:252`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.00-索引.md:22` / `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.02-サービス.md:217`
- 根拠: 「表示範囲更新」の `クラス名: MobService` と記載されているが、責務マッピング (`12_3.00-索引.md`) では `MobService` は「テンプレート・インスタンス管理、外部 API のエントリポイント」、`MobAiService` は「tick 駆動の状態遷移、待機行動駆動」とされ、viewer 集合管理は記載が無い。さらに「インスタンスごとに『表示中プレイヤー UUID 集合』を保持しておく」とあるが、`MobInstance.16.1` にも `MobService` 側の状態にも保持先フィールドが明示されていない。
- 問題: 表示中プレイヤー UUID 集合の所有者と更新責務が不明。`tick` から `MobService.updateViewers` を呼ぶフローと、責務マッピング上の `MobService` 責務が一致しない。
- 影響: viewer state の格納場所が決まらず、`MobInstance` 永続性なし設計と矛盾する状態を新規に追加することになる。
- 修正方針: (a) `updateViewers` を `MobAiService` の責務に移すか、(b) `MobService` の責務に「viewer 同期」を追記し、`MobInstance` または `MobService` 側状態に viewer UUID 集合の保持先を明記する。索引の責務マッピングも合わせて更新する。
- 修正対象候補: `複数`
- 修正可否: `設計判断待ち`

### AR-DOC-007 [中] モデル定義 13 節で `money` 型参照に「？」が混入
- 種別: `形式/命名`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/1-モデル定義/12_1.00-モデル定義.md:160`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/1-モデル定義/12_1.00-モデル定義.md:166`
- 根拠: 13 節 `Mob ドロップ設定` の `money` 行に `[[12_1.00-モデル定義]].Mob 金銭ドロップ？` とあるが、論理名は 14 節で「Mob 金銭ドロップ」と定義されている。
- 問題: 末尾の「？」は誤記。`null` 許容の意図を表現したい場合も、表記揺れとなり Wiki link 解決の障害となる。
- 影響: ファイル内のリンク整合が崩れ、Obsidian で論理名解決が外れる。
- 修正方針: `[[12_1.00-モデル定義]].Mob 金銭ドロップ` に修正する。`null` 許容は同行の「説明」列で表現する。
- 修正対象候補: `00_docs/10_プラグイン設計書/feature/12-mob/1-モデル定義/12_1.00-モデル定義.md`
- 修正可否: `自動修正可`

### AR-DOC-008 [中] `/mob list` がユースケース・メソッド仕様に存在しない
- 種別: `不足`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/0-概要/12_0.00-概要.md:28`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/2-ユースケース/12_2.00-ユースケース.md` / `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.02-サービス.md`
- 根拠: 概要に「`/mob load|list|spawn` を提供」と明示されているが、`list` に対応するユースケース・メソッド仕様が無い。`MobService` 側にも「全インスタンス取得」(`getInstances`) はあるが、これがコマンド `list` の対象か別用途かが不明。さらに `12_5.00-例外・ログ・運用.md` の「運用上の注意」では「`/mob list` で件数を監視すること」と運用前提にもなっている。
- 問題: コマンド `list` の出力対象（テンプレート一覧 / インスタンス一覧 / 件数のみ）と出力フォーマット、引数（フィルタ、ページング）が未定義。
- 影響: 運用 docs が前提とする監視手段が仕様化されておらず、実装時に解釈幅が大きい。
- 修正方針: `12_3.01-コマンド.md`（AR-DOC-003 と統合可）に `/mob list` の責務・出力・引数を規定する。テンプレート一覧か実体一覧かを設計判断で確定する。
- 修正対象候補: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.01-コマンド.md`
- 修正可否: `設計判断待ち`

### AR-DOC-009 [低] `findById` の `404` 時のログレベルが運用 docs と整合していない
- 種別: `矛盾`
- 対象: `00_docs/10_プラグイン設計書/feature/12-mob/5-例外・ログ・運用/12_5.00-例外・ログ・運用.md:48`
- 関連箇所: `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.04-リポジトリ.md:107`
- 根拠: `12_3.04-リポジトリ.md` 第 4 節「ステータスコード方針」では `GET` の `404` を「`null` 返却（ログ出力なし）」としている。`12_5.00-例外・ログ・運用.md` の MobRepository 表は「`findById` | 200/404 以外のレスポンス | ERROR（例外送出前）」と整合しているが、「`spawn` テンプレート未発見 | WARN」（`MobService` 側）と組み合わせると、`404 → null → WARN` 出力経路は仕様上 `findById` ではなく `MobService.spawn` 側でしか発生しない、と読み取らせるための補足が無い。
- 問題: ログ責務の境界（リポジトリは沈黙、サービスが WARN）が読み手に伝わりにくい。
- 影響: 実装時にリポジトリ側で `404` の WARN を二重出力する可能性。
- 修正方針: `12_5.00-例外・ログ・運用.md` の「例外処理の境界 - Repository」または運用方針に「`404` はリポジトリで沈黙、サービスで WARN」と明記する。
- 修正対象候補: `00_docs/10_プラグイン設計書/feature/12-mob/5-例外・ログ・運用/12_5.00-例外・ログ・運用.md`
- 修正可否: `自動修正可`

## 未確認/質問

### Q-DOC-001
- 関連指摘: `AR-DOC-002`
- 確認事項: キラー特定のフォールバックは (a) `MobInstance.lastAttackerUuid` を新設して保持する設計か、(b) 脅威値テーブルに必ず加算する前提とし、フォールバック仕様自体を削除する設計か、どちらを採るか。
- 判断が必要な理由: 脅威値減衰（`decay`）の周期と `applyDamage` の脅威値加算がある以上、`top()` が `null` になるシナリオは限定的だが、`handleDeath` の現仕様がフォールバックを明示しているため、モデル拡張か仕様縮小のいずれかが必要。

### Q-DOC-002
- 関連指摘: `AR-DOC-005`
- 確認事項: 終了時の tick 停止は `MobService.destroyAll` の責務に含めるか、core 側で `MobAiService.stop` を別途呼ぶか。
- 判断が必要な理由: 概要には両方の動作が並列に書かれており、責務境界の決定が必要。

### Q-DOC-003
- 関連指摘: `AR-DOC-006`
- 確認事項: 表示中プレイヤー UUID 集合の保持先は `MobInstance` 側か `MobService` 側か。また `updateViewers` のオーナーは `MobService` か `MobAiService` か。
- 判断が必要な理由: モデル定義への項目追加・責務マッピング修正の方針がここで分岐する。

### Q-DOC-004
- 関連指摘: `AR-DOC-008`
- 確認事項: `/mob list` の対象はテンプレート（`templates` キャッシュ）かインスタンス（`instances` キャッシュ）か、両方か。
- 判断が必要な理由: 運用 docs が「件数を監視」と書いており、ユースケースを限定しないと実装が確定しない。

## 修正スキル入力サマリ
- 自動修正候補: `AR-DOC-004`, `AR-DOC-007`, `AR-DOC-009`
- 要確認: `AR-DOC-001`, `AR-DOC-002`, `AR-DOC-003`
- 設計判断待ち: `AR-DOC-005`, `AR-DOC-006`, `AR-DOC-008`, `Q-DOC-001`, `Q-DOC-002`, `Q-DOC-003`, `Q-DOC-004`
- 推奨修正順: `AR-DOC-004` -> `AR-DOC-007` -> `AR-DOC-009` -> `AR-DOC-001` -> `AR-DOC-002` -> `AR-DOC-003` -> `AR-DOC-008` -> `AR-DOC-005` -> `AR-DOC-006`
- 対象範囲: `00_docs/10_プラグイン設計書/feature/12-mob`

## 修正状態
- AR-DOC-001: 修正済み
- AR-DOC-002: 修正済み
- AR-DOC-003: 修正済み
- AR-DOC-004: 修正済み
- AR-DOC-005: 修正済み
- AR-DOC-006: 修正済み
- AR-DOC-007: 修正済み
- AR-DOC-008: 修正済み
- AR-DOC-009: 修正済み

## 確認した範囲
- 読んだ設計書:
  - `00_docs/10_プラグイン設計書/README.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/12_README.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/0-概要/12_0.00-概要.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/1-モデル定義/12_1.00-モデル定義.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/2-ユースケース/12_2.00-ユースケース.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.00-索引.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.02-サービス.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.03-戦闘.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.04-リポジトリ.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/3-メソッド仕様/12_3.05-パケット表示.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/4-統合フロー/12_4.00-統合フロー.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/5-例外・ログ・運用/12_5.00-例外・ログ・運用.md`
  - `00_docs/10_プラグイン設計書/feature/12-mob/9-未決事項/12_9.00-未決事項.md`
- 実行した検査: `docs_structure_audit.py 00_docs/10_プラグイン設計書/feature/12-mob`（Findings: 0）

## ソース参照
ソースコードは参照していません。
