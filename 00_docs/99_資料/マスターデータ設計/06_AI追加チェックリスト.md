# AI追加チェックリスト

## 1. 作業前

- `E:\AstralRecord-Workspace\AGENTS.md` を読んだ。
- `E:\AstralRecord-Workspace\40_filebase\AGENTS.md` を読んだ。
- このフォルダの `00_README.md` から `05_初期オーバーワールド制作ブリーフ.md` までを読んだ。
- 対象カテゴリの YAML スキーマを読んだ。
- 既存の近い YAML を 2-3 件読んだ。
- 追加する ID が既存 `40_filebase/**/*.yml` と重複していない。

## 2. 作成中

- 本番向け ID に `test`, `sample`, `lab` を使っていない。
- 参照先を作る前に参照していない。
- `StatusType.md` にない status を使っていない。
- rarity は序盤にふさわしい。
- プレイヤーに見える name / displayName / title / label / description / lore / message / body は日本語になっている。
- lore は短く、強制ストーリーを前提にしていない。
- Bukkit Material 名は既存で使われている表記に合わせている。

## 3. 作業後

- 追加 YAML をすべて読み返した。
- item / mob / loot / spawner の ref を確認した。
- ファイル配置がカテゴリと一致している。
- ID、表示名、lore、数値の用途を説明できる。
- Plugin/API/Web/Resource Pack の変更が必要か確認した。
- 必要な変更が filebase 外にある場合は、勝手に実装せず残事項にした。

## 4. 報告に含めること

- 追加した playable loop の概要。
- 追加・変更ファイル一覧。
- 主要な参照関係。
- 実行した検証。
- 次に作るとよいマスタ。
