# Geyser起動時に読み取る。変更・新規ヘッド反映にはProxyの再起動が必要。
api:
  baseUrl: "https://device_server:444"
  # 空の場合はapiKeyEnvironmentVariableの環境変数を参照する。
  apiKey: ""
  apiKeyEnvironmentVariable: "ASTRALRECORD_API_KEY"
  timeoutSeconds: 15
  # 開発・閉域環境で証明書とホスト名の検証を無効化する場合だけtrue。本番ではfalseにする。
  allowInsecureTls: false
